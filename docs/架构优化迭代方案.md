# SecAlert 架构优化迭代方案

**版本:** v2.0
**日期:** 2026-04-06
**状态:** 起草

---

## 1. 背景与目标

### 1.1 架构审视发现

当前系统存在以下核心问题：

| 问题 | 严重度 | 影响 |
|------|--------|------|
| **Kafka 无 ARM 支持** | 🔴 HIGH | Confluent Kafka 官方无 ARM 镜像，部署到鲲鹏/飞腾会失败 |
| **中间件数量过多** | 🔴 HIGH | 10+ 中间件，资源占用 ~9GB，仅中间件就需要这么多 |
| **设计过度** | 🟡 MEDIUM | ClickHouse/MinIO/Flink 均未实现，但占据架构核心 |
| **Neo4j 未适配 ARM** | 🟡 MEDIUM | Neo4j 5.x 之前无 ARM 支持，需升级版本 |
| **文档与实现脱节** | 🟡 MEDIUM | 声称 Python+Java+Rust+Go，实际 95% 都是 Python |
| **AI 定位模糊** | 🟡 MEDIUM | AI Copilot 仅作为辅助工具，未成为核心架构驱动力 |

### 1.2 核心约束

| 约束 | 要求 |
|------|------|
| **部署模式** | 私有化离线部署，无外部云依赖 |
| **处理能力** | 每天 3 万+ 条告警，延迟可接受 |
| **硬件平台** | 需要支持 x86_64 和 ARM64 (鲲鹏/飞腾) |
| **用户画像** | 非专业运维人员，界面必须极度简单 |
| **AI 推理** | 所有 AI 推理基于私有化 Qwen3-32B |

### 1.3 优化目标

| 维度 | 精简前 | 精简后 | 改善 |
|------|--------|--------|------|
| **中间件数量** | 10+ | 6 | -40% |
| **Java 进程** | 4 | 2 | -50% |
| **最低内存** | ~4GB | ~2GB | -50% |
| **ARM 支持** | 部分 | 完全 | 100% |
| **AI 定位** | 辅助工具 | **核心驱动力** | 架构升级 |

---

## 2. AI-Native 架构定位

### 2.1 核心理念转变

**关键洞察：** SecAlert 的底层模块（数据接入、数据解析、威胁建模、告警分析）不应仅服务于 Web UI，而应成为 **AI 原生应用** 的能力底座。

```
┌─────────────────────────────────────────────────────────────────┐
│              Claude Code Agent SDK (AI 助手)                      │
│                                                             │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐    │
│   │  Alert Skill │  │ Threat Skill │  │ Parser Skill │    │
│   └──────────────┘  └──────────────┘  └──────────────┘    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    SecAlert 底层能力                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐           │
│  │数据接入  │  │数据解析  │  │威胁建模  │  │告警分析  │           │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘           │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│              Web UI (辅助人类查看/操作)                            │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术架构演进

| 阶段 | 架构形态 | 主用户 | 交互方式 |
|------|----------|--------|----------|
| **当前** | 传统 Web + AI 辅助 | IT 运维人员 | GUI + AI Copilot |
| **目标** | AI-Native + Skill 支撑 | AI Agent | API / Skill 调用 |

---

## 3. 精简后的目标架构

### 3.1 中间件精简

| 精简前 | 精简后 | 理由 |
|--------|--------|------|
| Kafka + Zookeeper | **Kafka (KRaft 单节点)** | KRaft 模式无需 Zookeeper，支持 ARM |
| Elasticsearch + ClickHouse | **仅 Elasticsearch** | 3万/天告警，ES 聚合能力足够 |
| **Neo4j** | **保留** | 图关联查询、攻击链构建，Neo4j 5.x 支持 ARM |
| MinIO | **移除** | 告警数据直接存 ES 即可 |
| Logstash | **移除** | 与 Vector 功能重复，且无 ARM 支持 |

### 3.2 精简后中间件栈

| 组件 | 镜像 | 端口 | 用途 | ARM 状态 |
|------|------|------|------|----------|
| **Kafka** | apache/kafka:3.7.0 | 9092, 29092 | 消息队列 | ✅ 支持 |
| **Elasticsearch** | elasticsearch:8.11.0 | 9200 | 全文检索 + 存储 | ✅ 支持 |
| **Neo4j** | neo4j:5.18 | 7474, 7687 | 图数据库 | ✅ 支持 |
| **PostgreSQL** | postgres:16-alpine | 5432 | 关系存储 | ✅ 支持 |
| **Redis** | redis:7-alpine | 6379 | 缓存 + DLQ | ✅ 支持 |
| **Vector** | timberio/vector:0.40.0-alpine | 514, 515, 9001 | 数据采集 | ✅ 支持 |

### 3.3 目标架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           SecAlert 精简架构 (v2.0)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    数据采集层 (Vector)                                │   │
│  │  JDBC 轮询  ·  REST/Webhook  ·  Syslog  ·  文件监控                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Kafka (KRaft 单节点)                               │   │
│  │                    Topic: raw-events                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    三层解析管道 (Python)                              │   │
│  │  预置模板 (50+) → Drain 聚类 → DSPy/LLM                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    分析层 (Python)                                    │   │
│  │  规则引擎  ·  告警关联  ·  AI 分类                                    │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    存储层 (ES + Neo4j + PostgreSQL + Redis)            │   │
│  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐ ┌───────────┐ │   │
│  │  │ Elasticsearch│  │    Neo4j     │  │  PostgreSQL  │ │   Redis   │ │   │
│  │  │ 全文检索+存储 │  │  攻击链图   │  │   关系存储   │ │ 缓存+DLQ  │ │   │
│  │  └─────────────┘  └──────────────┘  └──────────────┘ └───────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    API 层 (FastAPI)                                  │   │
│  │  REST + WebSocket + MCP Server + Prometheus Metrics                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              MCP Server / Skill 封装层 (TypeScript)                    │   │
│  │  alert_ingestion | alert_parser | threat_classifier | attack_chain  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      ↓                                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    前端层 (React + TypeScript)                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 插件系统设计

### 4.1 设计背景

数据接入、数据解析、数据规整层需要适配多种异构数据源（防火墙、IDS、VPN、WAF 等），采用**可插拔插件架构**实现灵活扩展。

```
┌─────────────────────────────────────────────────────────────────┐
│                     Collector Plugin System                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Plugin Registry                         │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │  │
│  │  │ Fortinet│ │Suricata │ │PaloAlto │ │CiscoASA │   ...   │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘         │  │
│  │                     可热插拔                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Plugin Interface (ABC)                  │  │
│  │  - collect() → List[RawEvent]                            │  │
│  │  - validate_config() → bool                              │  │
│  │  - get_supported_types() → List[str]                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              ↓                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    Core Engine                             │  │
│  │  数据解析 → 数据规整 → 威胁建模 → 告警分析 → 处置        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 插件接口定义

```python
# collector/plugins/base.py
from abc import ABC, abstractmethod
from dataclasses import dataclass
from typing import List, Dict, Any, Optional

@dataclass
class RawEvent:
    """原始事件"""
    uid: str                              # 事件唯一 ID
    timestamp: str                        # ISO 格式时间戳
    source_type: str                      # 数据源类型
    source_name: str                      # 数据源名称
    raw_content: str                      # 原始内容
    format_type: str                      # 格式类型: cef, syslog, json, etc.
    metadata: Dict[str, Any]              # 额外元数据

class CollectorPlugin(ABC):
    """数据采集插件接口"""
    
    @property
    @abstractmethod
    def name(self) -> str:
        """插件名称（唯一标识）"""
        pass
    
    @property
    def supported_types(self) -> List[str]:
        """支持的设备/数据源类型"""
        return [self.name]
    
    @property
    def version(self) -> str:
        """插件版本"""
        return "1.0.0"
    
    @abstractmethod
    async def collect(self) -> List[RawEvent]:
        """采集数据，返回原始事件列表"""
        pass
    
    @abstractmethod
    async def validate_config(self, config: Dict[str, Any]) -> bool:
        """验证插件配置，返回是否有效"""
        pass
    
    async def initialize(self, config: Dict[str, Any]):
        """初始化插件（可选实现）"""
        pass
    
    async def shutdown(self):
        """关闭插件（可选实现）"""
        pass
```

### 4.3 插件示例实现

```python
# collector/plugins/fortinet.py
class FortinetPlugin(CollectorPlugin):
    """Fortinet 防火墙插件"""
    
    name = "fortinet"
    supported_types = ["fortinet_firewall", "fortigate", "fortios"]
    
    def __init__(self):
        self._client = None
    
    async def validate_config(self, config: Dict[str, Any]) -> bool:
        required = ["host", "api_token"]
        return all(k in config for k in required)
    
    async def initialize(self, config: Dict[str, Any]):
        # 初始化 Fortinet API 客户端
        self._client = FortinetAPIClient(
            host=config["host"],
            api_token=config["api_token"]
        )
    
    async def collect(self) -> List[RawEvent]:
        events = []
        logs = await self._client.get_logs()
        for log in logs:
            events.append(RawEvent(
                uid=generate_uid(),
                timestamp=log["eventtime"],
                source_type=self.name,
                source_name=f"fortinet-{self._client.host}",
                raw_content=log["raw_message"],
                format_type="fortinet_log",
                metadata={"device_vendor": "Fortinet"}
            ))
        return events
```

```python
# collector/plugins/suricata.py
class SuricataPlugin(CollectorPlugin):
    """Suricata IDS/EVE.JSON 插件"""
    
    name = "suricata"
    supported_types = ["suricata_ids", "suricata_eve_json"]
    
    async def validate_config(self, config: Dict[str, Any]) -> bool:
        return "eve_json_path" in config
    
    async def collect(self) -> List[RawEvent]:
        events = []
        eve_path = self._config["eve_json_path"]
        # 读取并解析 Eve.JSON 文件
        for line in tail_file(eve_path):
            if line.strip():
                events.append(RawEvent(
                    uid=generate_uid(),
                    timestamp=datetime.now().isoformat(),
                    source_type=self.name,
                    source_name="suricata-ids",
                    raw_content=line,
                    format_type="json",
                    metadata={"parser": "suricata_eve"}
                ))
        return events
```

```python
# collector/plugins/syslog.py
class SyslogPlugin(CollectorPlugin):
    """Syslog 通用插件"""
    
    name = "syslog"
    supported_types = ["syslog", "rfc3164", "rfc5424"]
    
    async def validate_config(self, config: Dict[str, Any]) -> bool:
        return "port" in config
    
    async def collect(self) -> List[RawEvent]:
        # 从 Syslog UDP/TCP 端口接收事件
        ...
```

### 4.4 插件管理器

```python
# collector/plugin_manager.py
class PluginManager:
    """插件管理器"""
    
    def __init__(self):
        self._plugins: Dict[str, CollectorPlugin] = {}
        self._logger = get_logger(__name__)
    
    def register(self, plugin: CollectorPlugin):
        """注册插件"""
        if plugin.name in self._plugins:
            raise ValueError(f"Plugin {plugin.name} already registered")
        self._plugins[plugin.name] = plugin
        self._logger.info(f"Registered plugin: {plugin.name}")
    
    def unregister(self, name: str):
        """注销插件"""
        if name in self._plugins:
            plugin = self._plugins.pop(name)
            asyncio.create_task(plugin.shutdown())
            self._logger.info(f"Unregistered plugin: {name}")
    
    def get_plugin(self, name: str) -> Optional[CollectorPlugin]:
        """获取插件"""
        return self._plugins.get(name)
    
    def list_plugins(self) -> List[str]:
        """列出所有已注册插件"""
        return list(self._plugins.keys())
    
    async def collect_all(self) -> List[RawEvent]:
        """采集所有插件的数据"""
        all_events = []
        for plugin in self._plugins.values():
            try:
                events = await plugin.collect()
                all_events.extend(events)
                self._logger.debug(f"Collected {len(events)} events from {plugin.name}")
            except Exception as e:
                self._logger.error(f"Plugin {plugin.name} collect failed: {e}")
        return all_events
    
    def discover_plugins(self, plugin_dir: str = "collector/plugins"):
        """自动发现并加载插件"""
        import importlib
        import inspect
        
        for filename in os.listdir(plugin_dir):
            if filename.endswith("_plugin.py") and not filename.startswith("_"):
                module_name = filename[:-3]
                module = importlib.import_module(f"{plugin_dir}.{module_name}")
                
                for name, obj in inspect.getmembers(module):
                    if (inspect.isclass(obj) 
                        and issubclass(obj, CollectorPlugin) 
                        and obj != CollectorPlugin):
                        self.register(obj())
```

### 4.5 配置驱动

```yaml
# config/collector_plugins.yaml
collector:
  plugins:
    enabled:
      - fortinet
      - suricata
      - palo_alto
      - cisco_asa
      - syslog
    
    # Fortinet 防火墙
    fortinet:
      enabled: true
      host: "${FORTINET_HOST}"
      api_token: "${FORTINET_API_TOKEN}"
      interval_seconds: 60
      filter: "severity eq high"
    
    # Suricata IDS
    suricata:
      enabled: true
      eve_json_path: "/var/log/suricata/eve.json"
      position_file: "/var/lib/suricata/eve.json.pos"
      interval_seconds: 10
    
    # Palo Alto Networks
    palo_alto:
      enabled: true
      host: "${PaloAlto_HOST}"
      api_key: "${PaloAlto_API_KEY}"
      interval_seconds: 60
    
    # Cisco ASA
    cisco_asa:
      enabled: true
      syslog_host: "0.0.0.0"
      syslog_port: 514
      protocol: "udp"
    
    # Syslog 通用
    syslog:
      enabled: true
      port: 514
      protocol: "udp"
      address: "0.0.0.0"
```

### 4.6 内置插件清单

| 插件名称 | 支持类型 | 协议/格式 | 状态 |
|----------|----------|-----------|------|
| `fortinet` | FortiGate, FortiOS | REST API | 内置 |
| `suricata` | Suricata IDS | EVE.JSON | 内置 |
| `palo_alto` | Palo Alto PAN-OS | REST API | 内置 |
| `cisco_asa` | Cisco ASA | Syslog | 内置 |
| `syslog` | 通用 Syslog | RFC3164/RFC5424 | 内置 |
| `check_point` | Check Point | Syslog | 内置 |
| `snort` | Snort IDS | Unified2 | 内置 |
| `openvpn` | OpenVPN | Syslog | 内置 |

### 4.7 插件与 Skill 的关系

```
┌─────────────────────────────────────────────────────────────┐
│                    AI Agent (Claude Code)                    │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                    MCP Server / Skill                       │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ alert_ingestion Skill                                 │  │
│  │ - 调用 Collector Plugin 获取原始数据                   │  │
│  │ - 返回标准化事件列表                                   │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ alert_parser Skill                                   │  │
│  │ - 调用 Parser Plugin 解析异构格式                     │  │
│  │ - 返回 OCSF 标准格式                                   │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                 Plugin System (可热插拔)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           │
│  │ Fortinet│ │Suricata │ │PaloAlto │ │CiscoASA │   ...   │
│  │ Plugin  │ │ Plugin  │ │ Plugin  │ │ Plugin  │          │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘           │
└─────────────────────────────────────────────────────────────┘
```

### 4.8 演进路径

| 阶段 | 形态 | 说明 |
|------|------|------|
| **Phase 1** | 单体 + 插件接口 | 代码层面定义插件接口，实现内置插件 |
| **Phase 2** | 配置驱动 | 插件通过配置启用/禁用 |
| **Phase 3** | 动态加载 | 支持运行时加载第三方插件 |
| **Phase 4** | 独立服务 | 如有需要，Collector 作为独立微服务 |

---

## 5. API 与 Skill 设计

### 5.1 API 设计原则

| 原则 | 说明 |
|------|------|
| **面向 Agent 设计** | API 应服务于 AI Agent，而非仅服务于 Web UI |
| **标准化输入输出** | 每个 API 有明确的输入 schema 和输出 schema |
| **可组合调用** | API 之间可相互调用，支持复杂工作流 |
| **可观测性** | 每个 API 调用都有日志和指标 |

### 5.2 核心 API

| API | 方法 | 描述 | 面向用户 |
|-----|------|------|----------|
| `/api/alerts` | GET/POST | 告警 CRUD | Web UI / AI Agent |
| `/api/alerts/{id}` | GET | 告警详情 | Web UI / AI Agent |
| `/api/parser/parse` | POST | 解析原始告警 | AI Agent |
| `/api/classifier/classify` | POST | AI 分类 | AI Agent |
| `/api/chains` | GET | 攻击链列表 | Web UI / AI Agent |
| `/api/chains/{id}` | GET | 攻击链详情 | Web UI / AI Agent |
| `/api/remediation/{alert_id}` | GET | 处置建议 | Web UI / AI Agent |

### 5.3 Skill 封装

每个底层模块封装为可被 AI Agent 调用的 Skill：

```typescript
// SecAlert Skills for AI Agent

// Skill 1: 告警接入
const alertIngestionSkill = {
  name: "alert_ingestion",
  description: "接收并处理原始安全告警",
  input: {
    device: "string",      // 设备类型: firewall, ids, vpn, etc.
    raw: "string",         // 原始告警内容
    format: "string"       // 格式: cef, syslog, json, etc.
  },
  output: {
    event_id: "string",
    normalized: "object",   // OCSF 标准化格式
    confidence: "number"
  }
};

// Skill 2: 告警解析
const alertParserSkill = {
  name: "alert_parser",
  description: "解析异构告警格式",
  input: {
    format: "cef | syslog | json | custom",
    raw: "string"
  },
  output: {
    parsed: "object",
    confidence: "number",
    matched_template: "string | null"
  }
};

// Skill 3: 威胁分类
const threatClassifierSkill = {
  name: "threat_classifier",
  description: "判断威胁类型和严重度",
  input: {
    alert: "object"        // 解析后的告警
  },
  output: {
    threat_type: "string",
    severity: "Critical | High | Medium | Low",
    confidence: "number",
    mitre_techniques: "string[]"
  }
};

// Skill 4: 误报检测
const falsePositiveDetectorSkill = {
  name: "false_positive_detector",
  description: "检测误报",
  input: {
    alert: "object"
  },
  output: {
    is_false_positive: "boolean",
    reason: "string",
    confidence: "number"
  }
};

// Skill 5: 攻击链构建
const attackChainBuilderSkill = {
  name: "attack_chain_builder",
  description: "还原攻击链",
  input: {
    alert_ids: "string[]",
    time_window_seconds: "number"
  },
  output: {
    chain_id: "string",
    steps: "object[]",
    attack_path: "object"
  }
};

// Skill 6: 处置建议
const remediationSkill = {
  name: "remediation",
  description: "生成处置建议",
  input: {
    alert: "object",
    threat_type: "string"
  },
  output: {
    actions: "string[]",
    priority: "number",
    estimated_time: "string"
  }
};
```

### 5.4 MCP Server 实现

```typescript
// src/mcp/server.ts
import { createServer, Tool } from '@modelcontextprotocol/sdk/server';
import { queryAlerts } from './tools/query_alerts';
import { parseAlert } from './tools/parse_alert';
import { classifyThreat } from './tools/classify_threat';
import { buildAttackChain } from './tools/build_attack_chain';

const tools: Tool[] = [
  {
    name: 'secalert_query_alerts',
    description: '查询安全告警列表，支持按严重度和时间范围筛选',
    inputSchema: {
      type: 'object',
      properties: {
        severity: { type: 'string', enum: ['critical', 'high', 'medium', 'low'] },
        time_range: { type: 'string', default: 'now-1h~now' },
        limit: { type: 'number', default: 10 }
      }
    },
    handler: queryAlerts
  },
  {
    name: 'secalert_parse_alert',
    description: '解析原始告警为标准化格式',
    inputSchema: {
      type: 'object',
      properties: {
        raw: { type: 'string' },
        format: { type: 'string' }
      }
    },
    handler: parseAlert
  },
  {
    name: 'secalert_classify_threat',
    description: 'AI 分类告警，判断威胁类型',
    inputSchema: {
      type: 'object',
      properties: {
        alert_id: { type: 'string' }
      }
    },
    handler: classifyThreat
  },
  {
    name: 'secalert_build_attack_chain',
    description: '根据告警 ID 构建攻击链',
    inputSchema: {
      type: 'object',
      properties: {
        alert_ids: { type: 'array', items: { type: 'string' } },
        time_window_seconds: { type: 'number', default: 3600 }
      }
    },
    handler: buildAttackChain
  }
];

const server = createServer({
  name: 'SecAlert MCP Server',
  version: '1.0.0',
  tools
});

server.listen(3001);
```

---

## 6. 迁移路径

### 6.1 Phase 1: 立即清理（1-2 周）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| **Kafka ARM 适配** | P0 | 换成 apache/kafka:3.7.0 + KRaft 模式 |
| **移除 Zookeeper** | P0 | Kafka KRaft 模式无需 Zookeeper |
| **移除 Logstash** | P1 | 统一使用 Vector |
| **验证 Elasticsearch ARM** | P1 | 确认 elasticsearch:8.11.0 支持 arm64 |
| **更新文档** | P2 | 移除 ClickHouse/MinIO/Flink 规划，保留 Neo4j |

### 6.2 Phase 2: 架构简化（2-4 周）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| **Vector 统一采集** | P1 | Vector 接管所有采集任务 |
| **移除 MinIO** | P1 | 告警数据直接存 ES |
| **Neo4j ARM 适配** | P1 | 使用 neo4j:5.18 替换社区版 |
| **DLQ 优化** | P2 | 使用 Redis Sorted Set 统一 DLQ |
| **文档更新** | P2 | 同步更新架构文档 |

### 6.3 Phase 3: AI-Native 改造（4-6 周）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| **MCP Server 实现** | P1 | 暴露底层能力给 AI Agent |
| **Skill 封装** | P1 | 将底层模块封装为可调用 Skill |
| **API 重新设计** | P2 | 从面向 GUI 转为面向 Agent |
| **Claude Code 集成** | P2 | 作为 AI 助手后端 |

### 6.4 Phase 4: 持续优化（按需）

| 任务 | 条件 | 说明 |
|------|------|------|
| **Neo4j 优化** | 图查询性能调优 | Cypher 查询优化、索引优化 |
| **Flink 引入** | 告警量 > 50万/天 | 真正的流处理 |

---

## 7. 精简后 docker-compose

```yaml
# docker-compose.minimal.yml
version: '3.8'

services:
  # API 服务
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
      # 指定多架构构建
      args:
        - BUILDPLATFORM=linux/amd64
        - TARGETPLATFORM=linux/arm64
    platform: linux/amd64  # 本地开发用 amd64，CI/CD 需根据目标平台构建
    ports:
      - "8000:8000"
    environment:
      - REDIS_URL=redis://redis:6379
      - KAFKA_BROKERS=kafka:29092
      - ES_HOST=http://elasticsearch:9200
      - POSTGRES_URL=postgresql://postgres:5432/secalert
      - NEO4J_URI=bolt://neo4j:7687
    depends_on:
      - redis
      - kafka
      - elasticsearch
      - postgres
      - neo4j
    networks:
      - secalert

  # 前端
  frontend:
    build:
      context: ./frontend
      # 指定多架构构建
      args:
        - BUILDPLATFORM=linux/amd64
        - TARGETPLATFORM=linux/arm64
    platform: linux/amd64
    ports:
      - "3000:3000"
    depends_on:
      - api
    networks:
      - secalert

  # Kafka (KRaft 模式，无需 Zookeeper)
  kafka:
    image: apache/kafka:3.7.0
    ports:
      - "9092:9092"
      - "29092:29092"
    environment:
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_NODE_ID: 1
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29092
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
    networks:
      - secalert

  # Elasticsearch
  elasticsearch:
    image: elasticsearch:8.11.0
    ports:
      - "9200:9200"
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
    networks:
      - secalert

  # Neo4j (图数据库，攻击链关联)
  neo4j:
    image: neo4j:5.18
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      - NEO4J_AUTH=neo4j/secret
    networks:
      - secalert

  # PostgreSQL
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=secalert
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - secalert

  # Redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    networks:
      - secalert

  # Vector (统一采集)
  vector:
    image: timberio/vector:0.40.0-alpine
    volumes:
      - ./collector/configs:/etc/vector
      - /var/log:/var/log
    ports:
      - "514:514/udp"
      - "514:514/tcp"
      - "515:515/tcp"
    depends_on:
      - kafka
    networks:
      - secalert

networks:
  secalert:
    driver: bridge

volumes:
  postgres_data:
```

---

## 8. 技术栈对比

| 维度 | 精简前 | 精简后 |
|------|--------|--------|
| **语言** | Python + Java/Scala + Rust + TypeScript | 仅 Python + TypeScript |
| **中间件** | 10+ 个 | 6 个核心 |
| **Java 进程** | Kafka, ES, Logstash, Neo4j (4个) | Kafka, Neo4j (2个) |
| **最低内存** | ~4GB | ~2GB |
| **建议内存** | ~9GB | ~5GB |
| **ARM 支持** | 部分 | 完全 |
| **AI 定位** | 辅助工具 | 核心驱动力 |

---

## 9. 附录

### A. ARM 镜像验证清单

| 序号 | 组件 | 镜像 | ARM 验证命令 |
|------|------|------|--------------|
| 1 | Kafka | apache/kafka:3.7.0 | `docker pull --platform linux/arm64 apache/kafka:3.7.0` |
| 2 | Elasticsearch | elasticsearch:8.11.0 | `docker pull --platform linux/arm64 elasticsearch:8.11.0` |
| 3 | Neo4j | neo4j:5.18 | `docker pull --platform linux/arm64 neo4j:5.18` |
| 4 | PostgreSQL | postgres:16-alpine | `docker pull --platform linux/arm64 postgres:16-alpine` |
| 5 | Redis | redis:7-alpine | `docker pull --platform linux/arm64 redis:7-alpine` |
| 6 | Vector | timberio/vector:0.40.0-alpine | `docker pull --platform linux/arm64 timberio/vector:0.40.0-alpine` |

### B. Dockerfile 多架构构建

#### API Dockerfile (Python)

```dockerfile
# Dockerfile.api
FROM --platform=$BUILDPLATFORM python:3.10-slim AS builder

ARG TARGETPLATFORM
ARG BUILDPLATFORM

# 安装 buildx 支持
RUN apt-get update && apt-get install -y qemu-user-static

# ... 依赖安装 ...

FROM python:3.10-slim

# ARM 兼容性：使用 python:3.10-slim 官方镜像支持 amd64 和 arm64
COPY --from=builder /usr/local/lib/python3.10/site-packages /usr/local/lib/python3.10/site-packages
COPY ./src /app/src

WORKDIR /app
CMD ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### Frontend Dockerfile (Node.js)

```dockerfile
# frontend/Dockerfile
FROM --platform=$BUILDPLATFORM node:20-alpine AS builder

ARG TARGETPLATFORM
ARG BUILDPLATFORM

RUN apk add --no-cache python3 make g++

# ... 构建步骤 ...

FROM node:20-alpine

# ARM 兼容性：node:20-alpine 官方镜像支持 amd64 和 arm64
COPY --from=builder /app/dist /app/dist
WORKDIR /app

EXPOSE 3000
CMD ["npm", "start"]
```

### C. 多架构构建与推送

```bash
# 1. 启用 Docker buildx
docker buildx create --use

# 2. 构建多架构镜像
docker buildx build \
  --platform linux/amd64,linux/arm64 \
  --tag secalert/api:v2.0 \
  --push \
  .

# 3. 验证镜像
docker buildx imagetools inspect secalert/api:v2.0
```

### D. 插件系统 ARM 兼容性

| 插件 | 技术依赖 | ARM 兼容 | 说明 |
|------|----------|----------|------|
| `fortinet` | Python + REST API | ✅ | 纯 Python，无 native 依赖 |
| `suricata` | Python + 文件读取 | ✅ | 纯 Python，无 native 依赖 |
| `palo_alto` | Python + REST API | ✅ | 纯 Python，无 native 依赖 |
| `cisco_asa` | Python + Syslog | ✅ | 纯 Python，无 native 依赖 |
| `syslog` | Python + socket | ✅ | 纯 Python，无 native 依赖 |

> **注意：** 插件系统使用纯 Python 实现，无 C 扩展或 native 依赖，完全跨平台。

### E. 移除组件清单

| 组件 | 移除方式 | 替代方案 |
|------|----------|----------|
| Zookeeper | Kafka KRaft 模式 | 无需替代 |
| Logstash | 移除容器 + 配置 | Vector 接管 |
| ClickHouse | 从架构中移除 | Elasticsearch aggregation |
| MinIO | 移除容器 | Elasticsearch 存储 |

### F. 保留组件说明

| 组件 | 保留理由 |
|------|----------|
| **Neo4j** | 图关联查询、攻击链构建，Neo4j 5.18 支持 ARM64 |

### G. 关键文件路径

| 功能 | 文件路径 |
|------|----------|
| 当前架构文档 | `/home/admin/work/SecAlert/docs/详细设计方案.md` |
| Docker 配置 | `/home/admin/work/SecAlert/docker-compose.dev.yml` |
| Kafka Consumer | `/home/admin/work/SecAlert/src/chain/kafka_consumer.py` |
| Vector 配置 | `/home/admin/work/SecAlert/collector/configs/` |
| MCP Server | `/home/admin/work/SecAlert/src/mcp/` |
| API 入口 | `/home/admin/work/SecAlert/src/api/main.py` |
