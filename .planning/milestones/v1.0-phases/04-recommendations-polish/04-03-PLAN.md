---
phase: 04-recommendations-polish
plan: 04-03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/api/remediation_endpoints.py
  - frontend/src/components/AlertList.tsx
  - frontend/src/index.css
  - frontend/index.html
autonomous: true
requirements:
  - REMED-01
  - UI-01

must_haves:
  truths:
    - "响应平台可通过 API 获取处置建议"
    - "API 支持严重度过滤参数"
    - "前端支持已抑制告警 Tab"
    - "运维人员可在单屏完成完整响应流程"
  artifacts:
    - path: "src/api/remediation_endpoints.py"
      provides: "响应平台 API（列表、详情、状态查询）"
      exports: "GET /api/remediation/chains, GET /api/remediation/platform/status"
  key_links:
    - from: "响应平台"
      to: "/api/remediation/chains"
      via: "API 调用获取告警列表"
    - from: "响应平台"
      to: "/api/remediation/chains/{chain_id}"
      via: "API 调用获取处置建议"
---

<objective>
完成 API 集成：响应平台 API + 完善前端已抑制告警 Tab。

Purpose: Phase 4 完整交付，API 供响应平台调用，前端体验完善
Output: 完整 API 端点 + 前端 Tab 功能
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-recommendations-polish/04-01-PLAN.md
@.planning/phases/04-recommendations-polish/04-02-PLAN.md
@src/api/remediation_endpoints.py
@src/analysis/service.py

## Phase 4 Wave 3 上下文

Wave 3 在 Wave 1（处置建议后端）和 Wave 2（React 前端）基础上：
1. 扩展 API 端点（响应平台 API per D-11）
2. 完善前端已抑制告警 Tab 功能
3. 添加必要的样式文件

## D-11 要求（响应平台 API）
- 攻击链详情 API（含处置建议）✓ 已在 04-01 实现
- 告警状态查询 API ✗ 需要在 Wave 3 实现
- 不提供：响应执行 API（由响应平台自行实现）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展响应平台 API 端点</name>
  <files>src/api/remediation_endpoints.py</files>
  <action>
扩展 `src/api/remediation_endpoints.py`，添加响应平台 API 端点：

```python
"""处置建议 API 接口

FastAPI endpoints for remediation recommendations
Phase 4 API endpoints - 响应平台集成

Per D-11: 提供 API 接口供威胁响应平台调用
- 攻击链详情 API（含处置建议）✓
- 告警状态查询 API ✗ 需要添加
"""

from fastapi import APIRouter, HTTPException, Query
from typing import Optional

from src.analysis.remediation.advisor import RemediationAdvisor
from src.analysis.service import AnalysisService
from src.graph.client import Neo4jClient

router = APIRouter(prefix="/api/remediation", tags=["remediation"])

# ... (保留 Wave 1 的端点)
# - GET /chains/{chain_id}
# - POST /chains/{chain_id}/acknowledge
# - POST /chains/{chain_id}/restore

# 添加 Wave 3 新端点：

@router.get("/chains")
async def list_remediation_chains(
    limit: int = Query(default=50, ge=1, le=100),
    offset: int = Query(default=0, ge=0),
    severity: Optional[str] = Query(default="critical,high"),
    status: Optional[str] = Query(default="active", pattern="^(active|resolved|false_positive|all)$")
) -> dict:
    """列出攻击链（支持严重度过滤）

    响应平台调用此端点获取待处理告警列表

    - **limit**: 返回数量 (1-100)
    - **offset**: 偏移量
    - **severity**: 严重度过滤 (critical,high,medium,low,all)，默认 critical,high
    - **status**: 状态过滤 (active,resolved,false_positive,all)，默认 active

    Returns:
    - chains: 攻击链列表（只包含基本信息，不含完整告警详情）
    - total: 总数
    - limit, offset: 分页信息
    """
    neo4j = Neo4jClient()

    # 获取链列表
    if status == "all":
        chains = neo4j.list_chains(limit=limit, offset=offset)
    else:
        chains = neo4j.list_chains(limit=limit, offset=offset, status=status)

    neo4j.close()

    # 严重度过滤
    if severity != "all":
        severity_levels = severity.split(",")
        chains = [c for c in chains if c.get("max_severity") in severity_levels]

    return {
        "chains": chains,
        "total": len(chains),
        "limit": limit,
        "offset": offset
    }


@router.get("/platform/status")
async def get_platform_status() -> dict:
    """获取平台状态

    响应平台可调用此端点检查 SecAlert 平台状态

    Returns:
    - status: "ready" | "degraded" | "error"
    - version: SecAlert 版本
    - timestamp: 当前服务器时间
    """
    from datetime import datetime

    try:
        # 检查 Neo4j 连接
        neo4j = Neo4jClient()
        test_result = neo4j.list_chains(limit=1, offset=0)
        neo4j.close()

        return {
            "status": "ready",
            "version": "1.0.0",
            "timestamp": datetime.utcnow().isoformat() + "Z",
            "checks": {
                "neo4j": "connected"
            }
        }
    except Exception as e:
        return {
            "status": "degraded",
            "version": "1.0.0",
            "timestamp": datetime.utcnow().isoformat() + "Z",
            "checks": {
                "neo4j": "error"
            },
            "error": str(e)
        }


@router.get("/chains/{chain_id}/full")
async def get_chain_full(chain_id: str) -> dict:
    """获取攻击链完整详情（包含告警列表）

    与 /chains/{chain_id} 不同，此端点返回完整的告警列表

    - **chain_id**: 攻击链 ID

    Returns:
    - 完整的攻击链数据，包含所有告警详情
    """
    neo4j = Neo4jClient()
    chain_data = neo4j.get_chain_by_id(chain_id)
    neo4j.close()

    if not chain_data:
        raise HTTPException(status_code=404, detail=f"Chain {chain_id} not found")

    # 生成处置建议（可选，由调用方决定是否需要）
    # 注意：这会增加响应时间，谨慎使用

    return chain_data
```

**关键要求：**
- GET /api/remediation/chains - 列表端点，支持严重度和状态过滤
- GET /api/remediation/platform/status - 平台状态检查
- GET /api/remediation/chains/{chain_id}/full - 完整告警列表
</action>
  <verify>
<automated>cd /home/admin/work/SecAlert && python3 -c "
from src.api.remediation_endpoints import router
routes = [r.path for r in router.routes]
print('Routes:', routes)
print('Has /chains:', '/chains' in routes)
print('Has /platform/status:', '/platform/status' in routes)
"</automated>
  </verify>
  <done>响应平台 API 端点已添加：/chains 列表、/platform/status、/chains/{chain_id}/full</done>
</task>

<task type="auto">
  <name>Task 2: 完善前端已抑制告警 Tab</name>
  <files>frontend/src/components/AlertList.tsx</files>
  <action>
完善 `frontend/src/components/AlertList.tsx` 的已抑制告警 Tab 功能：

当前实现使用 `fetchFalsePositives` 获取误报列表，但需要确保：
1. 显示被 Phase 3 错误抑制的告警（status=false_positive）
2. 支持从已抑制列表恢复为活跃状态

更新 AlertList 组件的已抑制告警部分，确保：
```typescript
// 已抑制告警 - 调用 /api/chains?status=false_positive
async function loadFalsePositives() {
  // 使用 /api/chains?status=false_positive 获取误报列表
  const response = await fetch(`${API_BASE}/chains?limit=50&offset=0&status=false_positive`);
  // ...
}
```

同时确保 Tab 切换时正确刷新列表。
</action>
  <verify>
<automated>grep -l "false_positive" frontend/src/components/AlertList.tsx && grep "status.*false_positive" frontend/src/components/AlertList.tsx</automated>
  </verify>
  <done>已抑制告警 Tab 功能已完善，支持查看和恢复误报</done>
</task>

<task type="auto">
  <name>Task 3: 添加前端基础样式</name>
  <files>frontend/src/index.css, frontend/index.html</files>
  <action>
创建 `frontend/src/index.css`：

```css
/* SecAlert 前端基础样式 */

@tailwind base;
@tailwind components;
@tailwind utilities;

/* 基础重置 */
body {
  @apply bg-slate-50 text-slate-900 antialiased;
}

/* 滚动条样式 */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  @apply bg-slate-100;
}

::-webkit-scrollbar-thumb {
  @apply bg-slate-300 rounded;
}

::-webkit-scrollbar-thumb:hover {
  @apply bg-slate-400;
}

/* Focus ring */
*:focus-visible {
  @apply outline-none ring-2 ring-blue-500 ring-offset-2;
}
```

创建 `frontend/index.html`：

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SecAlert - 安全告警分析</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**关键要求：**
- 使用 Tailwind CSS 基础样式
- 响应式布局支持
- 中文语言设置
</action>
  <verify>
<automated>ls frontend/index.html frontend/src/index.css 2>/dev/null && echo "Files created"</automated>
  </verify>
  <done>前端样式文件已创建，支持 Tailwind CSS</done>
</task>

<task type="auto">
  <name>Task 4: 更新 __init__.py 导出</name>
  <files>src/analysis/remediation/__init__.py</files>
  <action>
更新 `src/analysis/remediation/__init__.py`，确保所有模块正确导出：

```python
"""处置建议生成模块

规则优先 + LLM 兜底策略生成通俗易懂的处置建议
Phase 4 核心模块
"""

from .advisor import RemediationAdvisor
from .templates import RemediationTemplates
from .timeline import simplify_chain_timeline, TACTIC_NAMES
from .signatures import RemediationRecommendationSignature

__all__ = [
    "RemediationAdvisor",
    "RemediationTemplates",
    "simplify_chain_timeline",
    "TACTIC_NAMES",
    "RemediationRecommendationSignature",
]
```
</action>
  <verify>
<automated>cd /home/admin/work/SecAlert && python3 -c "from src.analysis.remediation import RemediationAdvisor, simplify_chain_timeline; print('Import OK')"</automated>
  </verify>
  <done>模块导出已更新，所有公共 API 可正确导入</done>
</task>

<task type="auto">
  <name>Task 5: 验证 Phase 4 完整流程</name>
  <files>-</files>
  <action>
最终验证 Phase 4 完整流程：

1. **后端验证**:
```bash
cd /home/admin/work/SecAlert && python3 -c "
# 1. 导入检查
from src.analysis.remediation import RemediationAdvisor, simplify_chain_timeline
from src.api.remediation_endpoints import router

# 2. 模板加载
advisor = RemediationAdvisor()
print('Templates loaded:', len(advisor.templates.templates))

# 3. 测试数据
test_data = {
    'asset_ip': '10.0.0.5',
    'alerts': [{
        'src_ip': '192.168.1.100',
        'mitre_technique_id': 'T1190',
        'alert_signature': 'ET EXPLOIT SQL Injection',
        'severity': 3,
        'mitre_tactic': 'TA0001'
    }]
}

# 4. 生成建议
rec = advisor.get_recommendation(test_data)
print('Source:', rec.get('source'))
print('Short action:', rec.get('short_action', '')[:60])

# 5. 生成时间线
timeline = advisor.get_timeline(test_data)
print('Timeline nodes:', len(timeline['nodes']))
"
```

2. **API 端点验证**:
```bash
cd /home/admin/work/SecAlert && python3 -c "
from src.api.remediation_endpoints import router
routes = sorted([r.path for r in router.routes])
for r in routes:
    print(r)
"
```

预期输出：
```
/chains/{chain_id}
/chains/{chain_id}/acknowledge
/chains/{chain_id}/full
/chains/{chain_id}/restore
/platform/status
```

3. **前端文件验证**:
```bash
echo "=== 前端文件 ===" && ls frontend/src/components/*.tsx | xargs -I {} basename {}
echo "=== API 客户端 ===" && ls frontend/src/api/*.ts
echo "=== 类型定义 ===" && ls frontend/src/types/*.ts
```
</action>
  <verify>
<automated>cd /home/admin/work/SecAlert && python3 -c "
from src.analysis.remediation import RemediationAdvisor, simplify_chain_timeline
advisor = RemediationAdvisor()
test_data = {
    'asset_ip': '10.0.0.5',
    'alerts': [{'src_ip': '192.168.1.100', 'mitre_technique_id': 'T1190', 'alert_signature': 'Test', 'severity': 3, 'mitre_tactic': 'TA0001'}]
}
rec = advisor.get_recommendation(test_data)
print('Source:', rec.get('source'))
print('Has short_action:', bool(rec.get('short_action')))
timeline = advisor.get_timeline(test_data)
print('Timeline nodes:', len(timeline['nodes']))
"</automated>
  </verify>
  <done>Phase 4 完整流程验证通过</done>
</task>

</tasks>

<verification>
## Phase 4 Wave 3 验证

1. **API 端点检查**:
```bash
cd /home/admin/work/SecAlert && python3 -c "
from src.api.remediation_endpoints import router
print('API Routes:')
for r in sorted([r.path for r in router.routes]):
    print(' ', r)
"
```

2. **模块导入检查**:
```bash
cd /home/admin/work/SecAlert && python3 -c "
from src.analysis.remediation import RemediationAdvisor, simplify_chain_timeline
print('RemediationAdvisor: OK')
print('simplify_chain_timeline: OK')
"
```

3. **前端文件结构检查**:
```bash
find frontend -name "*.tsx" -o -name "*.ts" | head -20
```
</verification>

<success_criteria>
- 响应平台 API 端点已实现（/remediation/chains, /remediation/platform/status）
- 前端已抑制告警 Tab 可正常切换和恢复
- 所有 Python 模块可正确导入
- Phase 4 三个 Wave 可完整衔接
</success_criteria>

<output>
创建完成后，在 `.planning/phases/04-recommendations-polish/` 目录下创建 `04-03-SUMMARY.md`。
</output>
