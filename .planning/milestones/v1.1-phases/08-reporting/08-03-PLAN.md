# Phase 8: 报表 - Plan 03

---
phase: 08-reporting
plan: 03
type: execute
wave: 3
depends_on:
  - 08-reporting-02
files_modified:
  - src/api/reports.py
  - src/templates/reports/datasource_health_report.html
autonomous: false
requirements:
  - RP-04
user_setup: []

must_haves:
  truths:
    - "用户可以查看数据源健康报表"
    - "报表展示各数据源的健康状态、延迟、告警数量"
  artifacts:
    - path: "src/api/reports.py"
      provides: "数据源健康 API"
      exports: ["GET /api/reports/datasource-health"]
    - path: "src/templates/reports/datasource_health_report.html"
      provides: "数据源健康报表模板"
      contains: "datasource_health"
  key_links:
    - from: "src/api/reports.py"
      to: "Phase 5 DS-06"
      via: "DataSourceHealthCollector 依赖注入"
---

<objective>
实现数据源健康报表 (RP-04)，依赖 Phase 5 DS-06 定义的数据源健康指标。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/api/reports.py
@src/analysis/report_aggregator.py
@.planning/phases/05-multi-datasource/
</context>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认 Phase 5 DS-06 数据源健康指标接口</name>
  <decision>Phase 5 DS-06 数据源健康监控的接口定义</decision>
  <context>
    RP-04 数据源健康报表需要依赖 Phase 5 DS-06 定义的数据源健康指标。
    需要确认:
    1. DS-06 是否已完成？健康指标的数据结构是什么？
    2. 数据源健康数据存储在哪里？(Neo4j / Elasticsearch / 其他)
    3. 健康指标包含哪些字段？(延迟、丢失率、告警数等)

    如果 Phase 5 未完成，需要确定:
    - 使用占位接口等待 DS-06 实现
    - 或使用 stub 数据进行前端开发
  </context>
  <options>
    <option id="ds-06-complete">
      <name>DS-06 已完成</name>
      <pros>可以直接集成，使用真实健康指标数据</pros>
      <cons>需要读取 DS-06 实现的具体接口</cons>
    </option>
    <option id="ds-06-pending">
      <name>DS-06 待完成</name>
      <pros>可以先开发 UI 和模板，接口实现后集成</pros>
      <cons>需要设计临时接口并约定后续集成点</cons>
    </option>
  </options>
  <resume-signal>
    如果选择 ds-06-complete: 提供 DS-06 的数据模型和 API 接口
    如果选择 ds-06-pending: 提供约定的接口规范
  </resume-signal>
</task>

</tasks>

<verification>
- GET /api/reports/datasource-health 返回数据源健康数据
- 数据源健康报表模板可渲染健康状态表格
</verification>

<success_criteria>
- 用户可通过报表页面查看数据源健康状态
- 报表包含各数据源的健康度评分、延迟、状态
</success_criteria>

<output>
完成后创建 .planning/phases/08-reporting/08-03-SUMMARY.md
</output>
