---
phase: 16-global-metadata
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/ingestion_models.py
autonomous: true
requirements:
  - GM-01
  - GM-02

must_haves:
  truths:
    - 用户创建模板时必须提供 vendor_name/product_name/device_type/tenant_id/environment
    - tenant_id 默认为 "default"
    - environment 默认为 "prod"
    - device_type 映射到 OCSF target_category_uid/target_class_uid
  artifacts:
    - path: src/api/ingestion_models.py
      contains: "class Environment(str, Enum)"
    - path: src/api/ingestion_models.py
      contains: "class CollectionMetadata"
    - path: src/api/ingestion_models.py
      contains: "class TemplateCreate"
      has_metadata_field: true
  key_links:
    - from: TemplateCreate.metadata
      to: CollectionMetadata
      via: Field validation
---

<objective>
扩展 ingestion_models.py，添加 CollectionMetadata 模型和环境枚举，为 GM-01 强制元数据字段定义数据结构。
</objective>

<context>
@src/api/ingestion_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 Environment 枚举和 CollectionMetadata 模型</name>
  <files>src/api/ingestion_models.py</files>
  <read_first>src/api/ingestion_models.py</read_first>
  <action>
在 ingestion_models.py 文件末尾添加以下内容（所有新增代码在文件最后）：

1. Environment 枚举（per D-03 必填字段）:
```python
class Environment(str, Enum):
    """环境枚举"""
    PROD = "prod"
    DEV = "dev"
    TEST = "test"
```

2. CollectionMetadata 模型（per D-07 独立模型嵌入 DataSourceTemplate）:
```python
class CollectionMetadata(BaseModel):
    """采集元数据（GM-01 强制字段）"""
    vendor_name: str = Field(..., min_length=1, max_length=100, description="厂商名，如 Suricata")
    product_name: str = Field(..., min_length=1, max_length=100, description="产品名，如 EVE JSON")
    device_type: DeviceType = Field(..., description="设备类型")
    tenant_id: str = Field(default="default", description="租户标识（MSSP 多租户）")
    environment: Environment = Field(default=Environment.PROD, description="环境")
    target_category_uid: Optional[int] = Field(None, description="OCSF 目标类别 UID")
    target_class_uid: Optional[int] = Field(None, description="OCSF 事件类 UID")
```
</action>
  <acceptance_criteria>
- grep "class Environment" src/api/ingestion_models.py 返回匹配
- grep "class CollectionMetadata" src/api/ingestion_models.py 返回匹配
- grep "tenant_id.*default" src/api/ingestion_models.py 存在默认值
- grep "environment.*Environment.PROD" src/api/ingestion_models.py 存在默认值
  </acceptance_criteria>
  <verify>
grep -c "class Environment" src/api/ingestion_models.py && grep -c "class CollectionMetadata" src/api/ingestion_models.py
  </verify>
  <done>Environment 枚举和 CollectionMetadata 模型已添加到 ingestion_models.py</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 TemplateCreate/TemplatesUpdate 支持 metadata 字段</name>
  <files>src/api/ingestion_models.py</files>
  <read_first>src/api/ingestion_models.py</read_first>
  <action>
修改 TemplateCreate 类，将 device_type 改为 DeviceType 类型，新增 metadata 字段：

原 TemplateCreate:
```python
class TemplateCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=100, description="模板名称")
    device_type: str = Field(..., description="设备类型")
    connection: ConnectionConfig = Field(..., description="连接参数")
    log_format: str = Field(..., description="日志格式")
    custom_regex: Optional[str] = Field(None, description="自定义正则")
```

修改为:
```python
class TemplateCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=100, description="模板名称")
    device_type: DeviceType = Field(..., description="设备类型")
    connection: ConnectionConfig = Field(..., description="连接参数")
    log_format: LogFormat = Field(..., description="日志格式")
    custom_regex: Optional[str] = Field(None, description="自定义正则")
    metadata: CollectionMetadata = Field(..., description="采集元数据（vendor/product/device/tenant/environment）")
```

同样修改 TemplateUpdate（metadata 设为 Optional，支持部分更新）:
```python
class TemplateUpdate(BaseModel):
    name: Optional[str] = Field(None, min_length=1, max_length=100)
    device_type: Optional[DeviceType] = None
    connection: Optional[ConnectionConfig] = None
    log_format: Optional[LogFormat] = None
    custom_regex: Optional[str] = None
    metadata: Optional[CollectionMetadata] = None
```
</action>
  <acceptance_criteria>
- grep "metadata.*CollectionMetadata" src/api/ingestion_models.py 存在于 TemplateCreate
- grep "device_type.*DeviceType" src/api/ingestion_models.py TemplateCreate 使用枚举类型
- grep "log_format.*LogFormat" src/api/ingestion_models.py TemplateCreate 使用枚举类型
- TemplateUpdate.metadata 类型为 Optional[CollectionMetadata]
  </acceptance_criteria>
  <verify>
grep -n "metadata.*CollectionMetadata" src/api/ingestion_models.py && grep -n "device_type.*DeviceType" src/api/ingestion_models.py
  </verify>
  <done>TemplateCreate 强制要求 metadata 字段，TemplateUpdate 支持可选 metadata 更新</done>
</task>

</tasks>

<verification>
python -c "from src.api.ingestion_models import CollectionMetadata, Environment, TemplateCreate; print('Models import OK')"
</verification>

<success_criteria>
- CollectionMetadata 模型包含 vendor_name/product_name/device_type/tenant_id/environment/target_category_uid/target_class_uid
- TemplateCreate 要求 metadata 必填（Field(... ) 非 Optional）
- TemplateUpdate.metadata 为 Optional
- Environment 枚举包含 PROD/DEV/TEST
</success_criteria>

<output>
After completion, create `.planning/phases/16-global-metadata/16-01-SUMMARY.md`
</output>
