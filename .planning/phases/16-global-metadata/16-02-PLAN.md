---
phase: 16-global-metadata
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/collection/metadata.py
autonomous: true
requirements:
  - GM-01
  - GM-02

must_haves:
  truths:
    - MetadataEnricher 可将 metadata 注入到事件 payload
    - OCSFMapper 基于 device_type+log_format 推断 OCSF 映射
    - 规则无法确定时返回低置信度默认值
  artifacts:
    - path: src/collection/metadata.py
      contains: "class MetadataEnricher"
    - path: src/collection/metadata.py
      contains: "class OCSFMapper"
    - path: src/collection/metadata.py
      contains: "DEVICE_TYPE_RULES"
  key_links:
    - from: MetadataEnricher.enrich
      to: event["_collection_metadata"]
      via: 注入 _collection_metadata 子对象
---

<objective>
创建 src/collection/metadata.py，包含 MetadataEnricher 组件（GM-01 元数据注入）和 OCSFMapper 组件（GM-02 OCSF 映射）。
</objective>

<context>
@src/api/ingestion_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 MetadataEnricher 组件</name>
  <files>src/collection/metadata.py</files>
  <read_first>src/api/ingestion_models.py</read_first>
  <action>
创建 src/collection/metadata.py 文件（目录 src/collection/ 需先创建 __init__.py）：

```python
"""
采集元数据注入模块

GM-01: MetadataEnricher 组件 - Ingestion Gateway 统一注入
GM-02: OCSFMapper 组件 - 规则推断 + AI 兜底的 OCSF 映射
"""

import json
from datetime import datetime
from typing import Optional, Tuple
from dataclasses import dataclass

from src.api.ingestion_models import CollectionMetadata, DeviceType, Environment


@dataclass
class OCSFMappingResult:
    """OCSF 映射结果"""
    category_uid: int
    class_uid: int
    confidence: float  # 0.0-1.0
    source: str  # "rule" or "ai"


class MetadataEnricher:
    """全局元数据强制注入器（GM-01）"""

    def __init__(self, metadata: CollectionMetadata, collector_id: str):
        self.metadata = metadata
        self.collector_id = collector_id

    def enrich(self, event: dict) -> dict:
        """将元数据注入事件 payload（注入到 _collection_metadata 子对象，不污染顶层字段）"""
        enriched = event.copy()
        enriched["_collection_metadata"] = {
            "vendor_name": self.metadata.vendor_name,
            "product_name": self.metadata.product_name,
            "device_type": self.metadata.device_type.value if isinstance(self.metadata.device_type, DeviceType) else self.metadata.device_type,
            "tenant_id": self.metadata.tenant_id,
            "environment": self.metadata.environment.value if isinstance(self.metadata.environment, Environment) else self.metadata.environment,
            "target_category_uid": self.metadata.target_category_uid,
            "target_class_uid": self.metadata.target_class_uid,
            "collector_id": self.collector_id,
            "ingest_timestamp": datetime.utcnow().isoformat(),
        }
        return enriched

    def enrich_kafka_message(self, event: dict) -> Tuple[str, str]:
        """返回 (key, value) 用于 Kafka Producer"""
        enriched = self.enrich(event)
        # Partition key: tenant_id_device_type_hash
        device_type_val = self.metadata.device_type.value if isinstance(self.metadata.device_type, DeviceType) else self.metadata.device_type
        key = f"{self.metadata.tenant_id}_{device_type_val}_{hash(json.dumps(event, sort_keys=True))}"
        return key, json.dumps(enriched)
```

确保 src/collection/ 目录存在并包含 __init__.py
</action>
  <acceptance_criteria>
- grep "class MetadataEnricher" src/collection/metadata.py 返回匹配
- grep "def enrich" src/collection/metadata.py 返回 enrich 方法
- grep "_collection_metadata" src/collection/metadata.py 注入到子对象而非顶层
- src/collection/__init__.py 存在
  </acceptance_criteria>
  <verify>
grep -c "class MetadataEnricher" src/collection/metadata.py && python -c "from src.collection.metadata import MetadataEnricher; print('MetadataEnricher import OK')"
  </verify>
  <done>MetadataEnricher 可接收 CollectionMetadata 并注入到事件 payload</done>
</task>

<task type="auto">
  <name>Task 2: 创建 OCSFMapper 组件（含规则表）</name>
  <files>src/collection/metadata.py</files>
  <read_first>src/collection/metadata.py</read_first>
  <action>
在 src/collection/metadata.py 文件末尾添加 OCSFMapper 类：

```python
class OCSFMapper:
    """OCSF 目标映射器（GM-02）- 规则推断优先 + AI 兜底"""

    # 规则表: (device_type, log_format) -> (category_uid, class_uid)
    # 参考 OCSF Schema: category_uid=1(Network Activity), 2(Detective Control), 6(Entity)
    DEVICE_TYPE_RULES: dict[Tuple[str, str], Tuple[int, int]] = {
        # IDS/IPS
        ("ids", "JSON"): (1, 2001),        # Network Activity / Alert
        ("ids", "CEF"): (1, 2001),
        ("ids", "Syslog"): (1, 2001),
        ("ips", "JSON"): (1, 2001),
        ("ips", "CEF"): (1, 2001),
        # Firewall
        ("firewall", "CEF"): (1, 4001),    # Network Activity / Network Activity
        ("firewall", "Syslog"): (1, 4001),
        ("firewall", "JSON"): (1, 4001),
        # WAF
        ("waf", "CEF"): (1, 4001),
        ("waf", "JSON"): (1, 4001),
        # EDR
        ("edr", "JSON"): (2, 2001),        # Detective Control / Security Finding
        # Antivirus
        ("antivirus", "JSON"): (2, 2003),  # File Activity
        # VPN
        ("vpn", "Syslog"): (6, 6001),       # Entity / Authentication
        ("vpn", "CEF"): (6, 6001),
        # Switch/Router
        ("switch", "Syslog"): (1, 4001),
        ("router", "Syslog"): (1, 4001),
        # SIEM
        ("siem", "JSON"): (2, 2001),
    }

    # 置信度阈值：低于此值触发 AI 推断（目前未启用 AI 兜底，先用规则）
    CONFIDENCE_THRESHOLD = 0.7

    # 未知设备的默认值（低置信度）
    DEFAULT_CATEGORY = 1   # Network Activity
    DEFAULT_CLASS = 4001  # Network Activity

    @classmethod
    def map(cls, device_type: str, log_format: str) -> OCSFMappingResult:
        """推断 OCSF category_uid 和 class_uid"""
        key = (device_type.lower(), log_format.upper())
        if key in cls.DEVICE_TYPE_RULES:
            category, class_uid = cls.DEVICE_TYPE_RULES[key]
            return OCSFMappingResult(
                category_uid=category,
                class_uid=class_uid,
                confidence=0.95,
                source="rule"
            )
        # 未知组合，返回默认值 + 低置信度
        return OCSFMappingResult(
            category_uid=cls.DEFAULT_CATEGORY,
            class_uid=cls.DEFAULT_CLASS,
            confidence=0.5,
            source="rule"
        )

    @classmethod
    def validate_ocsf_uid(cls, category_uid: Optional[int], class_uid: Optional[int]) -> Tuple[bool, str]:
        """校验 OCSF UID 格式合规性"""
        if category_uid is None or class_uid is None:
            return True, ""  # Optional 字段，空值不校验

        # OCSF category_uid 有效范围: 1-999
        if not (1 <= category_uid <= 999):
            return False, f"Invalid category_uid: {category_uid} (must be 1-999)"

        # OCSF class_uid 有效范围: 1000-9999
        if not (1000 <= class_uid <= 9999):
            return False, f"Invalid class_uid: {class_uid} (must be 1000-9999)"

        return True, ""
```
</action>
  <acceptance_criteria>
- grep "class OCSFMapper" src/collection/metadata.py 返回匹配
- grep "DEVICE_TYPE_RULES" src/collection/metadata.py 规则表存在
- grep "def map" src/collection/metadata.py map 方法存在
- grep "firewall.*CEF.*4001" src/collection/metadata.py 规则表包含 firewall 映射
- OCSFMapper.map("firewall", "CEF") 返回 category_uid=1, class_uid=4001
  </acceptance_criteria>
  <verify>
python -c "from src.collection.metadata import OCSFMapper; r = OCSFMapper.map('firewall', 'CEF'); print(f'firewall+CEF: cat={r.category_uid}, class={r.class_uid}, conf={r.confidence}')"
  </verify>
  <done>OCSFMapper 可基于 device_type+log_format 推断 OCSF 映射</done>
</task>

</tasks>

<verification>
python -c "from src.collection.metadata import MetadataEnricher, OCSFMapper; print('All imports OK')"
</verification>

<success_criteria>
- MetadataEnricher.enrich() 将 metadata 注入到事件 _collection_metadata 字段
- OCSFMapper.map("firewall", "CEF") 返回 category_uid=1, class_uid=4001
- OCSFMapper.map("ids", "JSON") 返回 category_uid=1, class_uid=2001
- OCSFMapper.validate_ocsf_uid() 可校验 OCSF 格式合规性
</success_criteria>

<output>
After completion, create `.planning/phases/16-global-metadata/16-02-SUMMARY.md`
</output>
