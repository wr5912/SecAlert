---
phase: 16-global-metadata
plan: "03"
type: execute
wave: 2
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - src/api/ingestion_endpoints.py
autonomous: true
requirements:
  - GM-01
  - GM-02

must_haves:
  truths:
    - 创建模板时 metadata 必填校验生效
    - 更新模板时 device_type/log_format 变化自动重算 OCSF
    - v1.0 旧模板首次打开时自动填充默认 metadata
  artifacts:
    - path: src/api/ingestion_endpoints.py
      contains: "_get_default_metadata"
    - path: src/api/ingestion_endpoints.py
      contains: "OCSFMapper.map"
  key_links:
    - from: create_template
      to: DataSourceTemplate.metadata
      via: CollectionMetadata 验证
    - from: update_template
      to: OCSFMapper.map
      via: device_type/log_format 变化时重算
---

<objective>
扩展 ingestion_endpoints.py，支持 metadata 验证、OCSF 自动推断、v1.0 模板迁移填充。
</objective>

<context>
@src/api/ingestion_endpoints.py
@src/collection/metadata.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 扩展 create_template 支持 metadata 和 OCSF 自动推断</name>
  <files>src/api/ingestion_endpoints.py</files>
  <read_first>src/api/ingestion_endpoints.py</read_first>
  <action>
修改 create_template 函数（位于 ~line 97-119），新增功能：

1. 创建模板时自动推断 OCSF 映射（使用 device_type + log_format）
2. 将 OCSF 映射结果填充到 metadata 的 target_category_uid / target_class_uid

修改后的 create_template：
```python
@router.post("/templates", response_model=DataSourceTemplate, status_code=201)
async def create_template(template: TemplateCreate) -> DataSourceTemplate:
    """DI-01: 创建数据源模板（含 metadata 和 OCSF 自动推断）"""
    from src.collection.metadata import OCSFMapper

    template_id = _generate_id()

    # 自动推断 OCSF 映射
    ocsf_result = OCSFMapper.map(
        device_type=template.device_type.value,
        log_format=template.log_format.value
    )

    # 构建 metadata（含 OCSF 推断结果）
    metadata = template.metadata
    metadata.target_category_uid = ocsf_result.category_uid
    metadata.target_class_uid = ocsf_result.class_uid

    new_template = DataSourceTemplate(
        id=template_id,
        name=template.name,
        device_type=template.device_type,
        connection=template.connection,
        log_format=template.log_format,
        custom_regex=template.custom_regex,
        metadata=metadata  # 新增
    )
    _templates[template_id] = new_template
    return new_template
```
</action>
  <acceptance_criteria>
- create_template 函数包含 OCSFMapper.map 调用
- create_template 将 ocsf_result 填充到 metadata.target_category_uid
- create_template 将 ocsf_result 填充到 metadata.target_class_uid
- DataSourceTemplate 实例化包含 metadata 参数
  </acceptance_criteria>
  <verify>
grep -n "OCSFMapper.map" src/api/ingestion_endpoints.py && grep -n "target_category_uid" src/api/ingestion_endpoints.py
  </verify>
  <done>创建模板时自动推断并填充 OCSF 映射</done>
</task>

<task type="auto">
  <name>Task 2: 添加 _get_default_metadata 函数（v1.0 迁移填充）</name>
  <files>src/api/ingestion_endpoints.py</files>
  <read_first>src/api/ingestion_endpoints.py</read_first>
  <action>
在 ingestion_endpoints.py 中，在 router 定义之后（line ~76 之后）添加：

```python
def _get_default_metadata(template_name: str, device_type: str) -> CollectionMetadata:
    """v1.0 Suricata 模板默认 metadata（D-06 迁移填充）"""
    from src.api.ingestion_models import DeviceType as DT, Environment, CollectionMetadata
    from src.collection.metadata import OCSFMapper

    # Suricata EVE JSON 默认值
    if "suricata" in template_name.lower():
        vendor_name = "Suricata"
        product_name = "EVE JSON"
        device_type_enum = DT.IDS
    elif device_type:
        device_type_lower = device_type.lower()
        if device_type_lower == "ids" or device_type_lower == "ips":
            vendor_name = "unknown"
            product_name = "unknown"
            device_type_enum = DT.IDS
        elif device_type_lower == "firewall":
            vendor_name = "unknown"
            product_name = "unknown"
            device_type_enum = DT.FIREWALL
        elif device_type_lower == "waf":
            vendor_name = "unknown"
            product_name = "unknown"
            device_type_enum = DT.WAF
        else:
            vendor_name = "unknown"
            product_name = "unknown"
            device_type_enum = DT.OTHER
    else:
        vendor_name = "unknown"
        product_name = "unknown"
        device_type_enum = DT.OTHER

    # 自动推断 OCSF
    ocsf_result = OCSFMapper.map(
        device_type=device_type_enum.value,
        log_format="JSON"  # 默认使用 JSON
    )

    return CollectionMetadata(
        vendor_name=vendor_name,
        product_name=product_name,
        device_type=device_type_enum,
        tenant_id="default",
        environment=Environment.PROD,
        target_category_uid=ocsf_result.category_uid,
        target_class_uid=ocsf_result.class_uid,
    )
```
</action>
  <acceptance_criteria>
- grep "_get_default_metadata" src/api/ingestion_endpoints.py 返回函数定义
- grep "Suricata" src/api/ingestion_endpoints.py 包含 Suricata 默认值
- grep "vendor_name.*default" 等同于 grep "tenant_id.*default"
  </acceptance_criteria>
  <verify>
grep -c "_get_default_metadata" src/api/ingestion_endpoints.py && python -c "from src.api.ingestion_endpoints import _get_default_metadata; m = _get_default_metadata('Suricata IDS', 'ids'); print(f'vendor={m.vendor_name}, product={m.product_name}')"
  </verify>
  <done>_get_default_metadata 函数支持 v1.0 Suricata 模板迁移填充</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 update_template 支持 metadata 迁移和 OCSF 重算</name>
  <files>src/api/ingestion_endpoints.py</files>
  <read_first>src/api/ingestion_endpoints.py</read_first>
  <action>
修改 update_template 函数（位于 ~line 216-243），实现：
1. v1.0 旧模板无 metadata 时自动填充默认值
2. device_type 或 log_format 变化时自动重算 OCSF 映射

修改后的 update_template：
```python
@router.put("/templates/{template_id}", response_model=DataSourceTemplate)
async def update_template(
    template_id: str,
    template_update: TemplateUpdate
) -> DataSourceTemplate:
    """DI-02: 更新数据源模板（含 metadata 迁移和 OCSF 重算）"""
    from src.collection.metadata import OCSFMapper

    if template_id not in _templates:
        raise HTTPException(status_code=404, detail=f"Template {template_id} not found")

    existing = _templates[template_id]

    # v1.0 迁移：如果模板没有 metadata，自动填充默认值
    if existing.metadata is None:
        existing.metadata = _get_default_metadata(
            template_name=existing.name,
            device_type=existing.device_type
        )

    # 部分更新
    update_data = template_update.model_dump(exclude_unset=True)
    updated = existing.model_copy(update=update_data)

    # 如果更新涉及 device_type 或 log_format，重新推断 OCSF
    if "device_type" in update_data or "log_format" in update_data:
        device_type_val = updated.device_type.value if hasattr(updated.device_type, 'value') else updated.device_type
        log_format_val = updated.log_format.value if hasattr(updated.log_format, 'value') else updated.log_format
        ocsf_result = OCSFMapper.map(device_type_val, log_format_val)
        updated.metadata.target_category_uid = ocsf_result.category_uid
        updated.metadata.target_class_uid = ocsf_result.class_uid

    _templates[template_id] = updated
    return updated
```
</action>
  <acceptance_criteria>
- update_template 包含 existing.metadata is None 迁移逻辑
- update_template 包含 OCSFMapper.map 重算逻辑
- update_template 处理 device_type 和 log_format 变化时重算 OCSF
  </acceptance_criteria>
  <verify>
grep -n "existing.metadata is None" src/api/ingestion_endpoints.py && grep -n "OCSFMapper.map" src/api/ingestion_endpoints.py
  </verify>
  <done>update_template 支持 v1.0 迁移和 OCSF 重算</done>
</task>

</tasks>

<verification>
python -c "
from src.api.ingestion_endpoints import create_template, update_template, _get_default_metadata
from src.api.ingestion_models import TemplateCreate, CollectionMetadata, DeviceType, Environment, ConnectionConfig, LogFormat
print('Endpoint functions import OK')
"
</verification>

<success_criteria>
- create_template 创建时自动推断 OCSF 映射
- update_template 对无 metadata 的旧模板自动填充
- update_template 在 device_type/log_format 变化时自动重算 OCSF
- _get_default_metadata("Suricata IDS", "ids") 返回正确的默认值
</success_criteria>

<output>
After completion, create `.planning/phases/16-global-metadata/16-03-SUMMARY.md`
</output>
