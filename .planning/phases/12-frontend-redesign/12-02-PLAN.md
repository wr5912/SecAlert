---
phase: 12
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - frontend/src/components/ui/Card.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/AlertList.tsx
  - frontend/src/components/layout/Header.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/tooltip.tsx
  - frontend/src/components/dashboard/AlertTrendChart.tsx
  - frontend/src/components/dashboard/SeverityPieChart.tsx
  - frontend/src/components/charts/TrendChart.tsx
  - frontend/src/components/analysis/AIPanel.tsx
autonomous: false
requirements:
  - UI-12-04
  - UI-12-05
must_haves:
  truths:
    - "用户看到升级后的 Card 组件带有 glow 效果"
    - "按钮使用 Electric cyan (#00f0ff) 主色调"
    - "告警列表具有交错动画和 hover 效果"
    - "Header 显示战术风格和 accent 高亮"
    - "Charts 使用霓虹 severity 配色"
    - "AI Copilot Panel 使用 accent 边框"
  artifacts:
    - path: "frontend/src/components/ui/Card.tsx"
      provides: "升级的 Card 组件"
      contains: "bg-surface, border-border, severity"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "升级的 Button 组件"
      contains: "bg-accent, focus-visible:ring-accent"
    - path: "frontend/src/components/AlertList.tsx"
      provides: "升级的告警列表"
      contains: "animate-fade-in-up, hover:bg-surface"
    - path: "frontend/src/components/layout/Header.tsx"
      provides: "升级的 Header"
      contains: "bg-surface, font-heading, shadow-glow"
    - path: "frontend/src/components/analysis/AIPanel.tsx"
      provides: "升级的 AI Copilot Panel"
      contains: "bg-accent/10, border-accent"
  key_links:
    - from: "Card.tsx"
      to: "tailwind.config.js"
      via: "CSS 变量"
      pattern: "bg-surface"
    - from: "Button.tsx"
      to: "tailwind.config.js"
      via: "CSS 变量"
      pattern: "bg-accent"
    - from: "AlertList.tsx"
      to: "index.css"
      via: "动画类"
      pattern: "animate-fade-in-up"
    - from: "Charts"
      to: "tailwind.config.js"
      via: "severity 颜色"
      pattern: "severity-critical"
---

<objective>
完成 P1/P2/P3 优先级组件的视觉升级，包括 Card、Button、AlertRow、Header、Input、Select、Dialog、Tooltip、Charts 和 AI Copilot Panel。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/components/ui/Card.tsx
@frontend/src/components/ui/button.tsx
@frontend/src/components/AlertList.tsx
@frontend/src/components/layout/Header.tsx
@frontend/src/components/ui/input.tsx
@frontend/src/components/ui/select.tsx
@frontend/src/components/ui/dialog.tsx
@frontend/src/components/ui/tooltip.tsx
@frontend/src/components/dashboard/AlertTrendChart.tsx
@frontend/src/components/dashboard/SeverityPieChart.tsx
@frontend/src/components/charts/TrendChart.tsx
@frontend/src/components/analysis/AIPanel.tsx

<interfaces>
<!-- 来自 Plan 01 的新组件 -->
GlowCard - 带 glow 效果的卡片容器，支持 severity prop
CornerAccent - HUD 风格角落标记
GridBackground - 页面网格背景
NoiseOverlay - 噪点纹理层

<!-- UI-SPEC 颜色系统 -->
Background: var(--background) / #0a0f1a
Surface: var(--surface) / #111827
Border: var(--border) / #1e293b
Accent: var(--accent) / #00f0ff
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 升级 Card 组件</name>
  <files>frontend/src/components/ui/Card.tsx</files>
  <action>
    重写 Card.tsx 使用 CSS 变量和 GlowCard:

    ```tsx
    import { cn } from '@/lib/utils';

    interface CardProps {
      children: React.ReactNode;
      className?: string;
      onClick?: () => void;
      severity?: 'critical' | 'high' | 'medium' | 'low';
    }

    export function Card({ children, className = '', onClick, severity }: CardProps) {
      return (
        <div
          className={cn(
            'bg-surface border border-border rounded-lg transition-all duration-150',
            severity === 'critical' && 'border-severity-critical/50 hover:shadow-glow-critical',
            severity === 'high' && 'border-severity-high/50 hover:shadow-glow-high',
            onClick && 'cursor-pointer hover:border-accent/30',
            className
          )}
          onClick={onClick}
        >
          {children}
        </div>
      );
    }

    export function CardHeader({ children, className = '' }: { children: React.ReactNode; className?: string }) {
      return (
        <div className={cn('px-4 py-3 border-b border-border font-heading', className)}>
          {children}
        </div>
      );
    }

    export function CardContent({ children, className = '' }: { children: React.ReactNode; className?: string }) {
      return (
        <div className={cn('p-4', className)}>
          {children}
        </div>
      );
    }

    export function CardTitle({ children, className = '' }: { children: React.ReactNode; className?: string }) {
      return (
        <h3 className={cn('text-lg font-semibold text-slate-200 font-heading', className)}>
          {children}
        </h3>
      );
    }
    ```

    关键变化:
    - 使用 CSS 变量 (bg-surface, border-border)
    - 支持 severity prop 实现 glow 效果
    - CardHeader 使用 font-heading (Space Grotesk)
    - hover 状态添加 accent 边框
  </action>
  <verify>
    <automated>grep -q "bg-surface" frontend/src/components/ui/Card.tsx && grep -q "border-border" frontend/src/components/ui/Card.tsx && grep -q "severity" frontend/src/components/ui/Card.tsx</automated>
  </verify>
  <done>Card 组件使用 CSS 变量和 severity glow 效果</done>
</task>

<task type="auto">
  <name>Task 2: 升级 Button 组件</name>
  <files>frontend/src/components/ui/button.tsx</files>
  <action>
    更新 button.tsx 的 variant 样式使用 accent 色:

    修改默认按钮样式使用 accent 背景:
    ```tsx
    // 在 buttonVariants 的 variants.variant 中更新:
    default: "bg-accent text-background hover:bg-accent/80 focus-visible:ring-accent/50",

    // 添加 accent outline variant:
    outline: "border-accent/50 text-accent bg-accent/10 hover:bg-accent/20",

    // 确保 destructive 使用 severity-critical:
    destructive: "bg-destructive/10 text-destructive border border-destructive/50 hover:bg-destructive/20",
    ```

    关键变化:
    - 默认按钮使用 accent 背景 (00f0ff)
    - 添加 accent outline variant
    - focus ring 使用 accent 色
  </action>
  <verify>
    <automated>grep -q "bg-accent" frontend/src/components/ui/button.tsx && grep -q "focus-visible:ring-accent" frontend/src/components/ui/button.tsx</automated>
  </verify>
  <done>Button 组件使用 accent 主色调</done>
</task>

<task type="auto">
  <name>Task 3: 升级 AlertList 告警行组件</name>
  <files>frontend/src/components/AlertList.tsx</files>
  <action>
    更新 AlertList.tsx 添加 hover 效果和交错动画:

    1. 在每行添加 hover 效果类:
    ```tsx
    className="flex items-center gap-4 p-4 border-b border-border hover:bg-surface/50 transition-colors duration-150 cursor-pointer animate-fade-in-up"
    ```

    2. 添加交错动画延迟 (通过 style prop):
    ```tsx
    style={{ animationDelay: `${Math.min(index * 50, 350)}ms` }}
    ```

    3. 确保使用新的 Badge 组件和 Card 组件:
    - Badge 从 @/components/ui 导入
    - 使用 severity 相关样式

    参考示例行结构:
    ```tsx
    <div
      key={alert.id}
      className="flex items-center gap-4 p-4 border-b border-border hover:bg-surface/50 transition-colors duration-150 cursor-pointer animate-fade-in-up opacity-0"
      style={{ animationDelay: `${Math.min(index * 50, 350)}ms` }}
      onClick={() => onSelect?.(alert)}
    >
      <Badge severity={alert.severity}>{getSeverityLabel(alert.severity)}</Badge>
      <span className="font-mono text-sm text-slate-300">{alert.source}</span>
      <span className="text-sm text-slate-400 truncate flex-1">{alert.message}</span>
      <span className="text-xs text-slate-500 font-mono">{formatTimestamp(alert.timestamp)}</span>
    </div>
    ```
  </action>
  <verify>
    <automated>grep -q "animate-fade-in-up" frontend/src/components/AlertList.tsx && grep -q "hover:bg-surface" frontend/src/components/AlertList.tsx</automated>
  </verify>
  <done>AlertList 行使用交错动画和 hover 效果</done>
</task>

<task type="auto">
  <name>Task 4: 升级 Header 组件</name>
  <files>frontend/src/components/layout/Header.tsx</files>
  <action>
    更新 Header.tsx 应用战术风格:

    1. 添加底部渐变边框:
    ```tsx
    <header className="h-14 bg-surface border-b border-border relative">
      {/* 底部渐变线 */}
      <div className="absolute bottom-0 left-0 right-0 h-px bg-gradient-to-r from-transparent via-accent/50 to-transparent" />
    ```

    2. Logo 区域使用 Space Grotesk:
    ```tsx
    <div className="flex items-center gap-2">
      <div className="w-8 h-8 bg-accent/20 border border-accent/50 rounded flex items-center justify-center">
        <Shield className="w-5 h-5 text-accent" />
      </div>
      <span className="font-heading font-semibold text-lg text-slate-200 tracking-tight">SecAlert</span>
    </div>
    ```

    3. 导航项 hover 使用 accent:
    ```tsx
    <nav className="flex items-center gap-1">
      {navItems.map(item => (
        <a
          key={item.href}
          href={item.href}
          className="px-3 py-2 text-sm text-slate-400 hover:text-accent transition-colors duration-150 relative group"
        >
          {item.label}
          {item.active && (
            <span className="absolute bottom-0 left-0 right-0 h-0.5 bg-accent shadow-glow-accent" />
          )}
        </a>
      ))}
    </nav>
    ```

    4. AI Copilot 按钮使用 accent 样式:
    ```tsx
    <button className="flex items-center gap-2 px-3 py-1.5 bg-accent/10 border border-accent/50 rounded-lg text-accent hover:bg-accent/20 transition-colors duration-150">
      <Bot className="w-4 h-4" />
      <span className="text-sm font-medium">AI Copilot</span>
    </button>
    ```
  </action>
  <verify>
    <automated>grep -q "bg-accent" frontend/src/components/layout/Header.tsx && grep -q "font-heading" frontend/src/components/layout/Header.tsx && grep -q "shadow-glow" frontend/src/components/layout/Header.tsx</automated>
  </verify>
  <done>Header 使用战术风格配色和 accent 高亮</done>
</task>

<task type="auto">
  <name>Task 5: 升级 Input 和 Select 组件</name>
  <files>
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/select.tsx
  </files>
  <action>
    更新 input.tsx 使用新配色:

    ```tsx
    import * as React from "react"
    import { cn } from "@/lib/utils"

    const Input = React.forwardRef<HTMLInputElement, React.ComponentProps<"input">>(
      ({ className, type, ...props }, ref) => {
        return (
          <input
            type={type}
            className={cn(
              "flex h-9 w-full rounded-lg border border-border bg-surface px-3 py-1 text-sm text-slate-200 file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-slate-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:border-accent/50 disabled:cursor-not-allowed disabled:opacity-50 transition-colors duration-150 font-body",
              className
            )}
            ref={ref}
            {...props}
          />
        )
      }
    )
    Input.displayName = "Input"

    export { Input }
    ```

    更新 select.tsx 使用相同样式:
    ```tsx
    className={cn(
      "flex h-9 w-full rounded-lg border border-border bg-surface px-3 py-1 text-sm text-slate-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:border-accent/50 disabled:cursor-not-allowed disabled:opacity-50 transition-colors duration-150",
      className
    )}
    ```
  </action>
  <verify>
    <automated>grep -q "focus-visible:ring-accent" frontend/src/components/ui/input.tsx && grep -q "bg-surface" frontend/src/components/ui/input.tsx</automated>
  </verify>
  <done>Input 和 Select 使用 accent focus ring</done>
</task>

<task type="auto">
  <name>Task 6: 升级 Dialog 和 Tooltip 组件</name>
  <files>
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/tooltip.tsx
  </files>
  <action>
    更新 dialog.tsx 使用战术风格:

    1. 对话框背景使用 surface:
    ```tsx
    className="fixed inset-0 z-50 bg-background/80 backdrop-blur-sm data-[state=open]:animate-in data-[state=closed]:animate-out..."
    ```

    2. Dialog Panel 使用 surface + accent 边框:
    ```tsx
    className="relative w-full max-w-lg rounded-lg border border-border bg-surface shadow-lg data-[state=open]:animate-in data-[state=closed]:animate-out..."
    ```

    3. Dialog Header 添加顶部 accent 线:
    ```tsx
    className="flex flex-col space-y-1.5 text-center sm:text-left border-b border-border pb-4"
    ```

    更新 tooltip.tsx:
    ```tsx
    className={cn(
      "inline-flex items-center rounded-md border border-border bg-surface px-2 py-1 text-xs text-slate-300 shadow-sm animate-in fade-in-0 zoom-in-95",
      className
    )}
    ```
  </action>
  <verify>
    <automated>grep -q "bg-surface" frontend/src/components/ui/dialog.tsx && grep -q "border-border" frontend/src/components/ui/dialog.tsx</automated>
  </verify>
  <done>Dialog 和 Tooltip 使用 surface 背景和 border 边框</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: 验证 P1/P2 组件升级</name>
  <what-built>
    Card、Button、AlertList、Header、Input、Select、Dialog、Tooltip 组件已升级
  </what-built>
  <how-to-verify>
    1. 运行 `cd frontend && npm run dev` 启动开发服务器
    2. 访问 http://localhost:5173
    3. 验证:
       - Header 显示 SecAlert Logo 和导航项使用 accent 色
       - 告警列表行有 hover 效果
       - 按钮使用 accent 主色调 (00f0ff)
       - Input/Select focus 时显示 accent ring
       - Dialog 使用 surface 背景和 accent 边框
    4. 运行 `npm run build` 确保无构建错误
  </how-to-verify>
  <resume-signal>Type "approved" 或描述发现的问题</resume-signal>
</task>

<task type="auto">
  <name>Task 8: 升级 Charts 组件配色</name>
  <files>
    frontend/src/components/dashboard/AlertTrendChart.tsx
    frontend/src/components/dashboard/SeverityPieChart.tsx
    frontend/src/components/charts/TrendChart.tsx
  </files>
  <action>
    为所有 Charts 组件添加战术风格配色主题:

    **AlertTrendChart.tsx** - 添加 Recharts 主题:
    ```tsx
    // 在组件内添加 theme 属性
    <ResponsiveContainer width="100%" height={300}>
      <AreaChart data={data} margin={{ top: 10, right: 30, left: 0, bottom: 0 }}>
        <defs>
          <linearGradient id="colorCritical" x1="0" y1="0" x2="0" y2="1">
            <stop offset="5%" stopColor="#ff2d55" stopOpacity={0.3}/>
            <stop offset="95%" stopColor="#ff2d55" stopOpacity={0}/>
          </linearGradient>
          <linearGradient id="colorHigh" x1="0" y1="0" x2="0" y2="1">
            <stop offset="5%" stopColor="#ff6b35" stopOpacity={0.3}/>
            <stop offset="95%" stopColor="#ff6b35" stopOpacity={0}/>
          </linearGradient>
          {/* ... 其他 severity 颜色 */}
        </defs>
        <XAxis dataKey="time" stroke="#64748b" tick={{ fill: '#94a3b8', fontSize: 12 }} />
        <YAxis stroke="#64748b" tick={{ fill: '#94a3b8', fontSize: 12 }} />
        <Tooltip
          contentStyle={{
            backgroundColor: '#111827',
            border: '1px solid #1e293b',
            borderRadius: '8px',
            color: '#e2e8f0'
          }}
        />
        {/* Area series 使用 severity 颜色 */}
      </AreaChart>
    </ResponsiveContainer>
    ```

    **SeverityPieChart.tsx** - 添加霓虹配色:
    ```tsx
    <PieChart>
      <Pie
        data={data}
        cx="50%"
        cy="50%"
        innerRadius={60}
        outerRadius={100}
        paddingAngle={2}
        dataKey="value"
        stroke="#0a0f1a"
        strokeWidth={2}
      >
        {data.map((entry, index) => (
          <Cell
            key={`cell-${index}`}
            fill={entry.color}
            style={{ filter: `drop-shadow(0 0 8px ${entry.color}50)` }}
          />
        ))}
      </Pie>
      <Tooltip
        contentStyle={{
          backgroundColor: '#111827',
          border: '1px solid #1e293b',
          borderRadius: '8px'
        }}
      />
    </PieChart>
    ```

    关键变化:
    - Tooltip 使用 surface (#111827) 背景
    - 边框使用 border (#1e293b)
    - 文字使用 slate-200 (#e2e8f0)
    - 图表使用 severity 霓虹颜色
    - 添加 drop-shadow 增强 glow 效果
  </action>
  <verify>
    <automated>grep -q "bg-surface" frontend/src/components/dashboard/AlertTrendChart.tsx && grep -q "severity-critical" frontend/src/components/dashboard/AlertTrendChart.tsx</automated>
  </verify>
  <done>Charts 使用战术风格配色和 glow 效果</done>
</task>

<task type="auto">
  <name>Task 9: 升级 AIPanel 组件</name>
  <files>frontend/src/components/analysis/AIPanel.tsx</files>
  <action>
    更新 AIPanel.tsx 使用战术风格:

    1. 面板顶部添加 accent 边框:
    ```tsx
    <div className="flex flex-col h-full bg-surface border-l border-border relative">
      {/* 顶部 accent 线 */}
      <div className="absolute top-0 left-0 right-0 h-px bg-gradient-to-r from-accent via-accent/50 to-transparent" />
    ```

    2. Header 使用战术风格:
    ```tsx
    <div className="flex items-center gap-2 px-4 py-3 border-b border-border">
      <div className="p-2 bg-accent/10 rounded-lg border border-accent/20">
        <Bot className="w-4 h-4 text-accent" />
      </div>
      <div>
        <h3 className="text-sm font-heading font-semibold text-slate-200">AI Copilot</h3>
        <p className="text-xs text-slate-500">智能告警分析助手</p>
      </div>
    </div>
    ```

    3. 消息气泡使用 severity 配色:
    ```tsx
    {/* AI 消息 */}
    <div className="flex gap-2">
      <div className="p-2 bg-accent/10 rounded-lg border border-accent/20">
        <Bot className="w-4 h-4 text-accent" />
      </div>
      <div className="flex-1 p-3 bg-surface border border-border rounded-lg">
        <p className="text-sm text-slate-300">{message.content}</p>
      </div>
    </div>

    {/* 用户消息 */}
    <div className="flex gap-2 justify-end">
      <div className="max-w-[80%] p-3 bg-accent/10 border border-accent/30 rounded-lg">
        <p className="text-sm text-accent">{message.content}</p>
      </div>
    </div>
    ```

    4. 输入框使用 accent:
    ```tsx
    <div className="p-3 border-t border-border">
      <div className="relative">
        <textarea
          className="w-full rounded-lg border border-border bg-background px-3 py-2 text-sm text-slate-200 placeholder:text-slate-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/50 resize-none"
          placeholder="输入您的安全问题..."
          rows={2}
        />
        <button className="absolute right-2 bottom-2 p-2 bg-accent/10 rounded-lg border border-accent/50 text-accent hover:bg-accent/20 transition-colors">
          <Send className="w-4 h-4" />
        </button>
      </div>
    </div>
    ```
  </action>
  <verify>
    <automated>grep -q "bg-accent" frontend/src/components/analysis/AIPanel.tsx && grep -q "border-accent" frontend/src/components/analysis/AIPanel.tsx && grep -q "font-heading" frontend/src/components/analysis/AIPanel.tsx</automated>
  </verify>
  <done>AIPanel 使用 accent 边框和战术风格</done>
</task>

</tasks>

<verification>
运行以下命令验证构建:
- cd frontend && npm run build
- 检查 build 输出无错误
- 检查所有修改的文件没有 TypeScript 错误
</verification>

<success_criteria>
- [ ] Card 组件使用 CSS 变量和 severity glow
- [ ] Button 组件使用 accent 主色调
- [ ] AlertList 使用交错动画和 hover 效果
- [ ] Header 使用战术风格和 accent 高亮
- [ ] Input/Select 使用 accent focus ring
- [ ] Dialog/Tooltip 使用 surface 背景
- [ ] Charts 使用 severity 霓虹配色和 glow
- [ ] AIPanel 使用 accent 边框和战术风格
- [ ] `npm run build` 成功
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-redesign/12-02-SUMMARY.md`
</output>
