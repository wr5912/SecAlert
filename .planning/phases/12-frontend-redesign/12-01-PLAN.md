---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/index.css
  - frontend/tailwind.config.js
  - frontend/src/main.tsx
  - frontend/src/components/ui/Badge.tsx
  - frontend/src/components/dashboard/StatCard.tsx
autonomous: false
requirements:
  - UI-12-01
  - UI-12-02
  - UI-12-03
must_haves:
  truths:
    - "用户看到 Tactical Command Center 风格的深色主题"
    - "页面具有网格背景和噪点纹理效果"
    - "字体系统正确加载 (JetBrains Mono + Space Grotesk + IBM Plex Sans)"
    - "Severity 颜色使用霓虹风格 (Critical: #ff2d55, High: #ff6b35)"
  artifacts:
    - path: "frontend/src/index.css"
      provides: "CSS 变量系统和全局样式"
      contains: "var(--background), var(--accent), grid-background"
    - path: "frontend/tailwind.config.js"
      provides: "Tailwind 主题扩展"
      contains: "fontFamily, glow-accent, severity-critical"
    - path: "frontend/src/components/GridBackground.tsx"
      provides: "网格背景组件"
    - path: "frontend/src/components/NoiseOverlay.tsx"
      provides: "噪点纹理组件"
    - path: "frontend/src/components/GlowCard.tsx"
      provides: "发光卡片组件"
    - path: "frontend/src/components/CornerAccent.tsx"
      provides: "HUD 角落标记组件"
  key_links:
    - from: "index.css"
      to: "tailwind.config.js"
      via: "CSS 变量定义"
      pattern: "var\\(--background\\)"
    - from: "StatCard.tsx"
      to: "GlowCard.tsx"
      via: "import"
      pattern: "import.*GlowCard"
    - from: "Badge.tsx"
      to: "index.css"
      via: "CSS 变量"
      pattern: "var\\(--severity-"
---

<objective>
建立 Tactical Command Center 设计系统的全局样式基础设施，安装字体包，配置 CSS 变量，扩展 Tailwind 主题，并创建核心视觉组件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@frontend/src/index.css
@frontend/tailwind.config.js
@frontend/src/components/ui/Badge.tsx
@frontend/src/components/dashboard/StatCard.tsx

<interfaces>
<!-- UI-SPEC 颜色系统 (来自 12-UI-SPEC.md) -->
Color Palette:
- Background: #0a0f1a
- Surface: #111827
- Border: #1e293b
- Accent: #00f0ff (Electric cyan)
- Severity Critical: #ff2d55
- Severity High: #ff6b35
- Severity Medium: #fbbf24
- Severity Low: #4b5563

Font System:
- Technical: JetBrains Mono (timestamps, IPs, IOC, logs)
- Heading: Space Grotesk (headings, labels)
- Body: IBM Plex Sans (body text)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装字体包</name>
  <files>frontend/package.json</files>
  <action>
    在 frontend 目录执行以下命令安装 @fontsource-variable 包:

    npm install @fontsource-variable/jetbrains-mono @fontsource-variable/space-grotesk @fontsource-variable/ibm-plex-sans

    这些字体包替代 Google Fonts，适合 SecAlert 离线私有化部署场景。
  </action>
  <verify>
    <automated>grep -q "@fontsource-variable/jetbrains-mono" frontend/package.json && grep -q "@fontsource-variable/space-grotesk" frontend/package.json && grep -q "@fontsource-variable/ibm-plex-sans" frontend/package.json</automated>
  </verify>
  <done>字体包已添加到 package.json，node_modules 中可用</done>
</task>

<task type="auto">
  <name>Task 2: 更新 index.css CSS 变量</name>
  <files>frontend/src/index.css</files>
  <action>
    重写 frontend/src/index.css 文件，实现 UI-SPEC 定义的 Tactical Command Center 主题:

    ```css
    /* Tailwind CSS 基础指令 */
    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    /* UI-SPEC CSS 变量定义 */
    :root {
      /* Base palette */
      --background: #0a0f1a;
      --surface: #111827;
      --border: #1e293b;
      --accent: #00f0ff;
      --accent-muted: rgba(0, 240, 255, 0.1);

      /* Severity colors */
      --severity-critical: #ff2d55;
      --severity-high: #ff6b35;
      --severity-medium: #fbbf24;
      --severity-low: #4b5563;

      /* Semantic colors */
      --success: #10b981;
      --warning: #f59e0b;
      --destructive: #ff2d55;
      --info: #00f0ff;

      /* Grid background */
      --grid-color: rgba(30, 41, 59, 0.3);
      --grid-size: 40px;
    }

    @layer base {
      body {
        @apply antialiased;
        background-color: var(--background);
        color: #e2e8f0;
        font-family: 'IBM Plex Sans', -apple-system, BlinkMacSystemFont, sans-serif;
      }

      /* 网格背景效果 */
      .grid-background {
        background-image:
          linear-gradient(to right, var(--grid-color) 1px, transparent 1px),
          linear-gradient(to bottom, var(--grid-color) 1px, transparent 1px);
        background-size: var(--grid-size) var(--grid-size);
      }

      /* 噪点纹理 */
      .noise-overlay {
        position: fixed;
        inset: 0;
        pointer-events: none;
        opacity: 0.02;
        z-index: 9999;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
      }
    }

    /* 滚动条样式 */
    ::-webkit-scrollbar {
      width: 8px;
      height: 8px;
    }

    ::-webkit-scrollbar-track {
      background: var(--surface);
    }

    ::-webkit-scrollbar-thumb {
      background: var(--border);
      border-radius: 4px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #334155;
    }

    /* Focus ring - 使用 accent 色 */
    *:focus-visible {
      outline: none;
      box-shadow: 0 0 0 2px var(--background), 0 0 0 4px var(--accent);
    }

    /* Glow 效果 */
    .glow-accent {
      box-shadow: 0 0 20px rgba(0, 240, 255, 0.3), 0 0 40px rgba(0, 240, 255, 0.1);
    }

    .glow-critical {
      box-shadow: 0 0 20px rgba(255, 45, 85, 0.3), 0 0 40px rgba(255, 45, 85, 0.1);
    }

    .glow-high {
      box-shadow: 0 0 20px rgba(255, 107, 53, 0.3), 0 0 40px rgba(255, 107, 53, 0.1);
    }

    /* 交错动画 */
    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(10px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    .animate-fade-in-up {
      animation: fadeInUp 400ms ease-out forwards;
    }

    .stagger-1 { animation-delay: 0ms; }
    .stagger-2 { animation-delay: 50ms; }
    .stagger-3 { animation-delay: 100ms; }
    .stagger-4 { animation-delay: 150ms; }
    .stagger-5 { animation-delay: 200ms; }
    ```
  </action>
  <verify>
    <automated>grep -q "var(--background)" frontend/src/index.css && grep -q "var(--accent)" frontend/src/index.css && grep -q "grid-background" frontend/src/index.css && grep -q "noise-overlay" frontend/src/index.css</automated>
  </verify>
  <done>index.css 包含完整的 CSS 变量系统和视觉效果</done>
</task>

<task type="auto">
  <name>Task 3: 扩展 Tailwind 配置</name>
  <files>frontend/tailwind.config.js</files>
  <action>
    更新 tailwind.config.js，扩展主题配置以支持 UI-SPEC 颜色和字体:

    ```javascript
    /** @type {import('tailwindcss').Config} */
    export default {
      darkMode: 'class',
      content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
      ],
      theme: {
        extend: {
          colors: {
            background: 'var(--background)',
            surface: 'var(--surface)',
            border: 'var(--border)',
            accent: {
              DEFAULT: 'var(--accent)',
              muted: 'var(--accent-muted)',
            },
            severity: {
              critical: 'var(--severity-critical)',
              high: 'var(--severity-high)',
              medium: 'var(--severity-medium)',
              low: 'var(--severity-low)',
            },
            success: 'var(--success)',
            warning: 'var(--warning)',
            destructive: 'var(--destructive)',
            info: 'var(--info)',
          },
          fontFamily: {
            mono: ['JetBrains Mono', 'monospace'],
            heading: ['Space Grotesk', 'sans-serif'],
            body: ['IBM Plex Sans', 'sans-serif'],
          },
          boxShadow: {
            'glow-accent': '0 0 20px rgba(0, 240, 255, 0.3), 0 0 40px rgba(0, 240, 255, 0.1)',
            'glow-critical': '0 0 20px rgba(255, 45, 85, 0.3), 0 0 40px rgba(255, 45, 85, 0.1)',
            'glow-high': '0 0 20px rgba(255, 107, 53, 0.3), 0 0 40px rgba(255, 107, 53, 0.1)',
          },
          animation: {
            'fade-in-up': 'fadeInUp 400ms ease-out forwards',
          },
          keyframes: {
            fadeInUp: {
              '0%': { opacity: '0', transform: 'translateY(10px)' },
              '100%': { opacity: '1', transform: 'translateY(0)' },
            },
          },
        },
      },
      plugins: [
        require('@tailwindcss/typography'),
      ],
    }
    ```
  </action>
  <verify>
    <automated>grep -q "fontFamily" frontend/tailwind.config.js && grep -q "glow-accent" frontend/tailwind.config.js && grep -q "severity-critical" frontend/tailwind.config.js</automated>
  </verify>
  <done>tailwind.config.js 包含完整的颜色、字体和 glow 配置</done>
</task>

<task type="auto">
  <name>Task 4: 更新 main.tsx 导入字体</name>
  <files>frontend/src/main.tsx</files>
  <action>
    在 frontend/src/main.tsx 顶部添加字体导入语句:

    ```typescript
    // 字体导入 - Tactical Command Center 字体系统
    import '@fontsource-variable/jetbrains-mono';
    import '@fontsource-variable/space-grotesk';
    import '@fontsource-variable/ibm-plex-sans';
    ```

    确保字体导入在其他导入之前，作为首要初始化步骤。
  </action>
  <verify>
    <automated>grep -q "@fontsource-variable/jetbrains-mono" frontend/src/main.tsx && grep -q "@fontsource-variable/space-grotesk" frontend/src/main.tsx && grep -q "@fontsource-variable/ibm-plex-sans" frontend/src/main.tsx</automated>
  </verify>
  <done>字体在 main.tsx 中正确导入</done>
</task>

<task type="auto">
  <name>Task 5: 创建视觉组件</name>
  <files>
    frontend/src/components/GridBackground.tsx
    frontend/src/components/NoiseOverlay.tsx
    frontend/src/components/GlowCard.tsx
    frontend/src/components/CornerAccent.tsx
  </files>
  <action>
    创建以下 4 个视觉组件，参考 RESEARCH.md 中的实现方案:

    **1. GridBackground.tsx** - 页面网格背景
    ```tsx
    export function GridBackground({ className = '' }: { className?: string }) {
      return (
        <div
          className={`fixed inset-0 pointer-events-none ${className}`}
          style={{
            backgroundImage: 'linear-gradient(to right, rgba(30, 41, 59, 0.3) 1px, transparent 1px), linear-gradient(to bottom, rgba(30, 41, 59, 0.3) 1px, transparent 1px)',
            backgroundSize: '40px 40px',
          }}
        />
      );
    }
    ```

    **2. NoiseOverlay.tsx** - 噪点纹理层
    ```tsx
    export function NoiseOverlay() {
      return (
        <div
          className="fixed inset-0 pointer-events-none z-[9999]"
          style={{
            opacity: 0.02,
            backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E")`,
          }}
        />
      );
    }
    ```

    **3. GlowCard.tsx** - 带 glow 效果的卡片容器
    ```tsx
    import { cn } from '@/lib/utils';

    interface GlowCardProps {
      children: React.ReactNode;
      className?: string;
      severity?: 'critical' | 'high' | 'medium' | 'low';
    }

    export function GlowCard({ children, className = '', severity }: GlowCardProps) {
      return (
        <div
          className={cn(
            'bg-surface border border-border rounded-lg transition-shadow duration-150',
            severity === 'critical' && 'border-severity-critical/50 shadow-glow-critical',
            severity === 'high' && 'border-severity-high/50 shadow-glow-high',
            severity && 'shadow-lg',
            className
          )}
        >
          {children}
        </div>
      );
    }
    ```

    **4. CornerAccent.tsx** - HUD 风格角落标记
    ```tsx
    import { cn } from '@/lib/utils';

    interface CornerAccentProps {
      children: React.ReactNode;
      className?: string;
      position?: 'tl-br' | 'all';
    }

    export function CornerAccent({ children, className = '', position = 'tl-br' }: CornerAccentProps) {
      return (
        <div className={cn('relative', className)}>
          {position === 'tl-br' && (
            <>
              <div className="absolute top-0 left-0 w-3 h-3 border-t-2 border-l-2 border-accent" />
              <div className="absolute bottom-0 right-0 w-3 h-3 border-b-2 border-r-2 border-accent" />
            </>
          )}
          {position === 'all' && (
            <>
              <div className="absolute top-0 left-0 w-3 h-3 border-t-2 border-l-2 border-accent" />
              <div className="absolute top-0 right-0 w-3 h-3 border-t-2 border-r-2 border-accent" />
              <div className="absolute bottom-0 left-0 w-3 h-3 border-b-2 border-l-2 border-accent" />
              <div className="absolute bottom-0 right-0 w-3 h-3 border-b-2 border-r-2 border-accent" />
            </>
          )}
          {children}
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>test -f frontend/src/components/GridBackground.tsx && test -f frontend/src/components/NoiseOverlay.tsx && test -f frontend/src/components/GlowCard.tsx && test -f frontend/src/components/CornerAccent.tsx</automated>
  </verify>
  <done>4 个视觉组件已创建并导出</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: 验证全局样式和组件</name>
  <what-built>
    字体包安装、CSS 变量配置、Tailwind 扩展、4 个视觉组件 (GridBackground, NoiseOverlay, GlowCard, CornerAccent)
  </what-built>
  <how-to-verify>
    1. 运行 `cd frontend && npm run dev` 启动开发服务器
    2. 访问 http://localhost:5173
    3. 验证页面背景使用深色 (#0a0a0f)
    4. 验证网格背景 (40px 间距线条) 可见
    5. 验证页面顶部有噪点纹理效果
    6. 打开 DevTools 检查 CSS 变量:
       - --background: #0a0f1a
       - --accent: #00f0ff
       - --severity-critical: #ff2d55
    7. 验证 JetBrains Mono 字体应用于等技术数据
  </how-to-verify>
  <resume-signal>Type "approved" 或描述发现的问题</resume-signal>
</task>

<task type="auto">
  <name>Task 7: 升级 AlertBadge 组件</name>
  <files>frontend/src/components/ui/Badge.tsx</files>
  <action>
    更新 frontend/src/components/ui/Badge.tsx，使用 UI-SPEC 的 severity 颜色:

    修改 severityStyles 使用霓虹风格颜色:
    ```typescript
    const severityStyles: Record<Severity, string> = {
      critical: 'bg-[var(--severity-critical)]/20 text-[var(--severity-critical)] border border-[var(--severity-critical)]/50',
      high: 'bg-[var(--severity-high)]/20 text-[var(--severity-high)] border border-[var(--severity-high)]/50',
      medium: 'bg-[var(--severity-medium)]/20 text-[var(--severity-medium)] border border-[var(--severity-medium)]/50',
      low: 'bg-[var(--severity-low)]/20 text-[var(--severity-low)] border border-[var(--severity-low)]/50',
    };
    ```

    使用 bg-xxx/20 语法实现 muted 效果，border 添加边框增强 HUD 感。
  </action>
  <verify>
    <automated>grep -q "severity-critical" frontend/src/components/ui/Badge.tsx && grep -q "bg-\[var(--severity-critical)\]" frontend/src/components/ui/Badge.tsx</automated>
  </verify>
  <done>AlertBadge 使用 UI-SPEC 霓虹 severity 颜色</done>
</task>

<task type="auto">
  <name>Task 8: 升级 StatCard 组件</name>
  <files>frontend/src/components/dashboard/StatCard.tsx</files>
  <action>
    更新 StatCard.tsx 应用 GlowCard 和 CornerAccent 样式:

    修改导入和组件实现:
    ```tsx
    import { LucideIcon } from 'lucide-react';
    import { GlowCard } from '../GlowCard';
    import { CornerAccent } from '../CornerAccent';
    import { cn } from '../../lib/cn';

    interface StatCardProps {
      title: string;
      value: string | number;
      change?: number;
      changeLabel?: string;
      icon: LucideIcon;
      trend?: 'up' | 'down' | 'neutral';
    }

    export function StatCard({ title, value, change, changeLabel, icon: Icon, trend = 'neutral' }: StatCardProps) {
      return (
        <GlowCard className="relative overflow-hidden">
          <CornerAccent position="tl-br" className="p-6">
            <div className="flex items-start justify-between">
              <div>
                <p className="text-sm text-slate-400 font-heading font-medium tracking-wide uppercase">{title}</p>
                <p className="text-3xl font-bold text-slate-200 mt-2 font-mono">{value}</p>
                {change !== undefined && (
                  <div className="flex items-center gap-1 mt-2">
                    <span
                      className={cn(
                        'text-sm font-medium font-mono',
                        trend === 'up' && 'text-[var(--success)]',
                        trend === 'down' && 'text-[var(--destructive)]',
                        trend === 'neutral' && 'text-slate-500'
                      )}
                    >
                      {trend === 'up' && '+'}
                      {trend === 'down' && '-'}
                      {change}%
                    </span>
                    {changeLabel && (
                      <span className="text-sm text-slate-500">{changeLabel}</span>
                    )}
                  </div>
                )}
              </div>
              <div className="p-3 bg-accent/10 rounded-lg border border-accent/20">
                <Icon className="w-6 h-6 text-accent" />
              </div>
            </div>
          </CornerAccent>
        </GlowCard>
      );
    }
    ```

    关键变化:
    - 使用 GlowCard 替代 Card
    - 使用 CornerAccent 添加 HUD 角落标记
    - 图标背景使用 accent/muted 色
    - 标题使用 font-heading (Space Grotesk)
    - 数值使用 font-mono (JetBrains Mono)
    - 颜色使用 CSS 变量
  </action>
  <verify>
    <automated>grep -q "GlowCard" frontend/src/components/dashboard/StatCard.tsx && grep -q "CornerAccent" frontend/src/components/dashboard/StatCard.tsx && grep -q "font-mono" frontend/src/components/dashboard/StatCard.tsx</automated>
  </verify>
  <done>StatCard 使用 GlowCard + CornerAccent + 新配色方案</done>
</task>

</tasks>

<verification>
运行以下命令验证构建:
- cd frontend && npm run build
- 检查 build 输出无错误
</verification>

<success_criteria>
- [ ] @fontsource-variable 字体包已安装
- [ ] index.css 包含完整 CSS 变量系统
- [ ] tailwind.config.js 扩展了 UI-SPEC 颜色和字体
- [ ] main.tsx 导入所有三种字体
- [ ] 4 个视觉组件已创建 (GridBackground, NoiseOverlay, GlowCard, CornerAccent)
- [ ] AlertBadge 使用霓虹 severity 颜色
- [ ] StatCard 使用 GlowCard + CornerAccent + 新字体
- [ ] `npm run build` 成功
</success_criteria>

<output>
After completion, create `.planning/phases/12-frontend-redesign/12-01-SUMMARY.md`
</output>
