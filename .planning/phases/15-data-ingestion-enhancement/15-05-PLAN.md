---
phase: 15-data-ingestion-enhancement
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/ingestion_endpoints.py
  - frontend/src/components/ingestion/wizard/Step3LogFormat.tsx
  - frontend/src/components/ingestion/wizard/AIDetectPanel.tsx
  - frontend/src/stores/ingestionStore.ts
  - frontend/src/types/ingestion.ts
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "AI 识别成功后自动保存模板并传递 templateId 给 MappingPreview"
    - "recognize_log_format 在 DSPy 不可用时返回 503 错误而非模拟响应"
    - "点击'应用映射'按钮后保存映射到 store 并更新预览"
  artifacts:
    - path: "src/api/ingestion_endpoints.py"
      provides: "recognize_log_format 端点修复"
      issue: "DSPY_AVAILABLE=false 时返回模拟响应而非 503"
    - path: "frontend/src/components/ingestion/wizard/Step3LogFormat.tsx"
      provides: "AI 识别后自动保存模板"
      issue: "templateId=\"\" + 应用映射按钮无操作"
---

<objective>
关闭 Phase 15 验证发现的 gaps：
1. recognize_log_format 端点在 DSPy 不可用时应返回 503 而非模拟数据
2. AI 识别成功后应自动保存模板并传递 templateId 给 MappingPreview
3. "应用映射"按钮应实现实际保存逻辑

Purpose: 修复用户体验断点，让 AI 识别流程完整可用
Output: 修复后的 Step3LogFormat、AIDetectPanel、ingestion_endpoints
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/api/ingestion_endpoints.py
@frontend/src/components/ingestion/wizard/Step3LogFormat.tsx
@frontend/src/components/ingestion/wizard/AIDetectPanel.tsx
@frontend/src/stores/ingestionStore.ts
@frontend/src/types/ingestion.ts
@.planning/phases/15-data-ingestion-enhancement/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 recognize_log_format 端点 - DSPy 不可用时返回 503</name>
  <files>src/api/ingestion_endpoints.py</files>
  <read_first>
    - src/api/ingestion_endpoints.py (第 298-380 行 - recognize_log_format 端点)
  </read_first>
  <action>
    修复 src/api/ingestion_endpoints.py 中的 recognize_log_format 端点：

    问题分析：
    - 第 320-324 行：is_llm_available() 返回 false 时正确返回 503
    - 第 340 行：DSPY_AVAILABLE 检查是多余的 - 当 DSPy 不可用时不应该返回模拟数据
    - 第 359-376 行：当 DSPY_AVAILABLE=false 时的模拟响应是不正确的 fallback 行为

    修复方案：
    1. 删除第 340 行的 `if DSPY_AVAILABLE:` 条件检查
    2. 删除第 359-376 行的模拟响应分支
    3. 保留第 320-324 行的 503 检查作为唯一的 fallback
    4. 如果 DSPy 不可用，raise HTTPException(status_code=503)

    修复后的逻辑应该是：
    ```python
    # 检查 LLM 是否可用
    if not is_llm_available():
        raise HTTPException(
            status_code=503,
            detail="LLM service unavailable. Please check LLM configuration."
        )

    try:
        from parser.dspy.signatures import LogFormatRecognition
        lm = get_lm()
        if lm is None:
            raise HTTPException(status_code=503, detail="LLM not initialized")

        # 使用 DSPy 调用 LLM
        import dspy
        with dspy.context(lm=lm):
            predictor = dspy.Predict(LogFormatRecognition)
            result = predictor(
                raw_logs=raw_logs_text,
                source_type="unknown"
            )
            # ... 返回结果
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(status_code=500, detail=f"Recognition failed: {str(e)}")
    ```

    关键：删除所有模拟/硬编码的 CEF 响应，当服务不可用时必须返回 503。
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert && python -c "from src.api.ingestion_endpoints import router; print('Fixed recognize_log_format endpoint')"</automated>
  </verify>
  <done>
    recognize_log_format 端点在 DSPy/LLM 不可用时返回 503 错误
    不再返回硬编码的 CEF 模拟响应
  </done>
</task>

<task type="auto">
  <name>Task 2: AI 识别成功后自动保存模板并传递 templateId</name>
  <files>
    - frontend/src/components/ingestion/wizard/AIDetectPanel.tsx
    - frontend/src/components/ingestion/wizard/Step3LogFormat.tsx
    - frontend/src/stores/ingestionStore.ts
    - frontend/src/types/ingestion.ts
  </files>
  <read_first>
    - frontend/src/components/ingestion/wizard/AIDetectPanel.tsx (第 19-49 行 - mutation 定义)
    - frontend/src/components/ingestion/wizard/Step3LogFormat.tsx (第 107-158 行 - AI 识别后显示区域)
    - frontend/src/stores/ingestionStore.ts (第 68-76 行 - AI 识别相关状态)
  </read_first>
  <action>
    实现 AI 识别成功后自动保存模板并更新 MappingPreview：

    1. 扩展 ingestionStore.ts：
       - 添加 `currentTemplateId: string | null` 状态
       - 添加 `setCurrentTemplateId` action
       - 添加 `autoSaveTemplate: (name: string, logFormat: string, customRegex: string) => Promise<string>` action

    2. 扩展 frontend/src/types/ingestion.ts：
       - 在 `LogFormatRecognitionResult` 中添加 `detected_fields?: string[]`

    3. 修改 AIDetectPanel.tsx (第 19-49 行)：
       - 在 onSuccess 回调中：
         ```typescript
         onSuccess: async (data) => {
           setAiRecognitionResult(data);
           setFieldMappings(data.field_mappings || {});

           // 自动保存模板
           const templateName = `AI识别-${data.detected_format}-${Date.now()}`;
           try {
             const response = await fetch('/api/ingestion/templates', {
               method: 'POST',
               headers: { 'Content-Type': 'application/json' },
               body: JSON.stringify({
                 name: templateName,
                 device_type: 'other',
                 connection: { host: '', port: 514, username: '', password: '', protocol: 'syslog' },
                 log_format: data.detected_format,
                 custom_regex: data.regex_pattern
               }),
             });
             if (response.ok) {
               const template = await response.json();
               setCurrentTemplateId(template.id);
             }
           } catch (e) {
             console.error('Auto-save template failed:', e);
           }

           setErrorMessage(null);
           onSuccess?.(data);
         }
         ```

    4. 修改 Step3LogFormat.tsx (第 134-142 行)：
       - 将 `templateId=""` 改为 `templateId={currentTemplateId || ''}`
       - 从 useIngestionStore 中获取 currentTemplateId
       - 将 `sampleLogs={[]}` 改为 `sampleLogs={sampleLogs}`

    5. 修改 Step3LogFormat.tsx (第 146-155 行) "应用映射"按钮：
       - 实现保存映射逻辑：调用 setFieldMappings 更新 store
       - 同时更新已保存模板的 custom_regex 字段（调用 PUT /api/ingestion/templates/{id}）
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/Step3LogFormat.tsx src/components/ingestion/wizard/AIDetectPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    AI 识别成功后自动保存模板到后端
    currentTemplateId 存储在 store 中
    MappingPreview 收到正确的 templateId
    "应用映射"按钮实现保存逻辑
  </done>
</task>

<task type="auto">
  <name>Task 3: 完善前端类型定义</name>
  <files>frontend/src/types/ingestion.ts</files>
  <read_first>
    - frontend/src/types/ingestion.ts (现有类型)
  </read_first>
  <action>
    确保 frontend/src/types/ingestion.ts 包含所有必要的类型定义：

    1. 检查并添加（如果缺失）：
       ```typescript
       export interface LogFormatRecognitionResult {
         detected_format: string;
         regex_pattern: string;
         field_mappings: Record<string, string>;
         confidence: number;
         reasoning: string;
         detected_fields?: string[];  // 新增：检测到的源字段列表
       }

       // 模板创建请求
       export interface TemplateCreateRequest {
         name: string;
         device_type: string;
         connection: ConnectionConfig;
         log_format: string;
         custom_regex?: string;
       }
       ```

    2. 确保 WIZARD_STEPS 包含所有 6 个步骤：
       - Step1: 设备类型
       - Step2: 连接参数
       - Step3: 日志格式 (AI 识别 + 字段映射)
       - Step4: 模板名称
       - Step5: 批量导入
       - Step6: 解析测试
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/types/ingestion.ts 2>&1 | head -10</automated>
  </verify>
  <done>
    LogFormatRecognitionResult 包含 detected_fields
    所有必要类型已定义
  </done>
</task>

</tasks>

<verification>
- [ ] recognize_log_format 在 DSPy 不可用时返回 503
- [ ] 不再返回硬编码的 CEF 模拟响应
- [ ] AI 识别成功后自动保存模板
- [ ] MappingPreview 收到正确的 templateId
- [ ] "应用映射"按钮实现保存逻辑
</verification>

<success_criteria>
1. 当 LLM 服务不可用时，API 返回 503 错误消息 "LLM service unavailable"
2. AI 识别成功后，向导自动创建模板并获取 templateId
3. MappingPreview 使用正确的 templateId 显示预览
4. 用户点击"应用映射"后，映射保存到 store 并可被后续步骤使用
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-ingestion-enhancement/15-05-SUMMARY.md`
</output>
