---
phase: 15-data-ingestion-enhancement
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/api/ingestion_endpoints.py
  - src/api/ingestion_models.py
  - frontend/src/components/ingestion/wizard/Step5BatchImport.tsx
  - frontend/src/components/ingestion/wizard/BatchImportModal.tsx
  - frontend/src/components/ingestion/wizard/DeviceTable.tsx
  - frontend/src/stores/ingestionStore.ts
  - frontend/src/types/ingestion.ts
autonomous: true
requirements:
  - DI-08

must_haves:
  truths:
    - "用户可上传 CSV/Excel 文件批量导入设备"
    - "系统预览解析后的设备列表"
    - "用户确认后批量创建设备模板"
    - "系统显示导入成功/失败数量"
  artifacts:
    - path: "src/api/ingestion_endpoints.py"
      provides: "POST /api/ingestion/templates/batch 端点"
      exports: "batch_create_templates"
    - path: "frontend/src/components/ingestion/wizard/BatchImportModal.tsx"
      provides: "批量导入对话框"
      states: "closed, file-select, parsing, complete, error"
    - path: "frontend/src/components/ingestion/wizard/DeviceTable.tsx"
      provides: "批量设备表格"
      states: "normal, selected, error"
  key_links:
    - from: "BatchImportModal.tsx"
      to: "/api/ingestion/templates/batch"
      via: "TanStack Query mutation"
      pattern: "useMutation.*batch"
    - from: "BatchImportModal.tsx"
      to: "xlsx"
      via: "XLSX.read()"
      pattern: "XLSX\\.read"
---

<objective>
实现 DI-08 批量接入功能：CSV/Excel 批量导入设备列表，统一应用模板。

Purpose: 提高批量接入效率，10x+ 效率提升
Output: BatchImportModal + batch API 端点
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/api/ingestion_endpoints.py
@src/api/ingestion_models.py
@frontend/src/stores/ingestionStore.ts
@frontend/src/types/ingestion.ts
@.planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md
@.planning/phases/15-data-ingestion-enhancement/15-RESEARCH.md

<interfaces>
<!-- 现有 API 端点 (从 ingestion_endpoints.py 提取) -->

```python
# POST /api/ingestion/templates (现有)
# 创建单个模板

# POST /api/ingestion/templates/batch (需新增)
# Request: {
#   devices: List[{
#     name: str,
#     device_type: str,
#     host: str,
#     port: int,
#     protocol: str,
#     log_format: str,
#     template_id: str  # 统一应用的模板 ID
#   }],
#   apply_template_id: str | null  # 如果指定，统一应用此模板
# }
# Response: {
#   success_count: int,
#   failure_count: int,
#   results: List[{name, template_id, status, error}]
# }
```

<!-- xlsx 使用模式 (从 15-RESEARCH.md 提取) -->

```typescript
import * as XLSX from 'xlsx';

interface BatchDevice {
  name: string;
  device_type: string;
  host: string;
  port: number;
  protocol: string;
  log_format: string;
}

async function parseBatchFile(file: File): Promise<BatchDevice[]> {
  const buffer = await file.arrayBuffer();
  const workbook = XLSX.read(buffer, { type: 'array' });
  const sheet = workbook.Sheets[workbook.SheetNames[0]];
  const rows: BatchDevice[] = XLSX.utils.sheet_to_json(sheet);

  return rows.map(row => ({
    name: row['设备名称'] || row['name'],
    device_type: row['设备类型'] || row['device_type'],
    host: row['主机'] || row['host'],
    port: parseInt(row['端口'] || row['port']) || 514,
    protocol: row['协议'] || row['protocol'] || 'ssh',
    log_format: row['日志格式'] || row['log_format'] || 'Auto',
  }));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建批量导入 API 端点</name>
  <files>src/api/ingestion_endpoints.py</files>
  <read_first>
    - src/api/ingestion_endpoints.py (现有端点模式)
    - src/api/ingestion_models.py (现有 Pydantic 模型)
  </read_first>
  <action>
    在 src/api/ingestion_endpoints.py 添加：

    1. 新增 Pydantic 模型（直接在文件中定义或新建 parse_test_models.py）：
       ```python
       class BatchDevice(BaseModel):
           name: str
           device_type: str
           host: str
           port: int = 514
           protocol: str = "ssh"
           log_format: str = "Auto"

       class BatchCreateRequest(BaseModel):
           devices: List[BatchDevice]
           apply_template_id: Optional[str] = None  # 统一应用模板

       class BatchCreateResult(BaseModel):
           name: str
           template_id: Optional[str]
           status: str  # "success" | "failure"
           error: Optional[str]

       class BatchCreateResponse(BaseModel):
           success_count: int
           failure_count: int
           results: List[BatchCreateResult]
       ```

    2. 新增 POST /api/ingestion/templates/batch 端点：
       - 接受 BatchCreateRequest
       - 遍历 devices 列表，逐个创建模板（复用现有 create_template 逻辑）
       - 如果 apply_template_id 存在，则使用该模板的 log_format 和 custom_regex
       - 收集成功/失败结果
       - 返回 BatchCreateResponse
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert && python -c "from src.api.ingestion_endpoints import router; print('Batch endpoint defined')"</automated>
  </verify>
  <done>
    POST /api/ingestion/templates/batch 端点存在
    接受 CSV/Excel 解析后的设备列表
    返回成功数、失败数、每个设备的状态
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 BatchImportModal 组件</name>
  <files>
    - frontend/src/components/ingestion/wizard/BatchImportModal.tsx
    - frontend/src/components/ingestion/wizard/DeviceTable.tsx
    - frontend/src/types/ingestion.ts
  </files>
  <read_first>
    - frontend/src/types/ingestion.ts (现有类型)
    - .planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md (Component Inventory - BatchImportModal, DeviceTable)
    - .planning/phases/15-data-ingestion-enhancement/15-RESEARCH.md (Pattern 3: Batch Import with xlsx)
  </read_first>
  <action>
    1. 扩展 frontend/src/types/ingestion.ts：
       - 添加 BatchDevice, BatchCreateRequest, BatchCreateResponse 接口
       - 添加 BatchImportState 类型

    2. 创建 DeviceTable.tsx：
       - 显示解析后的设备列表
       - 列：名称、设备类型、主机、端口、协议、状态
       - 行高 48px（UI-SPEC Spacing Scale）
       - 错误行高亮红色
       - 支持选择/取消选择

    3. 创建 BatchImportModal.tsx（参考 UI-SPEC Component Inventory）：
       - 状态：closed, file-select, parsing, complete, error
       - 拖拽区域或文件选择按钮
       - 调用 xlsx 库解析文件
       - 预览解析结果（DeviceTable）
       - 确认导入按钮
       - 显示导入结果（成功 n/m）

    4. 添加 xlsx 类型声明（如需要）：`npm install -D @types/xlsx`
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/BatchImportModal.tsx src/components/ingestion/wizard/DeviceTable.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    BatchImportModal 可打开/关闭
    用户可拖拽或选择 CSV/Excel 文件
    文件解析后显示预览表格
    确认导入后调用 API 并显示结果
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Step5BatchImport 并扩展 WizardModal</name>
  <files>
    - frontend/src/components/ingestion/wizard/Step5BatchImport.tsx
    - frontend/src/components/ingestion/wizard/WizardModal.tsx
    - frontend/src/stores/ingestionStore.ts
  </files>
  <read_first>
    - frontend/src/components/ingestion/wizard/WizardModal.tsx (现有)
    - frontend/src/components/ingestion/wizard/StepIndicator.tsx (现有)
    - frontend/src/stores/ingestionStore.ts (现有)
    - frontend/src/types/ingestion.ts (已更新)
  </read_first>
  <action>
    1. 扩展 ingestionStore.ts：
       - 添加 batchDevices 状态
       - 添加 batchImportResult 状态
       - 添加 openBatchModal, closeBatchModal actions

    2. 创建 Step5BatchImport.tsx：
       - 显示批量导入入口按钮
       - 点击打开 BatchImportModal
       - 显示已导入设备数量

    3. 更新 WizardModal.tsx：
       - 扩展 WIZARD_STEPS 为 6 步
       - 在 Step4 和 Step5 之间添加 Step5BatchImport
       - Step6 是 ParseTest（下一个 plan）
       - 确保步骤导航正确（next/prev）

    4. 更新 StepIndicator.tsx：
       - 显示 6 个步骤指示器
       - 当前步骤高亮
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/WizardModal.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    WizardModal 显示 6 个步骤
    Step5 是批量导入步骤
    点击可打开 BatchImportModal
  </done>
</task>

</tasks>

<verification>
- [ ] POST /api/ingestion/templates/batch 端点存在
- [ ] BatchImportModal 有 5 个状态
- [ ] xlsx 库正确解析 CSV/Excel
- [ ] DeviceTable 显示解析结果
- [ ] WizardModal 正确显示 6 个步骤
</verification>

<success_criteria>
1. 用户可在 Step5 打开批量导入对话框
2. 用户可拖拽 CSV/Excel 文件
3. 系统预览解析后的设备列表
4. 用户确认后系统批量创建模板
5. 系统显示成功/失败数量
6. 失败设备显示错误原因
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-ingestion-enhancement/15-03-SUMMARY.md`
</output>
