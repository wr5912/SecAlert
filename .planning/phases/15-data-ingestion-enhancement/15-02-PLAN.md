---
phase: 15-data-ingestion-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ingestion/wizard/FieldMapping/FieldMapper.tsx
  - frontend/src/components/ingestion/wizard/FieldMapping/DraggableField.tsx
  - frontend/src/components/ingestion/wizard/FieldMapping/MappingPreview.tsx
  - frontend/src/components/ingestion/wizard/Step3LogFormat.tsx
  - frontend/src/stores/ingestionStore.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "用户可在左侧看到检测到的源字段列表"
    - "用户可在右侧看到目标标准字段列表"
    - "用户拖拽源字段到目标区域完成映射"
    - "映射变更时实时预览解析结果"
  artifacts:
    - path: "frontend/src/components/ingestion/wizard/FieldMapping/FieldMapper.tsx"
      provides: "拖拽式字段映射主组件"
      min_lines: 80
    - path: "frontend/src/components/ingestion/wizard/FieldMapping/DraggableField.tsx"
      provides: "可拖拽字段项"
      states: "default, dragging, mapped"
    - path: "frontend/src/components/ingestion/wizard/FieldMapping/MappingPreview.tsx"
      provides: "映射结果实时预览"
      states: "empty, parsing, success, error"
  key_links:
    - from: "FieldMapper.tsx"
      to: "/api/ingestion/preview-parse"
      via: "TanStack Query"
      pattern: "useQuery.*preview-parse"
    - from: "DraggableField.tsx"
      to: "FieldMapper.tsx"
      via: "@dnd-kit/core DndContext"
      pattern: "DndContext.*onDragEnd"
---

<objective>
实现可视化字段映射功能：拖拽式界面，实时预览解析效果。

Purpose: 替代手动编辑正则表达式，提供直观的字段映射 UI
Output: FieldMapper 组件 + MappingPreview 组件
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/types/ingestion.ts
@frontend/src/stores/ingestionStore.ts
@frontend/src/components/ingestion/wizard/Step3LogFormat.tsx
@.planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md
@.planning/phases/15-data-ingestion-enhancement/15-RESEARCH.md

<interfaces>
<!-- 现有 ingestionStore 状态 (需扩展) -->

```typescript
interface IngestionState {
  // 新增字段
  sampleLogs: string[];
  aiRecognitionResult: LogFormatRecognitionResult | null;
  fieldMappings: Record<string, string>;  // { sourceField: targetField }

  // 新增 actions
  setSampleLogs: (logs: string[]) => void;
  setAiRecognitionResult: (result: LogFormatRecognitionResult) => void;
  setFieldMappings: (mappings: Record<string, string>) => void;
}

interface LogFormatRecognitionResult {
  detected_format: string;
  regex_pattern: string;
  field_mappings: Record<string, string>;
  confidence: number;
  reasoning: string;
}
```

<!-- @dnd-kit 使用模式 (从 15-RESEARCH.md 提取) -->

```typescript
import { DndContext, DragEndEvent } from '@dnd-kit/core';
import { SortableContext, useSortable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';

function DraggableSourceField({ field }) {
  const { attributes, listeners, setNodeRef, transform, transition } = useSortable({
    id: field.name,
    data: { type: 'source', field }
  });

  return (
    <div ref={setNodeRef} style={{ transform: CSS.Transform.toString(transform), transition }}>
      <div {...attributes} {...listeners}>{field.name}</div>
    </div>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装 @dnd-kit 依赖</name>
  <files>frontend/package.json</files>
  <read_first>
    - frontend/package.json (检查现有依赖)
    - .planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md (Registry Safety)
  </read_first>
  <action>
    在 frontend 目录执行：

    ```bash
    cd /home/admin/work/SecAlert/frontend
    npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
    ```

    验证安装成功：检查 package.json 包含 @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && cat package.json | grep -E '"@dnd-kit'</automated>
  </verify>
  <done>
    @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities 已添加到 package.json
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 DraggableField 和 FieldMapper 组件</name>
  <files>
    - frontend/src/components/ingestion/wizard/FieldMapping/DraggableField.tsx
    - frontend/src/components/ingestion/wizard/FieldMapping/FieldMapper.tsx
  </files>
  <read_first>
    - .planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md (Component Inventory - FieldMapper, DraggableField)
    - .planning/phases/15-data-ingestion-enhancement/15-RESEARCH.md (Pattern 2: Drag-and-Drop Field Mapping)
    - frontend/src/stores/ingestionStore.ts (已更新字段)
  </read_first>
  <action>
    1. 创建 frontend/src/components/ingestion/wizard/FieldMapping/ 目录

    2. 创建 DraggableField.tsx：
       - 使用 useSortable hook（@dnd-kit/sortable）
       - 44px 最小触摸目标（UI-SPEC Spacing Scale）
       - 状态样式：default（灰色边框）, dragging（accent 边框 + 放大）, mapped（success 色勾选标记）
       - Props: { id, name, type: 'source' | 'target', isMapped?: boolean }

    3. 创建 FieldMapper.tsx：
       - 使用 DndContext + SortableContext
       - 左侧区域：源字段列表（从 aiRecognitionResult.detected_fields 提取）
       - 右侧区域：目标字段下拉选择（标准字段：timestamp, src_ip, dst_ip, src_port, dst_port, protocol, action, message 等）
       - handleDragEnd: 更新 fieldMappings 状态
       - 调用 /api/ingestion/preview-parse 实时预览

    4. 创建 index.ts 导出以上组件
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/FieldMapping/FieldMapper.tsx src/components/ingestion/wizard/FieldMapping/DraggableField.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    DraggableField 可拖拽，有 dragging 状态
    FieldMapper 左右布局，支持拖拽映射
    拖拽后调用 preview-parse API
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 MappingPreview 组件并集成到 Step3</name>
  <files>
    - frontend/src/components/ingestion/wizard/FieldMapping/MappingPreview.tsx
    - frontend/src/components/ingestion/wizard/Step3LogFormat.tsx
    - frontend/src/stores/ingestionStore.ts
  </files>
  <read_first>
    - .planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md (Component Inventory - MappingPreview)
    - frontend/src/components/ingestion/wizard/Step3LogFormat.tsx (现有组件)
    - frontend/src/stores/ingestionStore.ts (现有 store)
  </read_first>
  <action>
    1. 扩展 ingestionStore.ts：
       - 添加 state: parsePreviewResult
       - 添加 action: setParsePreviewResult

    2. 创建 MappingPreview.tsx：
       - 调用 GET /api/ingestion/preview-parse?template_id=xxx
       - 显示解析后的字段值表格
       - 状态：empty（无数据）, parsing（加载中）, success（绿色）, error（红色）
       - 实时预览：每次 fieldMappings 变化时重新请求

    3. 更新 Step3LogFormat.tsx：
       - 在"自动识别"标签卡下添加 FieldMapper 和 MappingPreview
       - 布局：上部分 AIDetectPanel + SampleLogInput，下部分 FieldMapper + MappingPreview
       - 添加"应用映射"按钮（Secondary CTA）

    4. 确保 AI 识别成功后自动填充源字段到 FieldMapper
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/Step3LogFormat.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    Step3 显示完整 AI 识别 + 字段映射 + 预览流程
    用户拖拽映射后能看到实时解析预览
    点击"应用映射"保存映射到 store
  </done>
</task>

</tasks>

<verification>
- [ ] @dnd-kit 已安装
- [ ] DraggableField 有 default/dragging/mapped 三状态
- [ ] FieldMapper 左右布局，支持拖拽
- [ ] MappingPreview 实时显示解析结果
- [ ] Step3 集成完整流程
</verification>

<success_criteria>
1. 用户在 AI 识别成功后看到源字段列表
2. 用户可拖拽源字段到右侧目标区域
3. 拖拽后 MappingPreview 实时显示解析结果
4. 点击"应用映射"保存映射到 store
5. 44px 触摸目标符合无障碍标准
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-ingestion-enhancement/15-02-SUMMARY.md`
</output>
