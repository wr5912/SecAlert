---
phase: 15-data-ingestion-enhancement
plan: 09
type: execute
wave: 2
depends_on: [15-07, 15-08]
files_modified:
  - frontend/src/components/ingestion/wizard/WizardModal.tsx
  - frontend/src/stores/ingestionStore.ts
  - .planning/phases/15-data-ingestion-enhancement/15-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "WizardModal 6 步骤状态管理策略明确"
    - "步骤间数据保持策略定义"
    - "验证脚本使用精确的 ID 匹配"
  artifacts:
    - path: "frontend/src/components/ingestion/wizard/WizardModal.tsx"
      provides: "6 步骤向导"
      issue: "状态管理复杂，需要明确数据保持策略"
---

<objective>
修复 Phase 15 LOW priority 问题：
1. 明确 WizardModal 6 步骤状态管理策略
2. 定义步骤间数据保持策略
3. 修复验证脚本的 grep 匹配问题

Purpose: 完善文档和状态管理，减少后续维护成本
Output: 更新的 WizardModal 和 store
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@frontend/src/components/ingestion/wizard/WizardModal.tsx
@frontend/src/stores/ingestionStore.ts
@.planning/phases/15-data-ingestion-enhancement/15-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 明确 WizardModal 6 步骤状态管理策略</name>
  <files>
    - frontend/src/components/ingestion/wizard/WizardModal.tsx
    - frontend/src/stores/ingestionStore.ts
  </files>
  <read_first>
    - frontend/src/components/ingestion/wizard/WizardModal.tsx
    - frontend/src/stores/ingestionStore.ts
  </read_first>
  <action>
    明确 WizardModal 6 步骤的状态管理策略：

    1. 定义状态管理策略（作为代码注释添加到 WizardModal.tsx）：

       ```typescript
       /**
        * WizardModal 6 步骤状态管理策略：
        *
        * 1. 步骤导航状态 (currentStep)
        *    - 使用 useState 管理
        *    - next()/prev() 控制
        *    - 跳转时不做数据验证
        *
        * 2. 各步骤数据 (ingestionStore)
        *    - Step1-2: deviceType, connectionConfig -> 立即持久化到 store
        *    - Step3: sampleLogs, aiRecognitionResult, fieldMappings -> 实时保存
        *    - Step4: templateName -> 保存到 store
        *    - Step5: batchDevices, batchImportResult -> 导入成功后保存
        *    - Step6: parseTestResult -> 测试完成后保存
        *
        * 3. 数据保持策略
        *    - 用户在任意步骤关闭 Wizard，数据保留在 store 中
        *    - 用户再次打开 Wizard，从 store 恢复数据
        *    - 用户点击"重新开始"时，调用 resetWizard() 清空 store
        *
        * 4. 模板 ID 传递
        *    - AI 识别后自动创建模板 -> currentTemplateId
        *    - 批量导入后 -> batchCreatedTemplateIds[]
        *    - Step6 使用 selectedTemplateIdForTest
        */
       ```

    2. 添加 resetWizard action 到 ingestionStore.ts：
       ```typescript
       interface IngestionState {
         // ... 现有状态
         resetWizard: () => void;
       }

       // 实现
       const resetWizard = () => {
         set({
           // 清空所有向导相关状态
           currentStep: 1,
           deviceType: null,
           connectionConfig: null,
           sampleLogs: [],
           aiRecognitionResult: null,
           fieldMappings: {},
           currentTemplateId: null,
           currentTemplateSaved: false,
           templateName: '',
           batchDevices: [],
           batchImportResult: null,
           batchCreatedTemplateIds: [],
           selectedTemplateIdForTest: null,
           parseTestResult: null,
         });
       };
       ```

    3. 更新 WizardModal.tsx 的完成按钮：
       ```typescript
       const handleComplete = () => {
         // 解析测试通过后
         resetWizard();  // 清空状态
         onClose();      // 关闭向导
       };
       ```
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/WizardModal.tsx 2>&1 | head -10</automated>
  </verify>
  <done>
    WizardModal 状态管理策略已文档化
    resetWizard action 存在且功能正确
    完成按钮正确重置状态
  </done>
</task>

<task type="auto">
  <name>Task 2: 修复验证脚本的 grep 匹配问题</name>
  <files>.planning/phases/15-data-ingestion-enhancement/15-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/15-data-ingestion-enhancement/15-VERIFICATION.md
  </read_first>
  <action>
    修复验证脚本的 grep 匹配问题：原 grep 会匹配到已有的 DI-01~DI-06

    原来的验证命令：
    ```bash
    grep -E "DI-07|DI-08|DI-09" .planning/REQUIREMENTS.md | wc -l
    ```

    这个命令会匹配到任何包含 DI-07、DI-08、DI-09 的行，但如果有其他地方引用这些 ID 也会被匹配。

    修复方案：使用更精确的匹配，限定在 Traceability 表中：

    ```bash
    # 精确匹配 Traceability 表中的 DI-07、DI-08、DI-09 条目
    grep "^| DI-0[789] |" .planning/REQUIREMENTS.md | wc -l
    ```

    或者使用 awk 精确提取 Traceability 表部分：
    ```bash
    awk '/^## Traceability/,/^---/' .planning/REQUIREMENTS.md | grep "^| DI-0[789] |" | wc -l
    ```

    更新 .planning/phases/15-data-ingestion-enhancement/15-VERIFICATION.md 中的验证命令。
  </action>
  <verify>
    <automated>awk '/^## Traceability/,/^---/' .planning/REQUIREMENTS.md | grep "^| DI-0[789] |" | wc -l</automated>
  </verify>
  <done>
    验证脚本使用精确的 ID 匹配
    只匹配 Traceability 表中的 DI 条目
  </done>
</task>

</tasks>

<verification>
- [ ] WizardModal 状态管理策略已文档化
- [ ] resetWizard action 功能正确
- [ ] 验证脚本精确匹配 DI-07、DI-08、DI-09
</verification>

<success_criteria>
1. WizardModal.tsx 包含状态管理策略注释
2. resetWizard 清空所有向导相关状态
3. 验证脚本不会误匹配其他 DI 条目
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-ingestion-enhancement/15-09-SUMMARY.md`
</output>
