---
phase: 15-data-ingestion-enhancement
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - src/api/ingestion_endpoints.py
  - src/api/parse_test_models.py
  - frontend/src/components/ingestion/wizard/Step6ParseTest.tsx
  - frontend/src/components/ingestion/wizard/ParseTestPanel.tsx
  - frontend/src/components/ingestion/wizard/AccuracyBadge.tsx
  - frontend/src/components/ingestion/wizard/WizardModal.tsx
  - frontend/src/stores/ingestionStore.ts
  - frontend/src/types/ingestion.ts
autonomous: true
requirements:
  - DI-09

must_haves:
  truths:
    - "用户可选择或粘贴测试日志"
    - "系统计算解析准确率"
    - "系统显示字段级和整体准确率"
    - "准确率达标后启用完成按钮"
    - "准确率未达标时提示调整映射"
  artifacts:
    - path: "src/api/ingestion_endpoints.py"
      provides: "POST /api/ingestion/test-parse 端点"
      exports: "test_parse_accuracy"
    - path: "frontend/src/components/ingestion/wizard/ParseTestPanel.tsx"
      provides: "解析测试面板"
      states: "idle, testing, success, failure"
    - path: "frontend/src/components/ingestion/wizard/AccuracyBadge.tsx"
      provides: "准确率徽章"
      states: "qualified (>85%), warning (70-85%), failed (<70%)"
  key_links:
    - from: "ParseTestPanel.tsx"
      to: "/api/ingestion/test-parse"
      via: "TanStack Query mutation"
      pattern: "useMutation.*test-parse"
    - from: "test_parse_accuracy"
      to: "parser/pipeline.py"
      via: "ThreeTierParser.parse()"
      pattern: "ThreeTierParser.*parse"
---

<objective>
实现 DI-09 解析测试功能：用历史日志测试解析准确率，达标后开启实时接入。

Purpose: 确保配置正确后再开启实时接入，减少错误配置
Output: ParseTestPanel + test-parse API + AccuracyBadge
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/api/ingestion_endpoints.py
@src/api/parse_test_models.py
@parser/pipeline.py
@frontend/src/stores/ingestionStore.ts
@frontend/src/types/ingestion.ts
@.planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md
@.planning/phases/15-data-ingestion-enhancement/15-RESEARCH.md

<interfaces>
<!-- ParseTestRequest/Result (从 15-RESEARCH.md 提取) -->

```python
class ParseTestRequest(BaseModel):
    """解析测试请求"""
    template_id: str
    test_logs: List[str]  # 最多 1000 条
    ground_truth: Optional[List[Dict]] = None

class FieldAccuracy(BaseModel):
    """字段级别准确率"""
    field_name: str
    correct: int
    total: int
    accuracy: float

class ParseTestResult(BaseModel):
    """解析测试结果"""
    total_logs: int
    success_count: int
    failure_count: int
    overall_accuracy: float
    field_accuracies: List[FieldAccuracy]
    failed_samples: List[Dict]
    is_qualified: bool  # 准确率 >= 阈值
```

<!-- ThreeTierParser (从 pipeline.py 提取) -->

```python
class ThreeTierParser:
    def parse(self, raw_log: str, source_type: str) -> dict:
        """Parse raw log through three tiers."""
        # 返回 {"source_type": ..., "raw_event": ..., "parse_status": ...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建解析测试 API 端点</name>
  <files>src/api/ingestion_endpoints.py, src/api/parse_test_models.py</files>
  <read_first>
    - src/api/ingestion_endpoints.py (现有端点模式)
    - src/api/parse_test_models.py (如果已存在)
    - parser/pipeline.py (ThreeTierParser)
    - .planning/phases/15-data-ingestion-enhancement/15-RESEARCH.md (Code Examples - Parse Test Request Model)
  </read_first>
  <action>
    1. 确保 src/api/parse_test_models.py 包含 ParseTestRequest, FieldAccuracy, ParseTestResult

    2. 在 ingestion_endpoints.py 添加 POST /api/ingestion/test-parse 端点：
       - 接受 ParseTestRequest { template_id, test_logs, ground_truth }
       - 加载对应模板（如果不存在返回 404）
       - 使用 ThreeTierParser.parse() 逐条解析 test_logs
       - 计算准确率：
         - overall_accuracy = success_count / total_logs
         - field_accuracies：每个字段的正确率
       - 如果有 ground_truth，对比每个字段的值
       - 返回 ParseTestResult { total_logs, success_count, failure_count, overall_accuracy, field_accuracies, failed_samples, is_qualified }

    3. 准确率阈值配置：
       - 使用环境变量 PARSE_MIN_CONFIDENCE=0.85 或默认值 0.85
       - is_qualified = overall_accuracy >= 阈值
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert && python -c "from src.api.parse_test_models import ParseTestRequest, ParseTestResult; print('Models loaded')"</automated>
  </verify>
  <done>
    POST /api/ingestion/test-parse 端点存在
    返回 ParseTestResult 结构
    is_qualified 正确反映准确率是否达标
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 AccuracyBadge 和 ParseTestPanel 组件</name>
  <files>
    - frontend/src/components/ingestion/wizard/AccuracyBadge.tsx
    - frontend/src/components/ingestion/wizard/ParseTestPanel.tsx
    - frontend/src/types/ingestion.ts
  </files>
  <read_first>
    - frontend/src/types/ingestion.ts (现有类型)
    - .planning/phases/15-data-ingestion-enhancement/15-UI-SPEC.md (Component Inventory - AccuracyBadge, ParseTestPanel)
  </read_first>
  <action>
    1. 扩展 frontend/src/types/ingestion.ts：
       - 添加 ParseTestRequest, ParseTestResult, FieldAccuracy 接口
       - 添加 ParseTestState 类型

    2. 创建 AccuracyBadge.tsx（参考 UI-SPEC Component Inventory）：
       - Props: accuracy: number (0-100)
       - 显示百分比数字
       - 三种样式：
         - qualified (>85%): success 绿色 (#10b981)
         - warning (70-85%): warning 黄色 (#f59e0b)
         - failed (<70%): destructive 红色 (#ff2d55)
       - 使用 badge 样式

    3. 创建 ParseTestPanel.tsx（参考 UI-SPEC Component Inventory）：
       - 状态：idle, testing, success, failure
       - 输入：文本区域粘贴测试日志（或上传文件）
       - 显示：测试进度条
       - 结果展示：
         - AccuracyBadge（整体准确率）
         - 字段级准确率表格
         - 失败样例列表（最多 5 条）
       - 达标：启用"完成"按钮
       - 未达标：显示"准确率未达标，请调整字段映射"

    4. 调用 POST /api/ingestion/test-parse
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/AccuracyBadge.tsx src/components/ingestion/wizard/ParseTestPanel.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    AccuracyBadge 根据准确率显示不同颜色
    ParseTestPanel 有 4 个状态
    字段级准确率表格显示
    失败样例最多显示 5 条
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Step6ParseTest 并完成向导集成</name>
  <files>
    - frontend/src/components/ingestion/wizard/Step6ParseTest.tsx
    - frontend/src/components/ingestion/wizard/Step4Complete.tsx
    - frontend/src/components/ingestion/wizard/WizardModal.tsx
    - frontend/src/stores/ingestionStore.ts
  </files>
  <read_first>
    - frontend/src/components/ingestion/wizard/Step4Complete.tsx (现有)
    - frontend/src/components/ingestion/wizard/WizardModal.tsx (现有)
    - frontend/src/stores/ingestionStore.ts (现有)
  </read_first>
  <action>
    1. 扩展 ingestionStore.ts：
       - 添加 parseTestResult 状态
       - 添加 isTestQualified 状态
       - 添加 setParseTestResult action

    2. 创建 Step6ParseTest.tsx：
       - 渲染 ParseTestPanel
       - 导入模板 ID（从当前编辑的模板或 batch 创建的模板）
       - 达标后启用"完成"按钮

    3. 更新 Step4Complete.tsx：
       - 如果是编辑现有模板：显示"配置已更新"
       - 如果是新模板：显示配置汇总

    4. 更新 WizardModal.tsx：
       - 确认 6 个步骤正确排列
       - Step6 是最后一步（ParseTest）
       - 完成后调用 resetWizard 并关闭
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npx tsc --noEmit src/components/ingestion/wizard/WizardModal.tsx 2>&1 | head -20</automated>
  </verify>
  <done>
    WizardModal 完整 6 步骤向导
    Step6 是解析测试
    准确率达标后可点击完成
    完成时重置向导并关闭
  </done>
</task>

</tasks>

<verification>
- [ ] POST /api/ingestion/test-parse 端点存在
- [ ] AccuracyBadge 显示准确率及颜色
- [ ] ParseTestPanel 有 idle/testing/success/failure 四状态
- [ ] 字段级准确率表格显示
- [ ] 达标后"完成"按钮可用
- [ ] WizardModal 6 步骤完整
</verification>

<success_criteria>
1. 用户在 Step6 选择或粘贴测试日志
2. 用户点击"开始测试"
3. 系统调用 /api/ingestion/test-parse
4. 系统显示整体准确率和字段级准确率
5. 准确率 >= 85% 时 AccuracyBadge 显示绿色"达标"
6. 准确率 < 85% 时 AccuracyBadge 显示红色"未达标"
7. 达标后"完成"按钮可用
8. 点击完成重置向导并关闭
</success_criteria>

<output>
After completion, create `.planning/phases/15-data-ingestion-enhancement/15-04-SUMMARY.md`
</output>
