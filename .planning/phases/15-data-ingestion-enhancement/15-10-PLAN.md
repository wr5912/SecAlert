---
phase: 15-data-ingestion-enhancement
plan: "15-10"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/ingestion.ts
  - frontend/src/components/ingestion/wizard/WizardModal.tsx
  - frontend/src/components/ingestion/wizard/StepIndicator.tsx
  - frontend/src/components/ingestion/wizard/Step4Complete.tsx
autonomous: true
requirements:
  - DI-07
  - DI-08
  - DI-09
must_haves:
  truths:
    - "向导从 6 步压缩到 4 步"
    - "步骤 4 包含模板设置和解析测试"
    - "解析测试通过前「完成」按钮禁用"
    - "解析测试通过后显示确认页，可再次测试"
  artifacts:
    - path: "frontend/src/types/ingestion.ts"
      provides: "WIZARD_STEPS 更新为 4 步"
      contains: "{ num: 4, label: '完成' }"
    - path: "frontend/src/components/ingestion/wizard/WizardModal.tsx"
      provides: "向导容器仅渲染 4 个步骤"
      exports: "renderStep, canGoNext"
    - path: "frontend/src/components/ingestion/wizard/StepIndicator.tsx"
      provides: "步骤指示器仅显示 4 步"
    - path: "frontend/src/components/ingestion/wizard/Step4Complete.tsx"
      provides: "步骤 4 包含模板设置 + ParseTestPanel + 确认页"
      contains: "ParseTestPanel, isTestQualified, setParseTestResult"
  key_links:
    - from: "Step4Complete.tsx"
      to: "ParseTestPanel"
      via: "import and conditional render"
      pattern: "ParseTestPanel"
    - from: "WizardModal.tsx"
      to: "Step4Complete.tsx"
      via: "step === 4 render"
      pattern: "case 4.*Step4Complete"
---

<objective>
**Purpose:** 将向导从 6 步压缩到 4 步，合并步骤 4 和步骤 6，移除批量导入步骤

**Output:** 重构后的 4 步向导，步骤 4 包含模板设置 + 解析测试 + 确认页

**Decisions Implemented:**
- D-01: 向导从 6 步压缩到 4 步
- D-03: 解析测试必须在点击「完成」前通过
- D-04: 模板设置 → 开始解析测试 → 测试通过 → 显示确认页 → 点「完成」关闭
- D-05: 解析测试未通过时，「完成」按钮禁用
- D-08: 解析测试通过后，显示确认页（有「完成」按钮和「再次测试」选项）
- D-09: 点击「完成」关闭弹窗，数据保存
</objective>

<context>
@frontend/src/types/ingestion.ts
@frontend/src/components/ingestion/wizard/WizardModal.tsx
@frontend/src/components/ingestion/wizard/StepIndicator.tsx
@frontend/src/components/ingestion/wizard/Step4Complete.tsx
@frontend/src/components/ingestion/wizard/Step6ParseTest.tsx
@frontend/src/components/ingestion/wizard/ParseTestPanel.tsx
@frontend/src/components/ingestion/wizard/AccuracyBadge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 更新 WIZARD_STEPS 常量从 6 步到 4 步</name>
  <files>frontend/src/types/ingestion.ts</files>
  <read_first>
    - frontend/src/types/ingestion.ts (当前 WIZARD_STEPS 定义)
  </read_first>
  <action>
    更新 `frontend/src/types/ingestion.ts` 中的 `WIZARD_STEPS` 数组：

    **修改前 (6步):**
    ```typescript
    export const WIZARD_STEPS: WizardStep[] = [
      { num: 1, label: '设备类型' },
      { num: 2, label: '连接参数' },
      { num: 3, label: '日志格式' },
      { num: 4, label: '模板设置' },
      { num: 5, label: '批量导入' },
      { num: 6, label: '完成' },
    ];
    ```

    **修改后 (4步):**
    ```typescript
    export const WIZARD_STEPS: WizardStep[] = [
      { num: 1, label: '设备类型' },
      { num: 2, label: '连接参数' },
      { num: 3, label: '日志格式' },
      { num: 4, label: '完成' },
    ];
    ```

    同时保留 `WizardStep` 接口定义不变。
  </action>
  <acceptance_criteria>
    - `grep -n "num: [0-9]" frontend/src/types/ingestion.ts | grep WIZARD_STEPS` 输出包含 4 个 num 字段 (1, 2, 3, 4)
    - `grep -n "设备类型\|连接参数\|日志格式\|完成" frontend/src/types/ingestion.ts` 包含这 4 个标签
    - 不存在 `批量导入` 相关定义
  </acceptance_criteria>
  <verify>
    <automated>grep -A 5 "WIZARD_STEPS" frontend/src/types/ingestion.ts | grep -c "num:"</automated>
  </verify>
  <done>WIZARD_STEPS 更新为 4 步，移除了步骤 5（批量导入）和步骤 6（完成）</done>
</task>

<task type="auto">
  <name>Task 2: 重构 Step4Complete 合并 ParseTestPanel</name>
  <files>frontend/src/components/ingestion/wizard/Step4Complete.tsx</files>
  <read_first>
    - frontend/src/components/ingestion/wizard/Step4Complete.tsx (当前实现)
    - frontend/src/components/ingestion/wizard/Step6ParseTest.tsx (将被合并的功能)
    - frontend/src/components/ingestion/wizard/ParseTestPanel.tsx (解析测试组件)
    - frontend/src/components/ingestion/wizard/AccuracyBadge.tsx (准确率徽章)
  </read_first>
  <action>
    重构 `Step4Complete.tsx`，实现以下内部状态机：

    **内部状态 (`step4Phase`):**
    - `'settings'`: 模板设置阶段（默认）
    - `'testing'`: 解析测试阶段（用户点击「开始解析测试」后）
    - `'confirmed'`: 确认阶段（测试通过后）

    **布局结构:**
    ```
    ┌─────────────────────────────────────────┐
    │  模板设置（初始状态）                      │
    │  - 模板名称输入                          │
    │  - 配置摘要显示                          │
    │  - 「开始解析测试」按钮                   │
    └─────────────────────────────────────────┘
           ↓ 点击「开始解析测试」
    ┌─────────────────────────────────────────┐
    │  解析测试面板（ParseTestPanel）           │
    │  - 日志输入区                            │
    │  - 准确率显示                            │
    │  - 「重新测试」按钮                       │
    └─────────────────────────────────────────┘
           ↓ 测试通过 (is_qualified=true)
    ┌─────────────────────────────────────────┐
    │  确认页                                  │
    │  - ✓ 解析成功提示                        │
    │  - 准确率显示（达标）                    │
    │  - 「再次测试」按钮                       │
    │  - 「完成」按钮（启用）                   │
    └─────────────────────────────────────────┘
    ```

    **关键实现:**

    1. 导入 ParseTestPanel 和 useIngestionStore 相关状态：
    ```typescript
    import { ParseTestPanel } from './ParseTestPanel';
    import { useIngestionStore } from '@/stores/ingestionStore';
    import type { ParseTestResult } from '@/types/ingestion';
    import { CheckCircle, AlertTriangle } from 'lucide-react';
    ```

    2. 新增内部状态：
    ```typescript
    const [step4Phase, setStep4Phase] = useState<'settings' | 'testing' | 'confirmed'>('settings');
    ```

    3. 处理测试 qualified 回调：
    ```typescript
    const handleTestQualified = (result: ParseTestResult) => {
      setParseTestResult(result, true);
      setStep4Phase('confirmed');
    };
    ```

    4. 处理「开始解析测试」按钮：
    ```typescript
    const handleStartTest = () => {
      setStep4Phase('testing');
    };
    ```

    5. 处理「再次测试」按钮：
    ```typescript
    const handleTestAgain = () => {
      setParseTestResult(null, false);
      setStep4Phase('testing');
    };
    ```

    6. 渲染逻辑（switch step4Phase）:
    - `'settings'`: 显示模板名称输入 + 配置摘要 + 「开始解析测试」按钮
    - `'testing'`: 显示 ParseTestPanel，onQualified={handleTestQualified}
    - `'confirmed'`: 显示成功提示 + 准确率 + 「再次测试」按钮 + 「完成」按钮（disabled={!isTestQualified}）

    7. 从 useIngestionStore 获取/设置：
    ```typescript
    const {
      // ... 现有状态
      parseTestResult,
      isTestQualified,
      setParseTestResult,
    } = useIngestionStore();
    ```

    **D-05 实现:** 完成按钮在 `!isTestQualified` 时 `disabled`

    **D-08 实现:** 确认页有「完成」和「再次测试」两个按钮
  </action>
  <acceptance_criteria>
    - Step4Complete.tsx 包含 `ParseTestPanel` 导入
    - Step4Complete.tsx 包含 `step4Phase` 状态定义 (`'settings' | 'testing' | 'confirmed'`)
    - Step4Complete.tsx 包含 `handleStartTest` 函数调用 setStep4Phase('testing')
    - Step4Complete.tsx 包含 `handleTestQualified` 回调设置 isTestQualified=true
    - Step4Complete.tsx 包含确认页渲染逻辑，有「完成」和「再次测试」按钮
    - 完成按钮的 disabled 条件包含 `!isTestQualified`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ParseTestPanel\|step4Phase\|handleStartTest\|handleTestQualified\|handleTestAgain" frontend/src/components/ingestion/wizard/Step4Complete.tsx</automated>
  </verify>
  <done>Step4Complete 包含 3 个内部阶段：settings、testing、confirmed，ParseTestPanel 在 testing 阶段显示</done>
</task>

<task type="auto">
  <name>Task 3: 重构 WizardModal 移除步骤 5 和 6</name>
  <files>frontend/src/components/ingestion/wizard/WizardModal.tsx</files>
  <read_first>
    - frontend/src/components/ingestion/wizard/WizardModal.tsx (当前 6 步实现)
  </read_first>
  <action>
    重构 `WizardModal.tsx`，实现以下更改：

    **1. 移除导入:**
    - 移除 `Step5BatchImport` 导入
    - 移除 `Step6ParseTest` 导入

    **2. 更新 renderStep switch:**
    ```typescript
    const renderStep = () => {
      switch (step) {
        case 1: return <Step1DeviceType />;
        case 2: return <Step2Connection />;
        case 3: return <Step3LogFormat />;
        case 4: return <Step4Complete onFinish={handleClose} />;
        default: return null;
      }
    };
    ```

    **3. 更新 canGoNext:**
    ```typescript
    const canGoNext = () => {
      switch (step) {
        case 1: return !!deviceType;
        case 2: return !!connection;
        case 3: return !!logFormat;
        case 4: return true;  // Step4 内部状态控制完成按钮
        default: return false;
      }
    };
    ```

    **4. 更新 Footer 条件:**
    ```typescript
    {!isEditMode && step <= 4 && (
      <DialogFooter>
        {step > 1 && (
          <Button variant="ghost" onClick={prevStep}>
            上一步
          </Button>
        )}
        <Button variant="ghost" onClick={handleClose}>
          取消
        </Button>
        <Button onClick={nextStep} disabled={!canGoNext()}>
          下一步
        </Button>
      </DialogFooter>
    )}
    ```

    **5. 移除「跳过批量导入」按钮:** 步骤 5 已不存在，相关逻辑移除

    **关键:** WizardModal 不再直接渲染 Step5BatchImport 和 Step6ParseTest，这些功能已合并到 Step4Complete 内部
  </action>
  <acceptance_criteria>
    - WizardModal.tsx 不包含 `Step5BatchImport` 导入
    - WizardModal.tsx 不包含 `Step6ParseTest` 导入
    - renderStep 函数 case 4 调用 Step4Complete
    - renderStep 函数不包含 case 5 和 case 6
    - canGoNext(4) 返回 true
    - DialogFooter 条件为 `step <= 4`
    - 不存在「跳过批量导入」按钮
  </acceptance_criteria>
  <verify>
    <automated>grep "Step5BatchImport\|Step6ParseTest" frontend/src/components/ingestion/wizard/WizardModal.tsx && echo "FAIL: found removed steps" || echo "PASS: no removed steps"</automated>
  </verify>
  <done>WizardModal 仅渲染 4 个步骤，移除了步骤 5 和 6 的渲染逻辑</done>
</task>

<task type="auto">
  <name>Task 4: 更新 StepIndicator 支持 4 步显示</name>
  <files>frontend/src/components/ingestion/wizard/StepIndicator.tsx</files>
  <read_first>
    - frontend/src/components/ingestion/wizard/StepIndicator.tsx (当前实现)
  </read_first>
  <action>
    StepIndicator.tsx 使用 WIZARD_STEPS 常量进行渲染，由于 WIZARD_STEPS 已更新为 4 步，该组件会自动显示 4 个步骤圆点。

    验证以下渲染逻辑正确：
    - `WIZARD_STEPS.map(...)` 遍历所有步骤
    - `isCompleted = step > s.num` 正确判断完成状态
    - `isActive = step === s.num` 正确判断当前步骤
    - 连接线在 `i < WIZARD_STEPS.length - 1` 时显示

    无需修改代码，仅验证渲染逻辑正确。
  </action>
  <acceptance_criteria>
    - StepIndicator.tsx 导入 WIZARD_STEPS 从 '@/types/ingestion'
    - 渲染逻辑使用 WIZARD_STEPS.length 确定步骤数量
    - WIZARD_STEPS 更新为 4 步后，指示器自动显示 4 步
  </acceptance_criteria>
  <verify>
    <automated>grep "WIZARD_STEPS" frontend/src/components/ingestion/wizard/StepIndicator.tsx</automated>
  </verify>
  <done>StepIndicator 自动适应 4 步 WIZARD_STEPS 配置</done>
</task>

</tasks>

<verification>
## Phase 15-10 Verification

**WIZARD_STEPS 检查:**
```bash
grep -A 6 "WIZARD_STEPS" frontend/src/types/ingestion.ts
```
期望: 4 个步骤定义，标签为「设备类型」「连接参数」「日志格式」「完成」

**Step4Complete 合并检查:**
```bash
grep "ParseTestPanel\|step4Phase" frontend/src/components/ingestion/wizard/Step4Complete.tsx
```
期望: 包含 ParseTestPanel 导入和使用，step4Phase 状态定义

**WizardModal 移除检查:**
```bash
grep "Step5BatchImport\|Step6ParseTest" frontend/src/components/ingestion/wizard/WizardModal.tsx
```
期望: 无输出（已移除导入）

**Step 数量检查:**
```bash
grep -c "case [0-9]:" frontend/src/components/ingestion/wizard/WizardModal.tsx
```
期望: 4 个 case 语句 (1, 2, 3, 4)
</verification>

<success_criteria>
1. **WIZARD_STEPS 更新:** 从 6 步减少到 4 步，移除步骤 5（批量导入）和步骤 6（完成）
2. **Step4Complete 合并:** 包含 3 个内部阶段 (settings/testing/confirmed)，ParseTestPanel 在 testing 阶段显示
3. **解析测试通过前完成按钮禁用:** `disabled={!isTestQualified}` 实现 D-05
4. **确认页功能:** 包含「完成」按钮和「再次测试」选项，实现 D-08
5. **WizardModal 重构:** 仅渲染 4 个步骤，移除 Step5BatchImport 和 Step6ParseTest
6. **StepIndicator 自动适应:** 无需修改，使用 WIZARD_STEPS 配置

**注意:** 批量导入作为独立功能（不在弹窗内）将在单独的 phase 中规划，不在 15-10 范围内。
</success_criteria>

<output>
完成后创建 `.planning/phases/15-data-ingestion-enhancement/15-10-SUMMARY.md`
</output>
