# Phase 8: 报表 - Plan 02

---
phase: 08-reporting
plan: 02
type: execute
wave: 2
depends_on:
  - 08-reporting-01
files_modified:
  - src/analysis/report_scheduler.py
  - src/api/main.py
  - src/templates/reports/weekly_report.html
autonomous: true
requirements:
  - RP-01
  - RP-02
user_setup: []

must_haves:
  truths:
    - "系统每日 08:00 自动生成日报"
    - "系统每周一 09:00 自动生成周报"
  artifacts:
    - path: "src/analysis/report_scheduler.py"
      provides: "APScheduler 定时调度"
      contains: "AsyncIOScheduler, daily_report, weekly_report"
    - path: "src/templates/reports/weekly_report.html"
      provides: "周报 HTML 模板"
      contains: "SecAlert 周报"
  key_links:
    - from: "src/api/main.py"
      to: "src/api/reports.py"
      via: "app.include_router(reports_router)"
    - from: "src/analysis/report_scheduler.py"
      to: "src/analysis/report_aggregator.py"
      via: "ReportAggregator().collect_daily_metrics()"
    - from: "src/analysis/report_scheduler.py"
      to: "src/exporters/pdf_exporter.py"
      via: "generate_pdf()"
---

<objective>
实现定时报表生成功能 (日报/周报)，集成 APScheduler 到 FastAPI lifespan。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/api/main.py
@src/api/reports.py
@src/analysis/report_aggregator.py
@src/exporters/pdf_exporter.py
@.planning/phases/08-reporting/08-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01 创建的接口 -->

From src/api/reports.py (Plan 01):
```python
class ReportAggregator:
    def collect_daily_metrics(self, date: datetime) -> DailyMetrics: ...
    def collect_weekly_metrics(self, start_date: datetime) -> Dict[str, Any]: ...
    def get_trends(self, days: int) -> List[Dict[str, Any]]: ...
```

From src/exporters/pdf_exporter.py (Plan 01):
```python
def generate_pdf(template_name: str, context: Dict[str, Any]) -> bytes: ...
```

From src/api/main.py:
```python
# FastAPI lifespan pattern
@asynccontextmanager
async def lifespan(app: FastAPI):
    logger.info("SecAlert API 启动中...")
    yield
    logger.info("SecAlert API 关闭中...")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 APScheduler 定时任务调度器 (src/analysis/report_scheduler.py)</name>
  <files>src/analysis/report_scheduler.py</files>
  <read_first>
    src/api/main.py
    src/analysis/report_aggregator.py
    src/exporters/pdf_exporter.py
  </read_first>
  <action>
    创建 src/analysis/report_scheduler.py:

    ```python
    """报表定时调度器

    使用 APScheduler 实现日报/周报自动生成
    """
    import logging
    from datetime import datetime, timedelta
    from apscheduler.schedulers.asyncio import AsyncIOScheduler
    from apscheduler.triggers.cron import CronTrigger

    from src.analysis.report_aggregator import ReportAggregator
    from src.exporters.pdf_exporter import generate_pdf

    logger = logging.getLogger(__name__)

    # 单一 scheduler 实例
    scheduler = AsyncIOScheduler()

    # 报表存储路径
    REPORTS_DIR = "storage/reports"

    async def generate_daily_report():
        """每日 08:00 生成日报"""
        logger.info("开始生成日报...")
        try:
            # 1. 收集昨日数据
            yesterday = datetime.now() - timedelta(days=1)
            aggregator = ReportAggregator()
            metrics = aggregator.collect_daily_metrics(yesterday)

            # 2. 渲染 PDF
            context = {
                "date": yesterday.strftime("%Y-%m-%d"),
                "metrics": metrics.__dict__ if hasattr(metrics, '__dict__') else metrics,
                "generated_at": datetime.now().isoformat()
            }
            pdf_bytes = generate_pdf("daily_report.html", context)

            # 3. 保存到文件
            import os
            os.makedirs(REPORTS_DIR, exist_ok=True)
            filename = f"{REPORTS_DIR}/daily_{yesterday.strftime('%Y%m%d')}.pdf"
            with open(filename, "wb") as f:
                f.write(pdf_bytes)

            logger.info(f"日报生成完成: {filename}")
        except Exception as e:
            logger.error(f"日报生成失败: {e}", exc_info=True)

    async def generate_weekly_report():
        """每周一 09:00 生成周报"""
        logger.info("开始生成周报...")
        try:
            # 1. 计算上周时间范围 (周一到周日)
            today = datetime.now()
            # 找到上周一
            days_since_monday = today.weekday()
            last_monday = today - timedelta(days=days_since_monday + 7)

            aggregator = ReportAggregator()
            metrics = aggregator.collect_weekly_metrics(last_monday)

            # 2. 渲染 PDF
            context = {
                "start_date": last_monday.strftime("%Y-%m-%d"),
                "end_date": (last_monday + timedelta(days=6)).strftime("%Y-%m-%d"),
                "metrics": metrics,
                "generated_at": datetime.now().isoformat()
            }
            pdf_bytes = generate_pdf("weekly_report.html", context)

            # 3. 保存
            import os
            os.makedirs(REPORTS_DIR, exist_ok=True)
            filename = f"{REPORTS_DIR}/weekly_{last_monday.strftime('%Y%m%d')}.pdf"
            with open(filename, "wb") as f:
                f.write(pdf_bytes)

            logger.info(f"周报生成完成: {filename}")
        except Exception as e:
            logger.error(f"周报生成失败: {e}", exc_info=True)

    def setup_scheduler():
        """配置定时任务并启动调度器"""
        scheduler.add_job(
            generate_daily_report,
            CronTrigger(hour=8, minute=0, timezone="Asia/Shanghai"),
            id="daily_report",
            replace_existing=True,
            misfire_grace_time=3600  # 允许1小时内的错过触发
        )
        scheduler.add_job(
            generate_weekly_report,
            CronTrigger(day_of_week="mon", hour=9, minute=0, timezone="Asia/Shanghai"),
            id="weekly_report",
            replace_existing=True,
            misfire_grace_time=3600
        )
        scheduler.start()
        logger.info("报表调度器已启动")

    def shutdown_scheduler():
        """关闭调度器"""
        scheduler.shutdown(wait=False)
        logger.info("报表调度器已关闭")
    ```

    注意: 时区使用 Asia/Shanghai 确保报表在正确时间生成
  </action>
  <acceptance_criteria>
    - grep "AsyncIOScheduler" src/analysis/report_scheduler.py 返回 1+ 行
    - grep "generate_daily_report" src/analysis/report_scheduler.py 返回 1+ 行
    - grep "generate_weekly_report" src/analysis/report_scheduler.py 返回 1+ 行
    - grep "CronTrigger" src/analysis/report_scheduler.py 返回 1+ 行
    - grep "setup_scheduler" src/analysis/report_scheduler.py 返回 1+ 行
  </acceptance_criteria>
  <done>APScheduler 定时任务已配置，每日 08:00 生成日报，每周 一 09:00 生成周报</done>
</task>

<task type="auto">
  <name>Task 2: 在 FastAPI lifespan 中集成调度器</name>
  <files>src/api/main.py</files>
  <read_first>
    src/api/main.py
    src/analysis/report_scheduler.py
  </read_first>
  <action>
    修改 src/api/main.py 的 lifespan 函数:

    1. 在启动部分 (yield 之前) 添加:
    ```python
    # 启动报表调度器
    try:
        from src.analysis.report_scheduler import setup_scheduler
        setup_scheduler()
        logger.info("报表调度器初始化完成")
    except Exception as e:
        logger.warning(f"报表调度器初始化失败: {e}")
    ```

    2. 在关闭部分 (yield 之后) 添加:
    ```python
    # 关闭报表调度器
    try:
        from src.analysis.report_scheduler import shutdown_scheduler
        shutdown_scheduler()
    except Exception as e:
        logger.warning(f"报表调度器关闭失败: {e}")
    ```

    3. 在路由注册部分添加:
    ```python
    from src.api.reports import router as reports_router
    # ...
    app.include_router(reports_router)
    ```
  </action>
  <acceptance_criteria>
    - grep "setup_scheduler" src/api/main.py 返回 1+ 行
    - grep "shutdown_scheduler" src/api/main.py 返回 1+ 行
    - grep "reports_router" src/api/main.py 返回 1+ 行
    - grep "reports" src/api/main.py 返回包含 "/api/reports" 的行
  </acceptance_criteria>
  <done>FastAPI 启动时自动启动调度器，关闭时自动关闭调度器</done>
</task>

<task type="auto">
  <name>Task 3: 创建周报 Jinja2 模板 (src/templates/reports/weekly_report.html)</name>
  <files>src/templates/reports/weekly_report.html</files>
  <read_first>
    src/templates/reports/daily_report.html
  </read_first>
  <action>
    创建 src/templates/reports/weekly_report.html:

    ```html
    <!DOCTYPE html>
    <html lang="zh-CN">
    <head>
        <meta charset="UTF-8">
        <title>SecAlert 周报</title>
        <style>
            body {
                font-family: "Noto CJK SC", "Microsoft YaHei", sans-serif;
                margin: 40px;
                color: #333;
            }
            .header {
                text-align: center;
                margin-bottom: 30px;
                border-bottom: 2px solid #4472C4;
                padding-bottom: 20px;
            }
            .header h1 {
                color: #4472C4;
                margin: 0;
            }
            .header .date-range {
                color: #666;
                margin-top: 10px;
            }
            .summary-cards {
                display: flex;
                gap: 20px;
                margin-bottom: 30px;
            }
            .card {
                flex: 1;
                padding: 20px;
                border-radius: 8px;
                background: #f5f5f5;
                text-align: center;
            }
            .card .value {
                font-size: 28px;
                font-weight: bold;
                color: #333;
            }
            .card .label {
                color: #666;
                margin-top: 5px;
            }
            .card.highlight {
                background: #4472C4;
                color: white;
            }
            .card.highlight .label {
                color: #ddd;
            }
            table {
                width: 100%;
                border-collapse: collapse;
                margin-top: 20px;
            }
            th, td {
                border: 1px solid #ddd;
                padding: 12px;
                text-align: left;
            }
            th {
                background: #4472C4;
                color: white;
            }
            tr:nth-child(even) {
                background: #f9f9f9;
            }
            .footer {
                margin-top: 40px;
                text-align: center;
                color: #999;
                font-size: 12px;
            }
        </style>
    </head>
    <body>
        <div class="header">
            <h1>SecAlert 安全周报</h1>
            <div class="date-range">{{ start_date }} 至 {{ end_date }}</div>
        </div>

        <div class="summary-cards">
            <div class="card">
                <div class="value">{{ metrics.total_alerts }}</div>
                <div class="label">本周告警总数</div>
            </div>
            <div class="card highlight">
                <div class="value">{{ metrics.true_positives }}</div>
                <div class="label">真实威胁</div>
            </div>
            <div class="card">
                <div class="value">{{ metrics.false_positives }}</div>
                <div class="label">误报数量</div>
            </div>
            <div class="card">
                <div class="value">{{ "%.1f"|format(metrics.fp_rate * 100) }}%</div>
                <div class="label">误报率</div>
            </div>
        </div>

        <h2>每日明细</h2>
        <table>
            <thead>
                <tr>
                    <th>日期</th>
                    <th>总告警</th>
                    <th>真威胁</th>
                    <th>误报</th>
                    <th>误报率</th>
                </tr>
            </thead>
            <tbody>
                {% for day in metrics.daily_breakdown %}
                <tr>
                    <td>{{ day.date }}</td>
                    <td>{{ day.total }}</td>
                    <td>{{ day.true_positives }}</td>
                    <td>{{ day.false_positives }}</td>
                    <td>{{ "%.1f"|format(day.fp_rate * 100) }}%</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>

        <div class="footer">
            生成时间: {{ generated_at }} | SecAlert 安全告警分析系统
        </div>
    </body>
    </html>
    ```
  </action>
  <acceptance_criteria>
    - grep "SecAlert 周报" src/templates/reports/weekly_report.html 返回 1+ 行
    - grep "daily_breakdown" src/templates/reports/weekly_report.html 返回 1+ 行
    - grep "jinja2" src/templates/reports/weekly_report.html 返回 1+ 行 (模板语法)
    - 文件 src/templates/reports/weekly_report.html 存在
  </acceptance_criteria>
  <done>周报模板包含统计卡片、每日明细表格，支持 Jinja2 语法</done>
</task>

</tasks>

<verification>
- src/analysis/report_scheduler.py 存在且包含 AsyncIOScheduler
- src/api/main.py 导入并调用 setup_scheduler 和 shutdown_scheduler
- src/api/main.py 注册 reports_router
- src/templates/reports/weekly_report.html 存在
</verification>

<success_criteria>
- FastAPI 启动后 scheduler.start() 被调用
- 日报任务在每日 08:00 (Asia/Shanghai) 触发
- 周报任务在每周一 09:00 (Asia/Shanghai) 触发
- 周报模板包含 7 天数据聚合
</success_criteria>

<output>
完成后创建 .planning/phases/08-reporting/08-02-SUMMARY.md
</output>
