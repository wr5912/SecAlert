---
phase: 02-attack-chain-correlation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_chain/test_correlator.py
  - tests/test_chain/test_attck_mapper.py
  - tests/test_chain/test_chain_reconstruction.py
  - tests/test_chain/test_chain_api.py
  - tests/conftest.py
  - rules/attck_suricata.yaml
autonomous: true
requirements:
  - CHAIN-01
user_setup: []

must_haves:
  truths:
    - "Test fixtures can generate mock OCSF alerts for unit testing"
    - "Neo4j test client can be spun up for integration tests"
    - "Suricata signatures map to valid ATT&CK techniques"
  artifacts:
    - path: "tests/test_chain/"
      provides: "Phase 2 test directory"
    - path: "tests/conftest.py"
      provides: "Shared fixtures for Neo4j and mock alerts"
    - path: "rules/attck_suricata.yaml"
      provides: "Suricata signature to ATT&CK technique mapping rules"
  key_links:
    - from: "tests/conftest.py"
      to: "pytest"
      via: "pytest fixtures"
    - from: "rules/attck_suricata.yaml"
      to: "src/chain/mitre/mapper.py"
      via: "YAML rule loading"
---

<objective>
创建 Phase 2 测试基础设施和 ATT&CK 映射规则文件。

Purpose: 为攻击链关联引擎提供测试骨架和预置规则，使后续 plan 的单元测试可以运行。
Output: tests/test_chain/ 目录结构、共享 fixtures、Suricata ATT&CK 映射规则文件。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-attack-chain-correlation/02-RESEARCH.md
@.planning/phases/02-attack-chain-correlation/02-CONTEXT.md
@.planning/phases/02-attack-chain-correlation/02-VALIDATION.md

# ATT&CK 映射规则格式参考 (来自 RESEARCH.md)
# 格式: {alert_signature: {tactic: TA0001, technique_id: T1190, technique_name: "Exploit Public-Facing Application"}}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 2 test directory structure</name>
  <files>tests/test_chain/__init__.py, tests/test_chain/conftest.py</files>
  <action>
创建 `tests/test_chain/` 目录及以下文件:

1. `tests/test_chain/__init__.py` - 空文件，标记 Python 包

2. `tests/conftest.py` (如果不存在则创建，存在则追加) - 添加以下 pytest fixtures:

```python
import pytest
from datetime import datetime, timezone
from typing import Dict, List
import uuid

# --- Mock OCSF Alert Fixtures ---

@pytest.fixture
def mock_ocsf_alert() -> Dict:
    """生成单条 mock OCSF 格式告警"""
    return {
        "event_id": str(uuid.uuid4()),
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "source_type": "suricata",
        "event_type": "alert",
        "alert_signature": "ET SCAN Potential SSH Scan",
        "severity": 2,
        "src_ip": "192.168.1.100",
        "dst_ip": "10.0.0.50",
        "src_port": 45678,
        "dst_port": 22,
        "protocol": "TCP",
        "ocsf_event": {"raw": "suricata alert event"}
    }

@pytest.fixture
def mock_ocsf_alerts() -> List[Dict]:
    """生成多条 mock OCSF 格式告警，用于关联测试"""
    base_time = datetime(2026, 3, 23, 10, 0, 0, tzinfo=timezone.utc)
    return [
        {
            "event_id": str(uuid.uuid4()),
            "timestamp": (base_time).isoformat(),
            "source_type": "suricata",
            "event_type": "alert",
            "alert_signature": "ET SCAN Potential SSH Scan",
            "severity": 2,
            "src_ip": "192.168.1.100",
            "dst_ip": "10.0.0.50",
            "src_port": 45678,
            "dst_port": 22,
            "protocol": "TCP"
        },
        {
            "event_id": str(uuid.uuid4()),
            "timestamp": (base_time.replace(minute=2)).isoformat(),
            "source_type": "suricata",
            "event_type": "alert",
            "alert_signature": "ET SCAN Potential SSH Scan",
            "severity": 2,
            "src_ip": "192.168.1.100",
            "dst_ip": "10.0.0.50",
            "src_port": 45679,
            "dst_port": 22,
            "protocol": "TCP"
        },
        {
            "event_id": str(uuid.uuid4()),
            "timestamp": (base_time.replace(minute=5)).isoformat(),
            "source_type": "suricata",
            "event_type": "alert",
            "alert_signature": "ET EXPLOIT SSH Root Auth Fail",
            "severity": 3,
            "src_ip": "192.168.1.100",
            "dst_ip": "10.0.0.50",
            "src_port": 45680,
            "dst_port": 22,
            "protocol": "TCP"
        }
    ]

@pytest.fixture
def mock_suricata_alert_with_attck() -> Dict:
    """生成带 ATT&CK 映射信息的 Suricata 告警"""
    return {
        "event_id": str(uuid.uuid4()),
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "source_type": "suricata",
        "event_type": "alert",
        "alert_signature": "ET SCAN Potential SSH Scan",
        "severity": 2,
        "src_ip": "192.168.1.100",
        "dst_ip": "10.0.0.50",
        "mitre_tactic": "TA0004",
        "mitre_technique_id": "T1021",
        "mitre_technique_name": "Remote Services"
    }

# --- Neo4j Test Client Fixture ---
# 注意: 实际的 Neo4j fixture 将在 02-04 plan 的 service 层实现
# 此处仅提供类型注解和 mock
```

3. 创建以下测试骨架文件 (空测试函数，仅有 pass):

- `tests/test_chain/test_correlator.py`:
```python
"""告警关联器单元测试"""
import pytest

def test_correlator_ip_match():
    """测试 IP 相同告警关联"""
    pass

def test_correlator_attck_stage_validation():
    """测试 ATT&CK 攻击阶段逻辑验证"""
    pass
```

- `tests/test_chain/test_attck_mapper.py`:
```python
"""ATT&CK 映射器单元测试"""
import pytest

def test_attck_mapper_rule_lookup():
    """测试预置规则查找"""
    pass

def test_attck_mapper_llm_fallback():
    """测试 LLM 兜底推断"""
    pass
```

- `tests/test_chain/test_chain_reconstruction.py`:
```python
"""攻击链构建单元测试"""
import pytest

def test_chain_reconstruction_from_alerts():
    """测试从关联告警构建攻击链"""
    pass
```

- `tests/test_chain/test_chain_api.py`:
```python
"""攻击链 API 集成测试"""
import pytest

def test_get_chain_by_id():
    """测试获取攻击链详情"""
    pass

def test_list_chains():
    """测试列出攻击链"""
    pass
```
</action>
  <verify>
    <automated>ls -la tests/test_chain/ && ls tests/conftest.py && python -c "import tests.conftest; print('fixtures import ok')"</automated>
  </verify>
  <done>tests/test_chain/ 目录存在，包含 4 个测试文件，conftest.py 包含 mock alert fixtures</done>
</task>

<task type="auto">
  <name>Task 2: Create Suricata ATT&CK mapping rules file</name>
  <files>rules/attck_suricata.yaml</files>
  <action>
创建 `rules/attck_suricata.yaml` 文件，包含 Suricata 常见告警签名到 ATT&CK Technique 的映射规则。

格式:
```yaml
# Suricata 告警签名 -> ATT&CK 映射规则
# 格式: alert_signature: {tactic: TAxxxx, technique_id: Txxxx, technique_name: "名称"}

rules:
  # 扫描类
  "ET SCAN Potential SSH Scan":
    tactic: TA0043
    technique_id: T1046
    technique_name: "Network Service Discovery"

  "ET SCAN Web Scan":
    tactic: TA0043
    technique_id: T1046
    technique_name: "Network Service Discovery"

  "ET SCAN Port Scan":
    tactic: TA0043
    technique_id: T1046
    technique_name: "Network Service Discovery"

  # 暴力破解类
  "ET EXPLOIT SSH Root Auth Fail":
    tactic: TA0006
    technique_id: T1021
    technique_name: "Remote Services"

  "ET EXPLOIT FTP Login Fail":
    tactic: TA0006
    technique_id: T1021
    technique_name: "Remote Services"

  "ET POLICY HTTP Basic Auth":
    tactic: TA0006
    technique_id: T1078
    technique_name: "Valid Accounts"

  # 利用类
  "ET EXPLOIT Buffer Overflow":
    tactic: TA0004
    technique_id: T1068
    technique_name: "Exploitation for Privilege Escalation"

  "ET EXPLOIT SQL Injection Attempt":
    tactic: TA0001
    technique_id: T1190
    technique_name: "Exploit Public-Facing Application"

  "ET EXPLOIT XSS Attempt":
    tactic: TA0001
    technique_id: T1190
    technique_name: "Exploit Public-Facing Application"

  # 命令与控制类
  "ET MALWARE Suspicious HTTP User-Agent":
    tactic: TA0011
    technique_id: T1071
    technique_name: "Application Layer Protocol"

  "ET MALWARE Known Malicious HTTP Traffic":
    tactic: TA0011
    technique_id: T1071
    technique_name: "Application Layer Protocol"

  # 数据泄露类
  "ET EXFILTRATION Large Outbound Data":
    tactic: TA0010
    technique_id: T1041
    technique_name: "Exfiltration Over C2 Channel"
```

ATT&CK Tactic ID 参考:
- TA0001: Initial Access (初始访问)
- TA0002: Execution (执行)
- TA0003: Persistence (持久化)
- TA0004: Privilege Escalation (权限提升)
- TA0005: Defense Evasion (防御规避)
- TA0006: Credential Access (凭证访问)
- TA0007: Discovery (发现)
- TA0008: Lateral Movement (横向移动)
- TA0009: Collection (收集)
- TA0010: Exfiltration (泄露)
- TA0011: Command and Control (命令与控制)
- TA0043: Reconnaissance (侦察)
</action>
  <verify>
    <automated>python -c "import yaml; rules = yaml.safe_load(open('rules/attck_suricata.yaml')); print(f'Loaded {len(rules[\"rules\"])} rules')"</automated>
  </verify>
  <done>rules/attck_suricata.yaml 存在且包含至少 10 条 Suricata 告警到 ATT&CK 的映射规则</done>
</task>

</tasks>

<verification>
- `ls tests/test_chain/` 显示 4 个测试文件
- `python -c "import yaml; yaml.safe_load(open('rules/attck_suricata.yaml'))"` 无报错
- `pytest tests/test_chain/test_correlator.py --collect-only` 收集到测试函数
</verification>

<success_criteria>
- tests/test_chain/ 目录包含 4 个测试骨架文件
- tests/conftest.py 包含 mock alert fixtures
- rules/attck_suricata.yaml 包含至少 10 条映射规则
- pytest --collect-only 可以发现测试
</success_criteria>

<output>
After completion, create `.planning/phases/02-attack-chain-correlation/02-01-SUMMARY.md`
</output>
