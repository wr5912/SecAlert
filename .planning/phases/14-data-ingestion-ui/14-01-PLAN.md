---
phase: 14
plan: 01
type: execute
wave: 1
depends_on: ["14-00"]
files_modified:
  - "src/api/ingestion_endpoints.py"
  - "src/api/ingestion_models.py"
autonomous: true
requirements:
  - DI-01
  - DI-02
  - DI-03
  - DI-04
must_haves:
  truths:
    - "模板可以创建并持久化"
    - "模板可以按 ID 查询"
    - "模板可以更新"
    - "模板可以删除"
    - "模板可以列出所有"
  artifacts:
    - path: "src/api/ingestion_endpoints.py"
      provides: "数据接入 API 路由"
      exports: ["GET /api/ingestion/templates", "POST /api/ingestion/templates", "PUT /api/ingestion/templates/{id}", "DELETE /api/ingestion/templates/{id}"]
    - path: "src/api/ingestion_models.py"
      provides: "数据接入 Pydantic 模型"
      exports: ["DataSourceTemplate", "TemplateCreate", "TemplateUpdate"]
---

<objective>
实现数据源模板的 CRUD API，为前端向导提供后端支持。

目的：提供模板创建、读取、更新、删除、列表接口
输出：src/api/ingestion_endpoints.py, src/api/ingestion_models.py
</objective>

<context>
@src/api/chain_endpoints.py (API 模式参考)
@src/api/main.py (路由注册模式)
@14-RESEARCH.md (FastAPI Router Pattern)
@14-UI-SPEC.md (数据模型设计)

# 接口设计

## 请求/响应类型

```python
# src/api/ingestion_models.py
class ConnectionConfig(BaseModel):
    host: str
    port: int
    username: str
    password: str
    protocol: str  # ssh, snmp, http, jdbc

class DataSourceTemplate(BaseModel):
    id: str
    name: str
    device_type: str  # firewall, ids, vpn, switch, router, waf, other
    connection: ConnectionConfig
    log_format: str  # CEF, Syslog, JSON, Custom
    custom_regex: Optional[str] = None

class TemplateCreate(BaseModel):
    name: str
    device_type: str
    connection: ConnectionConfig
    log_format: str
    custom_regex: Optional[str] = None

class TemplateUpdate(BaseModel):
    name: Optional[str] = None
    device_type: Optional[str] = None
    connection: Optional[ConnectionConfig] = None
    log_format: Optional[str] = None
    custom_regex: Optional[str] = None
```

## API 端点

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/ingestion/templates | 列出所有模板 |
| POST | /api/ingestion/templates | 创建新模板 |
| GET | /api/ingestion/templates/{id} | 获取单个模板 |
| PUT | /api/ingestion/templates/{id} | 更新模板 |
| DELETE | /api/ingestion/templates/{id} | 删除模板 |
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: 创建 Pydantic 模型</name>
  <files>src/api/ingestion_models.py</files>
  <read_first>src/api/chain_endpoints.py</read_first>
  <action>
    创建 `src/api/ingestion_models.py`，包含数据接入相关 Pydantic 模型：

    ```python
    """
    数据接入 Pydantic 模型

    定义数据源模板的数据结构和验证规则
    """

    from pydantic import BaseModel, Field
    from typing import Optional, List
    from enum import Enum

    class DeviceType(str, Enum):
        """支持的设备类型"""
        FIREWALL = "firewall"
        IDS = "ids"
        VPN = "vpn"
        SWITCH = "switch"
        ROUTER = "router"
        WAF = "waf"
        OTHER = "other"

    class LogFormat(str, Enum):
        """支持的日志格式"""
        CEF = "CEF"
        SYSLOG = "Syslog"
        JSON = "JSON"
        CUSTOM = "Custom"

    class ConnectionConfig(BaseModel):
        """连接配置"""
        host: str = Field(..., description="主机地址或 IP")
        port: int = Field(..., ge=1, le=65535, description="端口号")
        username: str = Field(..., description="用户名")
        password: str = Field(..., description="密码或密钥")
        protocol: str = Field(..., description="连接协议: ssh, snmp, http, jdbc")

    class DataSourceTemplate(BaseModel):
        """数据源模板"""
        id: str = Field(..., description="模板唯一 ID")
        name: str = Field(..., min_length=1, max_length=100, description="模板名称")
        device_type: str = Field(..., description="设备类型")
        connection: ConnectionConfig = Field(..., description="连接参数")
        log_format: str = Field(..., description="日志格式")
        custom_regex: Optional[str] = Field(None, description="自定义正则 (Custom 格式时必填)")

    class TemplateCreate(BaseModel):
        """创建模板请求"""
        name: str = Field(..., min_length=1, max_length=100, description="模板名称")
        device_type: str = Field(..., description="设备类型")
        connection: ConnectionConfig = Field(..., description="连接参数")
        log_format: str = Field(..., description="日志格式")
        custom_regex: Optional[str] = Field(None, description="自定义正则")

    class TemplateUpdate(BaseModel):
        """更新模板请求"""
        name: Optional[str] = Field(None, min_length=1, max_length=100)
        device_type: Optional[str] = None
        connection: Optional[ConnectionConfig] = None
        log_format: Optional[str] = None
        custom_regex: Optional[str] = None

    class TemplateListResponse(BaseModel):
        """模板列表响应"""
        templates: List[DataSourceTemplate]
        total: int

    class TemplateResponse(BaseModel):
        """单个模板响应"""
        id: str
        name: str
        device_type: str
        connection: ConnectionConfig
        log_format: str
        custom_regex: Optional[str]

    class DeleteResponse(BaseModel):
        """删除响应"""
        success: bool
        message: str
    ```

    参考 src/api/chain_endpoints.py 的 Pydantic 模型使用模式。
  </action>
  <verify>
    <automated>python -c "from src.api.ingestion_models import DataSourceTemplate, TemplateCreate, TemplateUpdate, ConnectionConfig; print('models OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/api/ingestion_models.py 包含 ConnectionConfig, DataSourceTemplate, TemplateCreate, TemplateUpdate, TemplateListResponse 模型
    - ConnectionConfig 包含 host, port, username, password, protocol 字段
    - DataSourceTemplate 包含 id, name, device_type, connection, log_format, custom_regex 字段
    - Pydantic 模型可正确导入
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1.2: 创建 API 端点</name>
  <files>src/api/ingestion_endpoints.py</files>
  <read_first>src/api/chain_endpoints.py</read_first>
  <action>
    创建 `src/api/ingestion_endpoints.py`，包含模板 CRUD API：

    ```python
    """
    数据接入 API 接口

    FastAPI endpoints for data source template management
    DI-01~DI-04: 模板 CRUD 操作
    """

    import uuid
    from fastapi import APIRouter, HTTPException, Query
    from typing import Optional, List

    from .ingestion_models import (
        DataSourceTemplate,
        TemplateCreate,
        TemplateUpdate,
        TemplateListResponse,
        DeleteResponse,
        ConnectionConfig
    )

    router = APIRouter(prefix="/api/ingestion", tags=["ingestion"])

    # 内存存储 (生产环境应使用数据库)
    _templates: dict[str, DataSourceTemplate] = {}

    def _generate_id() -> str:
        """生成唯一 ID"""
        return f"template-{uuid.uuid4().hex[:8]}"

    @router.get("/templates", response_model=TemplateListResponse)
    async def list_templates() -> TemplateListResponse:
        """
        DI-04: 列出所有数据源模板

        Returns:
            templates: 模板列表
            total: 总数
        """
        templates = list(_templates.values())
        return TemplateListResponse(templates=templates, total=len(templates))

    @router.post("/templates", response_model=DataSourceTemplate, status_code=201)
    async def create_template(template: TemplateCreate) -> DataSourceTemplate:
        """
        DI-01: 创建数据源模板

        - **name**: 模板名称
        - **device_type**: 设备类型 (firewall/ids/vpn/switch/router/waf/other)
        - **connection**: 连接参数
        - **log_format**: 日志格式 (CEF/Syslog/JSON/Custom)

        Returns:
            创建的模板对象
        """
        template_id = _generate_id()
        new_template = DataSourceTemplate(
            id=template_id,
            name=template.name,
            device_type=template.device_type,
            connection=template.connection,
            log_format=template.log_format,
            custom_regex=template.custom_regex
        )
        _templates[template_id] = new_template
        return new_template

    @router.get("/templates/{template_id}", response_model=DataSourceTemplate)
    async def get_template(template_id: str) -> DataSourceTemplate:
        """
        获取单个数据源模板

        - **template_id**: 模板 ID

        Returns:
            模板对象

        Raises:
            404: 模板不存在
        """
        if template_id not in _templates:
            raise HTTPException(status_code=404, detail=f"Template {template_id} not found")
        return _templates[template_id]

    @router.put("/templates/{template_id}", response_model=DataSourceTemplate)
    async def update_template(
        template_id: str,
        template_update: TemplateUpdate
    ) -> DataSourceTemplate:
        """
        DI-02: 更新数据源模板

        - **template_id**: 模板 ID
        - **template_update**: 更新内容 (部分更新)

        Returns:
            更新后的模板对象

        Raises:
            404: 模板不存在
        """
        if template_id not in _templates:
            raise HTTPException(status_code=404, detail=f"Template {template_id} not found")

        existing = _templates[template_id]

        # 部分更新
        update_data = template_update.model_dump(exclude_unset=True)
        updated = existing.model_copy(update=update_data)

        _templates[template_id] = updated
        return updated

    @router.delete("/templates/{template_id}", response_model=DeleteResponse)
    async def delete_template(template_id: str) -> DeleteResponse:
        """
        DI-03: 删除数据源模板

        - **template_id**: 模板 ID

        Returns:
            删除结果

        Raises:
            404: 模板不存在
        """
        if template_id not in _templates:
            raise HTTPException(status_code=404, detail=f"Template {template_id} not found")

        del _templates[template_id]
        return DeleteResponse(success=True, message=f"Template {template_id} deleted")
    ```

    参考 src/api/chain_endpoints.py 的 API 模式，使用内存存储作为临时方案。
  </action>
  <verify>
    <automated>python -c "from src.api.ingestion_endpoints import router; print('router OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/api/ingestion_endpoints.py 包含 router
    - router 包含 GET /api/ingestion/templates (DI-04)
    - router 包含 POST /api/ingestion/templates (DI-01)
    - router 包含 PUT /api/ingestion/templates/{template_id} (DI-02)
    - router 包含 DELETE /api/ingestion/templates/{template_id} (DI-03)
    - router 包含 GET /api/ingestion/templates/{template_id}
    - FastAPI router 可正确导入
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 1.3: 注册路由到 main.py</name>
  <files>src/api/main.py</files>
  <read_first>src/api/main.py</read_first>
  <action>
    修改 `src/api/main.py`，注册 ingestion 路由：

    1. 在文件顶部添加导入：
    ```python
    from src.api.ingestion_endpoints import router as ingestion_router
    ```

    2. 在路由注册部分添加：
    ```python
    app.include_router(ingestion_router)
    ```

    保持现有路由顺序不变。
  </action>
  <verify>
    <automated>python -c "from src.api.main import app; print([r.path for r in app.routes if hasattr(r, 'path')])" | grep -o 'ingestion'</automated>
  </verify>
  <acceptance_criteria>
    - src/api/main.py 导入 ingestion_router
    - src/api/main.py 调用 app.include_router(ingestion_router)
    - 启动 FastAPI 时不报错
  </acceptance_criteria>
</task>

</tasks>

<verification>
运行 `pytest tests/test_ingestion/test_templates.py -x` 验证 API 功能。
</verification>

<success_criteria>
- POST /api/ingestion/templates 返回 201 并包含 id
- GET /api/ingestion/templates 返回模板列表
- PUT /api/ingestion/templates/{id} 返回更新后的模板
- DELETE /api/ingestion/templates/{id} 返回 success
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/14-data-ingestion-ui/{phase}-01-SUMMARY.md`
</output>
