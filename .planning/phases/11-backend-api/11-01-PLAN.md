---
phase: 11-backend-api
plan: '01'
type: execute
wave: '1'
depends_on: []
files_created:
  - backend/api/analysis.py
  - backend/models/analysis.py
autonomous: true
requirements:
  - "故事线列表 API - 前端 AlertCenterPage 需要显示告警故事线"
  - "攻击图 API - 前端 AttackGraphPage 需要攻击链路数据"
  - "时间线 API - 前端 TimelinePage 需要事件数据"
  - "资产上下文 API - 前端 ContextPanel 需要资产详情"
  - "威胁狩猎 API - 前端 HuntingPage 需要查询结果"
must_haves:
  truths:
    - "前端页面能正确调用后端 API 获取数据"
    - "API 返回符合前端 types/analysis.ts 类型定义"
    - "支持筛选条件查询"
  artifacts:
    - path: "backend/api/analysis.py"
      provides: "分析模块 API 端点实现"
    - path: "backend/models/analysis.py"
      provides: "分析模块 Pydantic 模型"
---

<objective>
实现分析模块后端 API，为前端智能分析工作台提供数据支持。

目的：联调前端 Phase 9 实现的故事线聚合、攻击链路图、多轨道时间线、威胁狩猎、资产上下文功能。
输出：5 个 REST API 端点返回符合前端类型定义的 JSON 数据
</objective>

<context>
@frontend/src/api/analysisEndpoints.ts
@frontend/src/types/analysis.ts
@frontend/src/pages/AlertCenterPage.tsx
@frontend/src/pages/AttackGraphPage.tsx
@frontend/src/pages/TimelinePage.tsx
@frontend/src/pages/HuntingPage.tsx
</context>

<tasks>

<task type="execute">
<name>Task 1: 创建分析模块 Pydantic 模型</name>
<files>backend/models/analysis.py</files>
<action>
创建 backend/models/analysis.py，包含：
1. Severity 枚举 (critical/high/medium/low)
2. AttackNodeType 枚举 (host/user/ip/process)
3. TimelineLayer 枚举 (network/endpoint/identity/application)
4. Storyline, Alert, AttackNode, AttackEdge, TimelineEvent, AssetContext, ThreatIntelMatch, HuntingQuery, HuntingResult 等模型
5. 所有字段与 frontend/src/types/analysis.ts 对齐
</action>
<verify>
python -c "from backend.models.analysis import Storyline, TimelineEvent; print('OK')"
</verify>
<done>backend/models/analysis.py 包含所有分析模块类型定义
</done>
</task>

<task type="execute">
<name>Task 2: 实现获取故事线列表 API</name>
<files>backend/api/analysis.py</files>
<action>
实现 GET /api/analysis/storylines 端点：
1. Query 参数：time_start, time_end, severities (逗号分隔), asset_types, mitre_tactics, confidence_min, confidence_max, sources
2. 从数据库/Neo4j 查询故事线数据
3. 按置信度降序返回
4. 构造符合 frontend/src/types/analysis.ts 的 Storyline[] JSON
</action>
<verify>
curl -s "http://localhost:8000/api/analysis/storylines" | python -c "import sys,json; d=json.load(sys.stdin); print(f'Storylines: {len(d)}')"
</verify>
<done>GET /api/analysis/storylines 返回故事线列表
</done>
</task>

<task type="execute">
<name>Task 3: 实现攻击图数据 API</name>
<files>backend/api/analysis.py</files>
<action>
实现 GET /api/analysis/graph/{storyId} 端点：
1. Path 参数：storyId (故事线 ID)
2. Query 参数：time_start, time_end (可选时间范围)
3. 从 Neo4j 查询攻击链路节点和边
4. 返回 { nodes: AttackNode[], edges: AttackEdge[] }
</action>
<verify>
curl -s "http://localhost:8000/api/analysis/graph/story-001" | python -c "import sys,json; d=json.load(sys.stdin); print(f'Nodes: {len(d.get(\"nodes\",[]))}, Edges: {len(d.get(\"edges\",[]))}')"
</verify>
<done>GET /api/analysis/graph/{storyId} 返回攻击链路图数据
</done>
</task>

<task type="execute">
<name>Task 4: 实现时间线事件 API</name>
<files>backend/api/analysis.py</files>
<action>
实现 GET /api/analysis/timeline 端点：
1. Query 参数：time_start, time_end, sources (可选过滤器)
2. 从 Elasticsearch/ClickHouse 查询事件
3. 返回 TimelineEvent[] 列表
4. 支持 network/endpoint/identity/application 四层分类
</action>
<verify>
curl -s "http://localhost:8000/api/analysis/timeline?time_start=2026-03-25T00:00:00Z&time_end=2026-03-26T00:00:00Z" | python -c "import sys,json; d=json.load(sys.stdin); print(f'Events: {len(d)}')"
</verify>
<done>GET /api/analysis/timeline 返回时间线事件列表
</done>
</task>

<task type="execute">
<name>Task 5: 实现资产上下文 API</name>
<files>backend/api/analysis.py</files>
<action>
实现 GET /api/analysis/assets/{assetId} 端点：
1. Path 参数：assetId (资产 ID)
2. 查询资产基础信息、关联告警、威胁情报匹配
3. 返回 AssetContext JSON
</action>
<verify>
curl -s "http://localhost:8000/api/analysis/assets/asset-001" | python -c "import sys,json; d=json.load(sys.stdin); print(f'Asset: {d.get(\"assetId\")}, Type: {d.get(\"assetType\")}')"
</verify>
<done>GET /api/analysis/assets/{assetId} 返回资产上下文数据
</done>
</task>

<task type="execute">
<name>Task 6: 实现威胁狩猎查询 API</name>
<files>backend/api/analysis.py</files>
<action>
实现 POST /api/analysis/hunting 端点：
1. Request Body：HuntingQuery (filters, logic, timeRange)
2. 解析三元组查询条件 (field, operator, value)
3. 构建 Elasticsearch/ClickHouse 查询
4. 返回 HuntingResult { total, events, visualization }
</action>
<verify>
curl -s -X POST "http://localhost:8000/api/analysis/hunting" -H "Content-Type: application/json" -d '{"filters":[{"field":"src_ip","operator":"=","value":"192.168.1.1"}],"logic":"AND"}' | python -c "import sys,json; d=json.load(sys.stdin); print(f'Total: {d.get(\"total\")}')"
</verify>
<done>POST /api/analysis/hunting 返回狩猎查询结果
</done>
</task>

</tasks>

<verification>
1. 启动后端服务: cd backend && uvicorn main:app --reload
2. 测试所有 5 个 API 端点
3. 验证返回数据结构与 frontend/src/types/analysis.ts 一致
</verification>

<success_criteria>
- GET /api/analysis/storylines 返回 Storyline[]
- GET /api/analysis/graph/{id} 返回 { nodes: AttackNode[], edges: AttackEdge[] }
- GET /api/analysis/timeline 返回 TimelineEvent[]
- GET /api/analysis/assets/{id} 返回 AssetContext
- POST /api/analysis/hunting 返回 HuntingResult
- 所有 API 支持必要的筛选参数
</success_criteria>

<output>
After completion, create `.planning/phases/11-backend-api/11-01-SUMMARY.md`
</output>
