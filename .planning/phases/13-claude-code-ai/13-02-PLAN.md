---
phase: 13-claude-code-ai
plan: "02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - src/api/agent_endpoints.py
  - src/api/main.py
  - frontend/src/api/chat.ts
autonomous: true
requirements:
  - AG-02
  - AG-04
must_haves:
  truths:
    - "WebSocket 端点 /ws/chat/{user_id} 可接受连接"
    - "流式响应通过 WebSocket 实时推送"
    - "Agent 客户端与 WebSocket 正确连接"
    - "DeepSeek API Key 可通过环境变量配置"
    - "Fallback 机制在 SDK 失败时触发"
    - "前端 streamChat 支持 WebSocket 协议"
  artifacts:
    - path: "src/api/agent_endpoints.py"
      provides: "Agent WebSocket 端点"
      min_lines: 100
      exports: ["router"]
    - path: "src/api/main.py"
      provides: "agent_endpoints 路由注册"
      contains: "agent_endpoints"
    - path: "frontend/src/api/chat.ts"
      provides: "WebSocket 流式对话支持"
      contains: "WebSocket"
---

<objective>
实现 WebSocket 流式对话端点和 Fallback 机制，以及前端 WebSocket 集成。

在 Plan 01 基础上，创建 WebSocket 端点处理流式对话，集成 Agent 客户端，并添加 Fallback 到 DeepSeek API 的机制。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/api/main.py
@src/api/chat_endpoints.py
@src/agent/client.py
@src/agent/config.py
@frontend/src/api/chat.ts

## 关键接口

### WebSocket 端点模式 (来自 RESEARCH.md)
```python
@app.websocket("/ws/chat/{user_id}")
async def agent_chat(websocket: WebSocket, user_id: str):
    await websocket.accept()
    async with ClaudeSDKClient(options=options) as client:
        while True:
            message = await websocket.receive_text()
            await client.query(message)
            async for msg in client.receive_response():
                await websocket.send_json({"type": msg.type, "content": msg.content})
```

### Fallback 模式 (来自 RESEARCH.md)
```python
except (CLINotFoundError, CLIConnectionError) as e:
    # Fallback 到 DeepSeek API
    return await call_deepseek_api(system_prompt, user_message)
```

### 前端 WebSocket 调用 (需新增)
```typescript
// 使用 WebSocket 获取流式响应
const ws = new WebSocket(`ws://localhost:8000/ws/chat/${userId}`);
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  if (data.type === 'text') onChunk(data.content);
  else if (data.type === 'done') onDone();
};
ws.send(JSON.stringify({ message, context }));
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 WebSocket Agent 端点</name>
  <files>src/api/agent_endpoints.py</files>
  <read_first>
    - src/api/main.py (路由注册模式)
    - src/api/chat_endpoints.py (参考 call_deepseek_api 函数)
    - src/agent/client.py (AgentClient 使用方式)
  </read_first>
  <acceptance_criteria>
    - `src/api/agent_endpoints.py` exports `router` (APIRouter)
    - WebSocket endpoint at `/ws/chat/{user_id}`
    - Endpoint accepts JSON messages with "message" and "context" fields
    - Sends JSON responses with "type" and "content" fields
    - Uses AgentClient from src.agent for streaming
    - Handles WebSocketDisconnect gracefully
    - Falls back to DeepSeek API when SDK fails (fallback_required signal)
    - Creates per-user workspace directory
  </acceptance_criteria>
  <action>
创建 `src/api/agent_endpoints.py`:

```python
"""
Agent WebSocket 端点

提供:
- WebSocket /ws/chat/{user_id} - Agent 流式对话
"""

import os
import json
import logging
from typing import Optional
from fastapi import APIRouter, WebSocket, WebSocketDisconnect
from pydantic import BaseModel

from src.agent.client import create_agent_client
from src.agent.config import SYSTEM_PROMPT

logger = logging.getLogger(__name__)
router = APIRouter(prefix="/ws", tags=["agent"])

# 请求模型
class AgentMessage(BaseModel):
    message: str
    context: Optional[dict] = None


async def call_deepseek_fallback(
    system_prompt: str,
    user_message: str
) -> str:
    """Fallback 到 DeepSeek API

    当 Claude SDK 不可用时使用
    """
    import httpx

    api_key = os.getenv("DEEPSEEK_API_KEY")
    base_url = os.getenv("DEEPSEEK_BASE_URL", "https://api.deepseek.com")
    model = os.getenv("DEEPSEEK_MODEL", "deepseek-chat")

    if not api_key:
        return "错误: 未配置 DeepSeek API Key"

    try:
        async with httpx.AsyncClient(timeout=60.0) as client:
            response = await client.post(
                f"{base_url}/v1/chat/completions",
                headers={
                    "Authorization": f"Bearer {api_key}",
                    "Content-Type": "application/json"
                },
                json={
                    "model": model,
                    "messages": [
                        {"role": "system", "content": system_prompt},
                        {"role": "user", "content": user_message}
                    ],
                    "stream": False
                }
            )
            response.raise_for_status()
            data = response.json()
            return data.get("choices", [{}])[0].get("message", {}).get("content", "无响应")
    except httpx.HTTPError as e:
        return f"DeepSeek API 调用失败: {str(e)}"


@router.websocket("/chat/{user_id}")
async def agent_chat(websocket: WebSocket, user_id: str):
    """Agent 流式对话 WebSocket 端点

    连接协议:
    1. 客户端发送 JSON: {"message": "...", "context": {...}}
    2. 服务端发送 JSON: {"type": "text"|"tool_use"|"done"|"error", "content": "..."}
    3. 断开连接时清理资源
    """
    await websocket.accept()
    logger.info(f"用户 {user_id} 连接到 Agent WebSocket")

    # 创建用户沙箱目录
    workspace = f"./workspaces/{user_id}"
    os.makedirs(workspace, exist_ok=True)

    client = None
    try:
        # 创建 Agent 客户端
        client = await create_agent_client(user_id)

        while True:
            # 接收消息
            try:
                data = await websocket.receive_text()
                msg_dict = json.loads(data)
                user_message = msg_dict.get("message", "")
                context = msg_dict.get("context", {})

                if not user_message:
                    continue

            except json.JSONDecodeError:
                await websocket.send_json({
                    "type": "error",
                    "content": "无效的 JSON 格式"
                })
                continue

            # 构建系统提示 (包含上下文)
            system_prompt = SYSTEM_PROMPT
            if context:
                ctx_parts = []
                if context.get("type"):
                    ctx_parts.append(f"页面类型: {context['type']}")
                if context.get("chain_id"):
                    ctx_parts.append(f"攻击链ID: {context['chain_id']}")
                if context.get("asset_ip"):
                    ctx_parts.append(f"目标资产: {context['asset_ip']}")
                if ctx_parts:
                    system_prompt = f"{SYSTEM_PROMPT}\n\n当前上下文:\n" + "\n".join(f"- {c}" for c in ctx_parts)

            # 使用 Agent 查询
            fallback_triggered = False
            async for response in client.query(user_message):
                msg_type = response.get("type", "")
                content = response.get("content", "")

                # 检查是否需要 fallback
                if msg_type == "fallback_required":
                    fallback_triggered = True
                    break

                # 发送响应
                await websocket.send_json({
                    "type": msg_type,
                    "content": content
                })

            # 如果需要 fallback
            if fallback_triggered:
                logger.info(f"用户 {user_id}: SDK 失败，触发 DeepSeek Fallback")
                fallback_response = await call_deepseek_fallback(
                    system_prompt,
                    user_message
                )

                # 流式发送 fallback 响应
                for i in range(0, len(fallback_response), 10):
                    chunk = fallback_response[i:i+10]
                    await websocket.send_json({
                        "type": "text",
                        "content": chunk
                    })

            # 发送完成信号
            await websocket.send_json({"type": "done", "content": ""})

    except WebSocketDisconnect:
        logger.info(f"用户 {user_id} 断开连接")
    except Exception as e:
        logger.exception(f"WebSocket 异常: {e}")
        try:
            await websocket.send_json({
                "type": "error",
                "content": f"服务端异常: {str(e)}"
            })
        except:
            pass
    finally:
        # 清理资源
        if client:
            await client.close()
        logger.info(f"用户 {user_id} 会话结束")
```

参考:
- `src/api/chat_endpoints.py` 中的 `call_deepseek_api` 函数实现
- `src/agent/client.py` 中的 `AgentClient.query` 方法
</action>
  <verify>
    <automated>python -c "from src.api.agent_endpoints import router; print(f'WS endpoint registered: {router.prefix}')" && grep -l "websocket" src/api/agent_endpoints.py</automated>
  </verify>
  <done>WebSocket 端点已创建，/ws/chat/{user_id} 可接受连接并返回流式响应</done>
</task>

<task type="auto">
  <name>Task 2: 注册 Agent 路由到 FastAPI</name>
  <files>src/api/main.py</files>
  <read_first>
    - src/api/main.py
  </read_first>
  <acceptance_criteria>
    - `src/api/main.py` imports `agent_endpoints` module
    - `src/api/main.py` includes `agent_router` with prefix `/ws`
    - 导入语句在 chat_router 之后
    - app.include_router 调用在 chat_router 之后
  </acceptance_criteria>
  <action>
修改 `src/api/main.py`:

1. 添加导入 (在 chat_router 导入之后):
```python
from src.api.agent_endpoints import router as agent_router
```

2. 添加路由注册 (在 chat_router 之后):
```python
app.include_router(agent_router)
```

其他部分保持不变。
  </action>
  <verify>
    <automated>python -c "from src.api.main import app; routes = [r.path for r in app.routes]; assert any('/ws/chat' in str(r) for r in routes), f'WebSocket route not found in {routes}'; print('main_routes_ok')"</automated>
  </verify>
  <done>
Agent WebSocket 路由已注册到 FastAPI 应用，/ws/chat/{user_id} 端点可用
  </done>
</task>

<task type="auto">
  <name>Task 3: 添加前端 WebSocket 支持</name>
  <files>frontend/src/api/chat.ts</files>
  <read_first>
    - frontend/src/api/chat.ts
  </read_first>
  <acceptance_criteria>
    - `frontend/src/api/chat.ts` exports `streamChatWebSocket` function
    - Function accepts: message, sessionId, context, onChunk, onDone, onError
    - Function returns WebSocket connection that can be closed
    - Uses protocol: ws://host/ws/chat/{sessionId}
    - Sends JSON: {"message": "...", "context": {...}}
    - Handles JSON responses: {"type": "text"|"done"|"error", "content": "..."}
    - Existing `streamChat` (SSE) function remains unchanged
  </acceptance_criteria>
  <action>
修改 `frontend/src/api/chat.ts`:

在文件末尾添加 WebSocket 流式对话函数:

```typescript
/**
 * WebSocket 流式对话
 *
 * 使用 WebSocket 获取 Agent 流式响应
 *
 * @param message - 用户消息
 * @param sessionId - 会话ID (作为 user_id)
 * @param context - 当前上下文
 * @param onChunk - 每个 chunk 的回调
 * @param onDone - 完成时的回调
 * @param onError - 错误时的回调
 * @returns WebSocket 连接，可用于关闭
 */
export function streamChatWebSocket(
  message: string,
  sessionId: string,
  context: ChatContext,
  onChunk: (token: string) => void,
  onDone: () => void,
  onError: (error: Error) => void
): WebSocket {
  const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
  const host = window.location.host;
  const wsUrl = `${protocol}//${host}/ws/chat/${sessionId}`;

  const ws = new WebSocket(wsUrl);

  ws.onopen = () => {
    // 发送消息
    ws.send(JSON.stringify({
      message,
      context
    }));
  };

  ws.onmessage = (event) => {
    try {
      const data = JSON.parse(event.data);
      const msgType = data.type;
      const content = data.content || '';

      if (msgType === 'text') {
        onChunk(content);
      } else if (msgType === 'tool_use') {
        // 工具执行中，可选显示
        onChunk(content);
      } else if (msgType === 'done') {
        onDone();
      } else if (msgType === 'error') {
        onError(new Error(content || 'Unknown error'));
      }
    } catch (e) {
      // 忽略解析错误
    }
  };

  ws.onerror = (event) => {
    onError(new Error('WebSocket connection error'));
  };

  ws.onclose = () => {
    // 连接关闭时调用 onDone
    onDone();
  };

  return ws;
}
```

注意: 保持现有的 `streamChat` (SSE) 函数不变，因为 WebSocket 版本可能需要配置开关来切换使用。
</action>
  <verify>
    <automated>grep -c "streamChatWebSocket" frontend/src/api/chat.ts && grep -c "ws://" frontend/src/api/chat.ts</automated>
  </verify>
  <done>
前端 WebSocket 流式对话函数已添加，streamChatWebSocket 可用于建立 WebSocket 连接
  </done>
</task>

</tasks>

<verification>
## 整体验证

1. WebSocket 端点验证:
```bash
# 启动服务器后测试
python -c "
import asyncio
from src.api.agent_endpoints import router
print('Agent endpoints imported OK')
"
```

2. 路由注册验证:
```bash
python -c "
from src.api.main import app
routes = [r.path for r in app.routes]
assert any('/ws/chat' in str(r) for r in routes), 'WebSocket route not registered'
print('Routes OK:', [r for r in routes if 'ws' in str(r) or 'chat' in str(r)])
"
```

3. 前端代码验证:
```bash
grep -E "streamChatWebSocket|WebSocket.*ws://" frontend/src/api/chat.ts
```
</verification>

<success_criteria>
1. WebSocket 端点 `/ws/chat/{user_id}` 可接受连接
2. Agent 客户端正确处理流式响应
3. Fallback 机制在 SDK 失败时触发 DeepSeek API
4. 前端 `streamChatWebSocket` 函数可建立 WebSocket 连接
5. 路由正确注册到 FastAPI 应用
</success_criteria>

<output>
完成后创建 `.planning/phases/13-claude-code-ai/13-02-SUMMARY.md`
</output>
