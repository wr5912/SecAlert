---
phase: 13-claude-code-ai
plan: "03"
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
files_modified:
  - tests/test_agent/__init__.py
  - tests/test_agent/conftest.py
  - tests/test_agent/test_tools.py
  - tests/test_agent/test_client.py
  - tests/test_agent/test_websocket.py
  - tests/test_agent/test_fallback.py
autonomous: true
requirements:
  - AG-05
must_haves:
  truths:
    - "测试框架可运行，pytest-asyncio 配置正确"
    - "工具注册测试通过"
    - "Agent 客户端测试通过"
    - "WebSocket 端点测试通过"
    - "Fallback 机制测试通过"
  artifacts:
    - path: "tests/test_agent/__init__.py"
      provides: "测试包初始化"
    - path: "tests/test_agent/conftest.py"
      provides: "共享 fixtures"
    - path: "tests/test_agent/test_tools.py"
      provides: "工具注册测试"
    - path: "tests/test_agent/test_client.py"
      provides: "Agent 客户端测试"
    - path: "tests/test_agent/test_websocket.py"
      provides: "WebSocket 端点测试"
    - path: "tests/test_agent/test_fallback.py"
      provides: "Fallback 机制测试"
---

<objective>
创建 Agent 模块的集成测试和 E2E 测试。

验证 SDK 安装、工具注册、客户端功能、WebSocket 端点和 Fallback 机制。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@pyproject.toml
@src/agent/tools.py
@src/agent/client.py
@src/api/agent_endpoints.py

## 测试框架 (来自 13-RESEARCH.md)

### 测试命令
```bash
pytest tests/test_agent/ -x -v
```

### 测试结构
| 文件 | 测试内容 |
|------|----------|
| test_tools.py | 工具注册测试 |
| test_client.py | Agent 客户端测试 |
| test_websocket.py | WebSocket 端点测试 |
| test_fallback.py | Fallback 机制测试 |

### 验证架构 (来自 13-RESEARCH.md)
| Req ID | Behavior | Test Type |
|--------|----------|-----------|
| AG-01 | SDK 安装成功 | smoke |
| AG-02 | WebSocket 连接成功 | integration |
| AG-02 | 流式响应正常 | integration |
| AG-03 | 工具注册成功 | unit |
| AG-03 | 工具调用返回结果 | integration |
| AG-04 | DeepSeek API 配置正确 | smoke |
| AG-05 | Fallback 机制工作 | integration |
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建测试目录和基础配置</name>
  <files>
    - tests/test_agent/__init__.py
    - tests/test_agent/conftest.py
  </files>
  <read_first>
    - pyproject.toml
    - tests/ (查看现有测试结构)
  </read_first>
  <acceptance_criteria>
    - `tests/test_agent/__init__.py` exists (empty or with __all__)
    - `tests/test_agent/conftest.py` exists with pytest-asyncio 配置
    - pytest can discover tests in tests/test_agent/
    - conftest.py includes @pytest.fixture for event_loop
    - conftest.py includes @pytest.fixture for api_base_url (http://localhost:8000)
  </acceptance_criteria>
  <action>
1. 创建 `tests/test_agent/__init__.py`:
```python
"""Agent 模块测试"""
```

2. 创建 `tests/test_agent/conftest.py`:

```python
"""Agent 测试共享 fixtures"""

import pytest
import asyncio
from typing import Generator

# pytest-asyncio 配置
pytest_plugins = ('pytest_asyncio',)


@pytest.fixture(scope="session")
def event_loop() -> Generator:
    """创建事件循环供所有测试使用"""
    loop = asyncio.get_event_loop_policy().new_event_loop()
    yield loop
    loop.close()


@pytest.fixture
def api_base_url() -> str:
    """API 基础 URL"""
    return "http://localhost:8000"


@pytest.fixture
def test_user_id() -> str:
    """测试用户 ID"""
    return "test-user-001"


@pytest.fixture
def mock_workspace(tmp_path) -> str:
    """创建临时工作空间目录"""
    workspace = tmp_path / "workspaces" / "test-user"
    workspace.mkdir(parents=True, exist_ok=True)
    return str(workspace)
```

检查 pyproject.toml 中的 pytest 配置，如需要可添加 pytest-asyncio 依赖。
</action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); from tests.test_agent.conftest import *; print('fixtures_ok')" 2>&1 | head -5</automated>
  </verify>
  <done>
测试目录和基础配置已创建，pytest 可发现测试
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建工具注册测试</name>
  <files>tests/test_agent/test_tools.py</files>
  <read_first>
    - src/agent/tools.py
  </read_first>
  <acceptance_criteria>
    - `tests/test_agent/test_tools.py` exists
    - Test `test_security_tools_server_exists` verifies security_tools.name == "security"
    - Test `test_query_alerts_tool_exists` verifies tool exists in security_tools.tools
    - Test `test_analyze_chain_tool_exists` verifies tool exists in security_tools.tools
    - Test `test_tool_descriptions` verifies query_alerts has "查询" description
    - Tests can run with `pytest tests/test_agent/test_tools.py -x -v`
  </acceptance_criteria>
  <action>
创建 `tests/test_agent/test_tools.py`:

```python
"""工具注册测试"""

import pytest
from src.agent.tools import security_tools, query_alerts, analyze_chain


def test_security_tools_server_exists():
    """验证 security_tools MCP server 存在"""
    assert security_tools is not None
    assert security_tools.name == "security"
    assert security_tools.version == "1.0.0"


def test_query_alerts_tool_exists():
    """验证 query_alerts 工具已注册"""
    tool_names = [t.name for t in security_tools.tools]
    assert "query_alerts" in tool_names


def test_analyze_chain_tool_exists():
    """验证 analyze_chain 工具已注册"""
    tool_names = [t.name for t in security_tools.tools]
    assert "analyze_chain" in tool_names


def test_tool_count():
    """验证工具数量"""
    assert len(security_tools.tools) >= 2


def test_query_alerts_signature():
    """验证 query_alerts 工具签名"""
    # query_alerts 是被 @tool 装饰的函数
    assert callable(query_alerts)
    assert query_alerts.__name__ == "query_alerts"


def test_analyze_chain_signature():
    """验证 analyze_chain 工具签名"""
    assert callable(analyze_chain)
    assert analyze_chain.__name__ == "analyze_chain"
```

运行测试:
```bash
pytest tests/test_agent/test_tools.py -x -v
```
</action>
  <verify>
    <automated>python -c "from src.agent.tools import security_tools; print(f'Tools: {[t.name for t in security_tools.tools]}')"</automated>
  </verify>
  <done>
工具注册测试已创建，所有测试可通过
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 Agent 客户端测试</name>
  <files>tests/test_agent/test_client.py</files>
  <read_first>
    - src/agent/client.py
    - src/agent/config.py
  </read_first>
  <acceptance_criteria>
    - `tests/test_agent/test_client.py` exists
    - Test `test_create_agent_client` verifies function returns AgentClient
    - Test `test_agent_client_config` verifies config contains correct values
    - Test `test_client_has_query_method` verifies query method exists and is async
    - Test `test_client_has_close_method` verifies close method exists
    - Tests use pytest.mark.asyncio where needed
    - Tests can run with `pytest tests/test_agent/test_client.py -x -v`
  </acceptance_criteria>
  <action>
创建 `tests/test_agent/test_client.py`:

```python
"""Agent 客户端测试"""

import pytest
from src.agent.client import AgentClient, create_agent_client
from src.agent.config import get_agent_config


def test_create_agent_client():
    """验证 create_agent_client 工厂函数"""
    client = create_agent_client("test-user-001")
    assert isinstance(client, AgentClient)
    assert client.user_id == "test-user-001"


def test_agent_client_config():
    """验证客户端配置正确"""
    client = AgentClient("test-user-001")
    config = client._config

    assert config["permission_mode"] == "acceptEdits"
    assert "test-user-001" in config["cwd"]
    assert "system_prompt" in config
    assert "allowed_tools" in config


def test_client_has_query_method():
    """验证 query 方法存在"""
    client = AgentClient("test-user-001")
    assert hasattr(client, "query")
    import inspect
    assert inspect.iscoroutinefunction(client.query)


def test_client_has_close_method():
    """验证 close 方法存在"""
    client = AgentClient("test-user-001")
    assert hasattr(client, "close")
    import inspect
    assert inspect.iscoroutinefunction(client.close)


def test_allowed_tools_includes_security_tools():
    """验证 allowed_tools 包含安全工具"""
    config = get_agent_config("test-user")
    allowed = config["allowed_tools"]

    assert "mcp__security__query_alerts" in allowed
    assert "mcp__security__analyze_chain" in allowed
    assert "acceptEdits" in [tool for tool in allowed if "accept" in str(tool)]


def test_max_steps_configured():
    """验证 max_steps 配置"""
    config = get_agent_config("test-user")
    assert config.get("max_steps") == 10
```

运行测试:
```bash
pytest tests/test_agent/test_client.py -x -v
```
</action>
  <verify>
    <automated>python -c "from src.agent.client import create_agent_client; c = create_agent_client('test'); print(f'client created: {c.user_id}')"</automated>
  </verify>
  <done>
Agent 客户端测试已创建，所有测试可通过
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建 WebSocket 端点测试</name>
  <files>tests/test_agent/test_websocket.py</files>
  <read_first>
    - src/api/agent_endpoints.py
  </read_first>
  <acceptance_criteria>
    - `tests/test_agent/test_websocket.py` exists
    - Test `test_agent_router_exists` verifies router is APIRouter
    - Test `test_ws_endpoint_registered` verifies /ws/chat/{user_id} route exists
    - Test `test_deepseek_fallback_function` verifies fallback function exists
    - Tests use WebSocket test client (fastapi.testclient or websockets)
    - Tests can run with `pytest tests/test_agent/test_websocket.py -x -v`
  </acceptance_criteria>
  <action>
创建 `tests/test_agent/test_websocket.py`:

```python
"""WebSocket 端点测试"""

import pytest
from fastapi.testclient import TestClient
from src.api.agent_endpoints import router, call_deepseek_fallback


def test_agent_router_exists():
    """验证 router 是 APIRouter"""
    from fastapi import APIRouter
    assert isinstance(router, APIRouter)


def test_ws_endpoint_registered():
    """验证 WebSocket 端点已注册"""
    from src.api.main import app
    route_paths = [r.path for r in app.routes]
    assert any("/ws/chat" in str(p) for p in route_paths)


def test_deepseek_fallback_exists():
    """验证 DeepSeek fallback 函数存在"""
    import inspect
    assert callable(call_deepseek_fallback)
    assert inspect.iscoroutinefunction(call_deepseek_fallback)


def test_ws_endpoint_accepts_connection(mock_workspace, monkeypatch):
    """验证 WebSocket 端点接受连接"""
    import os
    # Mock workspace path
    monkeypatch.setenv("DEEPSEEK_API_KEY", "test-key")

    from fastapi import FastAPI
    from src.api.agent_endpoints import router

    app = FastAPI()
    app.include_router(router)

    client = TestClient(app)

    # 注意: TestClient 的 WebSocket 测试需要特殊处理
    with client.websocket_connect("/ws/chat/test-user-001") as websocket:
        # 发送测试消息
        import json
        test_message = {"message": "test", "context": {}}
        websocket.send_text(json.dumps(test_message))

        # 接收响应 (可能是 error 或 fallback)
        try:
            data = websocket.receive_json()
            assert "type" in data
        except Exception:
            pass  # 预期可能失败 (无 API key)
```

运行测试:
```bash
pytest tests/test_agent/test_websocket.py -x -v
```
</action>
  <verify>
    <automated>python -c "from src.api.agent_endpoints import router; print(f'Router prefix: {router.prefix}')"</automated>
  </verify>
  <done>
WebSocket 端点测试已创建
  </done>
</task>

<task type="auto">
  <name>Task 5: 创建 Fallback 机制测试</name>
  <files>tests/test_agent/test_fallback.py</files>
  <read_first>
    - src/api/agent_endpoints.py
  </read_first>
  <acceptance_criteria>
    - `tests/test_agent/test_fallback.py` exists
    - Test `test_fallback_requires_api_key` verifies fallback returns error when no key
    - Test `test_fallback_with_api_key` verifies fallback calls DeepSeek API when configured
    - Test `test_fallback_handles_network_error` verifies fallback handles network errors gracefully
    - Tests can run with `pytest tests/test_agent/test_fallback.py -x -v`
  </acceptance_criteria>
  <action>
创建 `tests/test_agent/test_fallback.py`:

```python
"""Fallback 机制测试"""

import pytest
from unittest.mock import patch, AsyncMock
from src.api.agent_endpoints import call_deepseek_fallback


@pytest.mark.asyncio
async def test_fallback_requires_api_key(monkeypatch):
    """验证 fallback 在没有 API key 时返回错误"""
    monkeypatch.delenv("DEEPSEEK_API_KEY", raising=False)

    result = await call_deepseek_fallback(
        system_prompt="You are a helpful assistant.",
        user_message="Hello"
    )

    assert "错误" in result
    assert "API Key" in result


@pytest.mark.asyncio
async def test_fallback_with_mock_api(monkeypatch):
    """验证 fallback 调用 DeepSeek API"""
    import httpx

    # Mock httpx.AsyncClient.post
    mock_response = AsyncMock()
    mock_response.raise_for_status = AsyncMock()
    mock_response.json = AsyncMock(return_value={
        "choices": [{"message": {"content": "Hello from DeepSeek!"}}]
    })

    mock_client = AsyncMock()
    mock_client.post = AsyncMock(return_value=mock_response)
    mock_client.__aenter__ = AsyncMock(return_value=mock_client)
    mock_client.__aexit__ = AsyncMock()

    monkeypatch.setenv("DEEPSEEK_API_KEY", "test-key")

    with patch("httpx.AsyncClient", return_value=mock_client):
        result = await call_deepseek_fallback(
            system_prompt="You are a helpful assistant.",
            user_message="Hello"
        )

        assert "Hello from DeepSeek" in result


@pytest.mark.asyncio
async def test_fallback_handles_network_error(monkeypatch):
    """验证 fallback 处理网络错误"""
    import httpx

    monkeypatch.setenv("DEEPSEEK_API_KEY", "test-key")

    # Mock httpx.AsyncClient.post to raise HTTPError
    mock_client = AsyncMock()
    mock_client.post = AsyncMock(side_effect=httpx.HTTPError("Connection failed"))
    mock_client.__aenter__ = AsyncMock(return_value=mock_client)
    mock_client.__aexit__ = AsyncMock()

    with patch("httpx.AsyncClient", return_value=mock_client):
        result = await call_deepseek_fallback(
            system_prompt="You are a helpful assistant.",
            user_message="Hello"
        )

        assert "失败" in result or "error" in result.lower()


@pytest.mark.asyncio
async def test_fallback_uses_correct_base_url(monkeypatch):
    """验证 fallback 使用正确的 base URL"""
    import httpx

    monkeypatch.setenv("DEEPSEEK_API_KEY", "test-key")
    monkeypatch.setenv("DEEPSEEK_BASE_URL", "https://custom.deepseek.com")

    called_url = []

    async def mock_post(url, **kwargs):
        called_url.append(url)
        mock_response = AsyncMock()
        mock_response.raise_for_status = AsyncMock()
        mock_response.json = AsyncMock(return_value={
            "choices": [{"message": {"content": "OK"}}]
        })
        return mock_response

    mock_client = AsyncMock()
    mock_client.post = mock_post
    mock_client.__aenter__ = AsyncMock(return_value=mock_client)
    mock_client.__aexit__ = AsyncMock()

    with patch("httpx.AsyncClient", return_value=mock_client):
        await call_deepseek_fallback("system", "user")

        assert any("custom.deepseek.com" in url for url in called_url)
```

运行测试:
```bash
pytest tests/test_agent/test_fallback.py -x -v
```
</action>
  <verify>
    <automated>python -c "from src.api.agent_endpoints import call_deepseek_fallback; import inspect; print(f'Fallback is async: {inspect.iscoroutinefunction(call_deepseek_fallback)}')"</automated>
  </verify>
  <done>
Fallback 机制测试已创建，所有测试可通过
  </done>
</task>

</tasks>

<verification>
## 整体验证

运行完整测试套件:
```bash
pytest tests/test_agent/ -x -v
```

预期输出:
- test_tools.py: 6 passed
- test_client.py: 7 passed
- test_websocket.py: 4 passed
- test_fallback.py: 4 passed
</verification>

<success_criteria>
1. 测试框架可运行 (pytest-asyncio 配置正确)
2. 工具注册测试全部通过
3. Agent 客户端测试全部通过
4. WebSocket 端点测试全部通过
5. Fallback 机制测试全部通过
6. `pytest tests/test_agent/ -v` 全部通过
</success_criteria>

<output>
完成后创建 `.planning/phases/13-claude-code-ai/13-03-SUMMARY.md`
</output>
