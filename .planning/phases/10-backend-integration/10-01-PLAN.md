---
phase: 10-backend-integration
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - frontend/src/components/ui/Button.tsx
  - frontend/src/pages/AlertDetailPage.tsx
  - frontend/src/lib/utils.ts
  - frontend/src/pages/AlertListPage.tsx
  - frontend/src/components/chat/ChatDialog.tsx
  - frontend/src/components/chat/ChatHeader.tsx
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/src/components/chat/ChatMessage.tsx
  - frontend/src/components/chat/ChatMessageList.tsx
  - frontend/src/components/chat/ContextIndicator.tsx
  - frontend/src/stores/chatStore.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "npm run build 成功完成无错误"
    - "TypeScript 编译错误全部修复"
  artifacts:
    - path: "frontend/src/components/ui/button.tsx"
      provides: "唯一的 Button 组件 (小写)"
    - path: "frontend/src/lib/utils.ts"
      provides: "formatDate 函数已导出"
    - path: "frontend/src/stores/chatStore.ts"
      provides: "updateLastMessage 支持函数式更新"
  key_links:
    - from: "frontend/src/pages/AlertDetailPage.tsx"
      to: "frontend/src/components/ui/button.tsx"
      via: "import"
      pattern: "from.*ui/button"
    - from: "frontend/src/pages/AlertListPage.tsx"
      to: "frontend/src/lib/utils"
      via: "import formatDate"
      pattern: "import.*formatDate"
---

<objective>
修复所有 pre-existing TypeScript 编译错误，确保前端构建通过。

目的：清理 Phase 04/06/07 引入的技术债务，为 Phase 10 后端联调做准备。
输出：npm run build 成功
</objective>

<context>
@frontend/src/components/ui/button.tsx
@frontend/src/components/ui/Button.tsx
@frontend/src/lib/utils.ts
@frontend/src/stores/chatStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修复 Button.tsx 大小写冲突</name>
  <files>frontend/src/components/ui/Button.tsx, frontend/src/pages/AlertDetailPage.tsx</files>
  <read_first>
    - frontend/src/components/ui/button.tsx (新的 shadcn/ui 风格组件)
    - frontend/src/components/ui/Button.tsx (旧的 uppercase 组件)
    - frontend/src/pages/AlertDetailPage.tsx (导入 Button 的页面)
  </read_first>
  <action>
    1. 删除 `frontend/src/components/ui/Button.tsx` (大写版本)
    2. 修改 AlertDetailPage.tsx 第 10 行:
       FROM: import { Button } from '../components/ui/Button';
       TO:   import { Button } from '../components/ui/button';
    3. 检查其他文件是否导入 '../components/ui/Button' (大写)，全部改为 '../components/ui/button' (小写)
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npm run build 2>&1 | grep -c "Button.tsx"</automated>
  </verify>
  <done>AlertDetailPage.tsx 导入小写 button.tsx，npm run build 不再报 Button 大小写冲突错误</done>
</task>

<task type="auto">
  <name>Task 2: 添加 formatDate 到 utils.ts 并修复 AlertListPage 导入</name>
  <files>frontend/src/lib/utils.ts, frontend/src/pages/AlertListPage.tsx</files>
  <read_first>
    - frontend/src/lib/utils.ts (当前只导出 cn)
    - frontend/src/pages/AlertListPage.tsx (第 15 行导入 formatDate)
  </read_first>
  <action>
    1. 在 utils.ts 末尾添加 formatDate 函数:
       ```typescript
       export function formatDate(date: string | Date): string {
         const d = typeof date === 'string' ? new Date(date) : date;
         return d.toLocaleString('zh-CN', {
           year: 'numeric',
           month: '2-digit',
           day: '2-digit',
           hour: '2-digit',
           minute: '2-digit',
           second: '2-digit'
         });
       }
       ```
    2. AlertListPage.tsx 导入已正确 (import { formatDate } from '../lib/utils')，无需修改
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npm run build 2>&1 | grep -c "formatDate"</automated>
  </verify>
  <done>formatDate 已导出，AlertListPage.tsx 可以正确导入和使用</done>
</task>

<task type="auto">
  <name>Task 3: 修复 ChatInput.tsx 函数式更新类型错误</name>
  <files>frontend/src/components/chat/ChatInput.tsx, frontend/src/stores/chatStore.ts</files>
  <read_first>
    - frontend/src/stores/chatStore.ts (updateLastMessage 类型定义在第 40 行和第 68-78 行)
    - frontend/src/components/chat/ChatInput.tsx (第 60 和 68 行传入函数)
  </read_first>
  <action>
    修改 chatStore.ts 的 updateLastMessage 以支持函数式更新:
    1. 第 40 行接口定义改为: updateLastMessage: (content: string | ((prev: string) => string)) => void;
    2. 第 68-78 行实现改为:
       ```typescript
       updateLastMessage: (content) =>
         set((state) => {
           const messages = [...state.messages];
           if (messages.length > 0) {
             const lastMessage = messages[messages.length - 1];
             const newContent = typeof content === 'function'
               ? content(lastMessage.content)
               : content;
             messages[messages.length - 1] = {
               ...lastMessage,
               content: newContent
             };
           }
           return { messages };
         }),
       ```
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npm run build 2>&1 | grep -c "updateLastMessage"</automated>
  </verify>
  <done>updateLastMessage 支持函数式更新，ChatInput.tsx 第 60 和 68 行类型检查通过</done>
</task>

<task type="auto">
  <name>Task 4: 修复 Chat 组件未使用导入</name>
  <files>frontend/src/components/chat/ChatDialog.tsx, frontend/src/components/chat/ChatHeader.tsx, frontend/src/components/chat/ChatMessage.tsx, frontend/src/components/chat/ChatMessageList.tsx, frontend/src/components/chat/ContextIndicator.tsx</files>
  <read_first>
    - frontend/src/components/chat/ChatDialog.tsx
    - frontend/src/components/chat/ChatHeader.tsx
    - frontend/src/components/chat/ChatMessage.tsx
    - frontend/src/components/chat/ChatMessageList.tsx
    - frontend/src/components/chat/ContextIndicator.tsx
  </read_first>
  <action>
    逐文件修复未使用导入:

    1. ChatDialog.tsx (第 7, 9 行):
       - 删除: import React from 'react'; (第 7 行)
       - 删除: import { X } from 'lucide-react'; (第 9 行) - 如果 X 未使用

    2. ChatHeader.tsx (第 5 行):
       - 删除: import React from 'react';

    3. ChatMessage.tsx (第 5 行):
       - 删除: import React from 'react';

    4. ChatMessageList.tsx (第 7 行):
       - 删除: import React from 'react';

    5. ContextIndicator.tsx (第 7 行):
       - 删除: import React from 'react';
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npm run build 2>&1 | grep -c "TS6133"</automated>
  </verify>
  <done>所有 Chat 组件不再有未使用的 React 导入警告</done>
</task>

<task type="auto">
  <name>Task 5: 移除 chatStore.ts 未使用的 get 参数</name>
  <files>frontend/src/stores/chatStore.ts</files>
  <read_first>
    - frontend/src/stores/chatStore.ts (第 47 行)
  </read_first>
  <action>
    修改 chatStore.ts 第 47 行:
    FROM: (set, get) => ({
    TO:   (set) => ({
    (get 参数未被使用，可安全移除)
  </action>
  <verify>
    <automated>cd /home/admin/work/SecAlert/frontend && npm run build 2>&1 | grep -c "get.*declared"</automated>
  </verify>
  <done>chatStore.ts 第 47 行不再有未使用的 get 参数</done>
</task>

</tasks>

<verification>
所有修复完成后，执行完整构建验证:
```bash
cd /home/admin/work/SecAlert/frontend && npm run build
```

预期结果: 构建成功，无 TypeScript 错误
</verification>

<success_criteria>
- npm run build 成功完成，exit code 0
- 无 TypeScript 编译错误 (TS6133, TS2305, TS1261, TS2345 等)
- 所有 tech debt 问题已修复:
  - Button.tsx 大小写冲突已解决
  - formatDate 已导出
  - ChatInput updateLastMessage 类型正确
  - Chat 组件无未使用导入
  - chatStore get 参数已移除
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-integration/10-01-SUMMARY.md`
</output>
